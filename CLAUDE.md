# CLAUDE.md

The repository's agent instructions live in `AGENTS.md` so that every agent (Claude Code, Codex,
others) reads the same file. This file only imports it — put new rules in `AGENTS.md`, not here.

@AGENTS.md
