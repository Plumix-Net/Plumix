# Backlog — open work not tied to a single control row

The one place for work that is known to be open but does not fit `docs/CUPERTINO_TODO.md` (a
Cupertino file), `docs/ai/DIVERGENCES.md` (an intentional, documented divergence) or a qualified
marker in `docs/ai/PORT_MAP.md` (an admitted approximate port). Created 2026-08-16 from an audit of
the retired per-iteration notes; everything else in those notes was verified closed or already tracked.

Rules:

- One row per item: **what is open**, **where** (C# file + Dart source), **next step**. No history, no
  rationale beyond one clause.
- Remove the row in the same change that closes it (and add the `CHANGELOG.md` line).
- An iteration that ends blocked adds a row here (`PORT_PLAYBOOK.md` Step 8) instead of a note file.
- `PORT_PLAYBOOK.md` Step 0 reaches this file only after the Cupertino TODO, the divergence registry
  and the qualified-marker list are empty — but a row here may be picked any time it unblocks a control.

## Control-level gaps found outside their rows

| Item | Where | Next step |
| --- | --- | --- |
| `RenderRepaintBoundary.toImage`/`toImageSync` have no counterpart, because `OffsetLayer` cannot rasterize itself. | `src/Plumix/Rendering/RepaintBoundary.cs`, `src/Plumix/Rendering/Layer.cs` (`OffsetLayer`) vs `rendering/proxy_box.dart` (3486-3560), `rendering/layer.dart` | Give `OffsetLayer` a `ToImage(Rect, pixelRatio)` that runs `BuildScene` into an Avalonia `RenderTargetBitmap` context under Dart's `_createSceneForImage` transform, then add the two boundary methods and the `assert(!debugNeedsPaint)` guard. |
| `PipelineOwner.Request*For` conflate dirty-list insertion with `requestVisualUpdate`, so `markNeedsLayout`/`markNeedsPaint` ask for a frame twice and `scheduleInitialLayout`/`markNeedsCompositingBitsUpdate` ask where Dart does not. | `src/Plumix/PipelineOwner.cs` (`RequestLayoutFor`, `RequestPaintFor`, `RequestCompositingBitsUpdateFor`) vs `rendering/object.dart` (2683–2750, 3228, 3338–3418) | Make the `Request*For` methods pure list inserts, move `RequestVisualUpdate` to the exact `RenderObject` call sites Dart uses, and re-check the host's frame scheduling. |
| Flutter's `debugCheckHas*` helpers have no counterpart: `widgets/debug.dart` (`debugCheckHasMediaQuery`, `debugCheckHasTable`, `debugCheckHasWidgetsLocalizations`; `debugCheckHasDirectionality` and `debugCheckHasOverlay` are ported) and `material_ui/debug.dart` (`debugCheckHasMaterial`, `debugCheckHasMaterialLocalizations`, `debugCheckHasScaffold`, `debugCheckHasScaffoldMessenger`) are not ported, so the widgets that need those ancestors either fail late or throw a bare exception. | `src/Plumix/Widgets/Debug.cs`, `src/Plumix.Material` (no `Debug.cs`) vs `widgets/debug.dart`, `material_ui/lib/src/debug.dart` | Port both files over the `BuildContext.Describe*` members (which shipped 2026-09-07), then call each helper from the `Of` accessors that currently throw on their own. |
| `-c Release` and `-c Profile` are broken, so `AGENTS.md`'s claim that both configurations are green is wrong: ~10 tests fail (`FrameworkParityTests`, `BuildScopeTests`, the two `DebugFillProperties_*` tests) and the run then aborts on a stack overflow that recurses through `ComponentElement.PerformRebuild`, so most of the suite never runs. Pre-existing; unrelated to any current port. | `src/Plumix.Tests/FrameworkParityTests.cs`, `BuildScopeTests.cs`, `ScrollableTests.cs:154`, `ScrollViewTests.cs` vs `src/Plumix/Widgets/Framework.*.cs` | Fix the stack overflow first (it truncates every non-Debug run), then split the rest: mark the assert-only tests `DebugOnlyFact`, and decide whether `DebugFillProperties` should be unconditional. |
| The Debug test suite is intermittently red on a clean `main` (3 of 8 runs on 2026-09-25): the same ~54 Material size tests (`MaterialButtonsTests`, `MaterialListTileTests`, `MaterialInputDecoratorTests`, `MaterialRadioTests`, ...) come out 8px smaller, i.e. they see a compact `VisualDensity`, so some global platform/density state changes during the run. | `src/Plumix.Tests/Material*Tests.cs`, `src/Plumix.Material/VisualDensity`/`ThemeData` defaults, `DefaultTargetPlatform` | Run the suite with a per-test logger until it fails, find the test that precedes the first failure, and look for a platform override or theme default written from a timer, thread-pool continuation or unrestored `finally`. |
| `RelayoutWhenSystemFontsChangeMixin` is not ported; `Scheduler.ScheduleFrameCallback` shipped 2026-08-29, but the `PaintingBinding.systemFonts` listenable still does not exist. | `src/Plumix/Painting/*` vs `rendering/object.dart` (4693–4753) | Add a system-fonts `ChangeNotifier` fed by the hosts, then port the mixin onto `RenderParagraph`, `RenderEditable` (its `systemFontsDidChange` also marks the text painter dirty) and friends. |
| The platform-channel profiler is not ported: `kProfilePlatformChannels`, `debugProfilePlatformChannels`, `_ProfiledBinaryMessenger` (its `Platform Channel send` timeline task) and `_PlatformChannelStats`, plus the `profilePlatformChannels` service extension. | `src/Plumix/UI/PlatformChannel.cs`, `BinaryMessenger.cs` vs `services/platform_channel.dart` | Port the profiled messenger over `TimelineTask` (shipped with `FlutterTimeline`), route `BasicMessageChannel`/`MethodChannel.binaryMessenger` through it when the flag is on, then register the extension. |
| `ImageCache` carries a strict marker but is a thread-safe adaptation (a lock, an LRU linked list, disposal outside the lock) rather than Dart's single-isolate `LinkedHashMap` structure; its timeline events were ported onto that structure. | `src/Plumix/Rendering/ImageCache.cs` vs `painting/image_cache.dart` | Diff it top to bottom against Dart; either port the structure 1:1 (keeping disposal off the lock only if a host needs it) or add the `(adapted)` qualifier and a `DIVERGENCES.md` row. |
| `FlutterMemoryAllocations` is ported and `Element`/`State`/`ChangeNotifier` dispatch their created/disposed events, but Dart's other dispatch sites (`RenderObject`, `Layer`, `Ticker`, `AnimationController`, `TextPainter`, ...) do not, and the drawing backend has no image/picture creation hooks for `_subscribeToSdkObjects`. | `src/Plumix/Foundation/MemoryAllocations.cs`, `Debug.cs` vs `foundation/memory_allocations.dart` and every `debugMaybeDispatchCreated` call site in `flutter/lib/src` | Grep the pinned sources for `debugMaybeDispatch` and add each call at the matching C# constructor/`Dispose`. |
| `SliverMultiBoxAdaptorElement.ForgetChild` and `TwoDimensionalViewportElement.ForgetChild` keep weakened asserts because `Element.DeactivateChild` used to call `ForgetChild`; since the framework.dart tightening it no longer does. | `src/Plumix/Widgets/Scroll.cs`, `TwoDimensionalViewport.cs` vs `widgets/sliver.dart`, `two_dimensional_viewport.dart` | Restore Dart's `assert(_childElements.containsKey(child.slot))`/`assert(!_debugIsDoingLayout)`, drop the defensive lookups, and trim the matching `DIVERGENCES.md` row. |
| Material `TextField` and `SelectableText` have no `selectionHeightStyle`/`selectionWidthStyle` parameters, so they always get `EditableText`'s defaults. | `src/Plumix.Material/TextField.cs`, `SelectableText.cs` vs `material_ui/lib/src/text_field.dart`, `selectable_text.dart` | Add both nullable parameters and pass them to `EditableText` unresolved, as `CupertinoTextField` does. |
| `ButtonThemeData` has no `colorScheme` field, so `ThemeData`'s `buttonTheme` default cannot pass one as Dart does | `src/Plumix.Material/ButtonTheme.cs` (`material_ui/lib/src/button_theme.dart`); consumed by `ThemeData` | Port `ButtonThemeData.colorScheme` (and the `getTextColor`/`getFillColor` paths that read it), then pass `colorScheme` from `ThemeData`'s `buttonTheme` default. |
| Two debug-only pieces of the `Hero` port are missing: Dart's `NavigatorState._updateHeroController` reports a `FlutterError` from a post-frame callback when one `HeroController` ends up owned by two navigators, and `Hero`/`HeroMode` implement `debugFillProperties` (`tag`, `mode`). | `src/Plumix/Widgets/Navigation.NavigatorState.cs` (`UpdateHeroController`), `src/Plumix/Widgets/Hero.cs` vs `flutter/.../widgets/navigator.dart`, `heroes.dart` | Port the ownership check over `FlutterError.ReportError` (now available); add the two `DebugFillProperties` overrides. Neither affects runtime behavior. |
| `BuildOwner.FlushBuild` drains the transient frame callbacks itself (`Scheduler.RunScheduledFrameCallbacksOutsideFrame`), because a widget-test harness pumps by flushing build/layout/paint by hand and never produces a frame — and `LayoutBuilder`'s scope defers its rebuild request to one. The same gap is already worked around in `RenderOverlayChildLayoutBox.PerformLayout`. | `src/Plumix/Widgets/Framework.BuildOwner.cs` (`FlushBuild`), `src/Plumix/Scheduler.cs`, `src/Plumix/Rendering/Overlay.cs` | Drive the harness pumps through `Scheduler.HandleFrame` so a real frame runs the transient callbacks, then drop both work-arounds. |
| `ListTile`/`ListTileThemeData.IconColor`/`TextColor` and `ToggleButtons`/`ToggleButtonsThemeData.FillColor` are typed `WidgetStateProperty<Color?>?` where Dart declares a plain `Color?` that may happen to be a `WidgetStateColor`; callers now have to write `WidgetStateProperty<Color?>.All(color)`. | `src/Plumix.Material/ListTile.cs`, `ListTileTheme.cs`, `ToggleButtons.cs`, `ToggleButtonsTheme.cs` vs `material_ui/lib/src/list_tile.dart`, `toggle_buttons.dart` | Retype the four slots to `WidgetStateColor?` (which converts implicitly from `Color`, like `AppBar.backgroundColor` already does) and keep the state resolution inside, the way Dart's `_IndividualOverrides` does. |
| The service-extension registry (`BindingBase.Register*ServiceExtension`, `PostEvent`, `InvokeServiceExtensionAsync`) carries the rendering and `timeDilation` extensions and the `Flutter.Frame` event, but `BindingBase.initServiceExtensions`' own six (`reassemble`, `exit`, `connectedVmServiceUri`, `activeDevToolsServerAddress`, `platformOverride`, `brightnessOverride`), the services/painting/widgets extensions, and rendering's `invertOversizedImages` (Plumix's `PaintImage` has no oversized-image check) are not registered, and nothing outside the process can reach the registry. | `src/Plumix/Foundation/Binding.cs`, `src/Plumix/RendererBinding.cs` (`InitServiceExtensions`), `src/Plumix/Rendering/DecorationImage.cs` vs `foundation/binding.dart`, `painting/decoration_image.dart` (`paintImage` debug block), `widgets/binding.dart`, `services/binding.dart` | Register the foundation six over a `reassembleApplication` that runs `LockEvents(PerformReassemble)`, port `paintImage`'s `debugInvertOversizedImages` block and register it, then the widgets/services extensions; a DevTools bridge can then call `InvokeServiceExtensionAsync`. |
| `RenderView.HitTest` bounds-checks the view itself through `RenderBox.HitTest`, while Dart's `RenderView.hitTest` never checks its size and always adds itself; twelve Plumix tests read a `false` root result as a miss. | `src/Plumix/RenderView.cs` vs `rendering/view.dart` (`hitTest`) | Port Dart's `hitTest`, then change those tests (`GesturePipelineTests`, `MaterialCheckboxTests`, `MaterialButtonsTests`, `MaterialRadioTests`) to assert on `result.Path` instead of the returned bool. `ProxyBoxTransformParityTests.Transform_TranslatedChildIntoTranslatedBox_HitTest` is skipped on it. |
| Several render objects still drive Avalonia `TextLayout` directly instead of `TextPainter`, so they keep the `TextLayoutFallback` estimate in headless runs: the `Slider`/`RangeSlider` value indicators, `TimePickerDialog`, `RenderButton`, `Banner` and `DebugOverflowIndicator`. | `src/Plumix.Material/Slider.cs`, `RangeSlider.cs`, `TimePickerDialog.cs`, `src/Plumix/RenderButton.cs`, `Widgets/Banner.cs`, `Rendering/DebugOverflowIndicator.cs` | Move each onto `TextPainter` the way its Dart source does, then delete `UI/TextLayoutFallback.cs`. |
| `RenderSliverList`, `RenderSliverGrid`, `SliverGridLayout`/`SliverGridRegularTileLayout` and the two grid delegates live in `Sliver.cs` without their own `Dart parity source` markers, so `PORT_MAP.md` does not map `sliver_list.dart` or `sliver_grid.dart` and nobody has diffed them against Dart; the sliver-protocol pass of 2026-09-22 only tightened `sliver.dart`, `proxy_sliver.dart`'s protocol members and `sliver_padding.dart`. | `src/Plumix/Rendering/Sliver.cs` vs `rendering/sliver_list.dart`, `rendering/sliver_grid.dart` | Move each into its own file with a marker, diff it top to bottom (both files are under 800 lines), port the gaps and Flutter's `sliver_list`/`sliver_grid` rendering tests. |
| Three slivers keep a Plumix-specific structure that the protocol port did not touch: `RenderSliverPersistentHeader` derives from `RenderSliverSingleBoxAdapter` and stores its child offset in `SliverPhysicalParentData`, where Dart extends `RenderSliver` with `RenderObjectWithChildMixin<RenderBox>`/`RenderSliverHelpers` and computes the offset in `paint`/`applyPaintTransformForBoxChild`; `RenderSliverResizingHeader` sets up `BoxParentData`; `RenderSliverOpacity` paints through a composited `OpacityLayer` instead of `context.pushOpacity` with `_alpha`. Behavior matches today (hit testing already goes through `hitTestBoxChild`). | `src/Plumix/Rendering/SliverPersistentHeader.cs`, `SliverHeaders.cs`, `Sliver.cs` (`RenderSliverOpacity`) vs `rendering/sliver_persistent_header.dart`, `widgets/sliver_resizing_header.dart`, `rendering/proxy_sliver.dart` | Rebase the persistent header onto `RenderSliver` with Dart's `paint`/`applyPaintTransform`, switch the resizing header to Dart's parent data, and port `RenderSliverOpacity.paint` over `PushOpacity`. |
| `FrameworkDartTester` still differs from flutter_test's binding: semantics are off unless a test holds a handle (`testWidgets` defaults to `semanticsEnabled: true`), it mounts a plain `RenderView` instead of the binding's reusable one, `Pump` always runs a frame even when none is scheduled, and its finders include offstage elements. The scrollable ports worked around each privately (`DartSemantics`, `SemanticsTester`, `TestRestorationBinding`, offstage-skipping finders, `KeySender`, `TestButton`/`TestTextField`). | `src/Plumix.Tests/FrameworkDartTester*.cs` vs `flutter_test/lib/src/binding.dart`, `finders.dart`, `restoration.dart`; the private helpers in `Scrollable*DartParityTests.cs`, `ScrollSemanticsDartParityTests.cs`, `TwoDimensionalScrollableDartParityTests.cs` | Move the helpers into shared harness files, enable semantics by default and mount through `ReusableRenderView`, skip the frame when nothing is scheduled, then delete the private copies. |
| `WidgetStateTextStyle` is still a wrapper around a resolver, although `TextStyle` has been a non-sealed class since the 2026-09-24 strict port; Dart's `WidgetStateTextStyle extends TextStyle implements WidgetStateProperty<TextStyle>`, so any `TextStyle?` slot can hold one. | `src/Plumix/Widgets/WidgetState.cs`, `src/Plumix.Material/InputDecorator.cs`, `InputDecoratorTheme.cs` vs `widgets/widget_state.dart` | Derive `WidgetStateTextStyle` from `TextStyle` (implementing `WidgetStateProperty<TextStyle>`), retype the wrapper-typed slots back to `TextStyle?` with `is WidgetStateProperty<TextStyle>` resolution, and trim the `DIVERGENCES.md` row. |
| `SemanticsBinding` does not own the accessibility features: `accessibilityFeatures`, `handleAccessibilityFeaturesChanged`, `disableAnimations` (with `debugSemanticsDisableAnimations`) and `createSemanticsUpdateBuilder` are missing, and `WidgetsBinding.HandleAccessibilityFeaturesChanged(features)` keeps the value instead. | `src/Plumix/Rendering/Semantics.Binding.cs`, `src/Plumix/Widgets/AppLifecycleListener.cs` vs `semantics/binding.dart`, `widgets/binding.dart` | Add `PlatformDispatcher.AccessibilityFeatures`/`OnAccessibilityFeaturesChanged` (with Dart's persistent-phase post-frame deferral), move the value onto `SemanticsBinding`, and make the widgets binding override `HandleAccessibilityFeaturesChanged` the way Dart does. |
| `WidgetsBinding.DrawFrame` has no first-frame reporting: `firstFrameRasterized`, `waitUntilFirstFrameRasterized`, `debugDidSendFirstFrameEvent`, the `Flutter.FirstFrame` event, the `Rasterized first useful frame`/`Widgets built first useful frame` timeline instants and the `debugBuildingDirtyElements` guard are missing; `Localizations` never defers the first frame because its delegates load synchronously. | `src/Plumix/Widgets/WidgetsBindingBootstrap.cs`, `src/Plumix/Widgets/Localizations.cs` vs `widgets/binding.dart` (`drawFrame`), `widgets/localizations.dart` (`load`) | Port the timings-callback half of `drawFrame` over `Scheduler.AddTimingsCallback`, then make delegate loading asynchronous and defer/allow the first frame around it. |
| Hot reload still reassembles per host (`HotReloadManager` → `PlumixHost.ReassembleApplication`) instead of Dart's `BindingBase.reassembleApplication` → `lockEvents(performReassemble)` chain, so `RendererBinding.PerformReassemble` (views, warm-up frame, `EndOfFrame`) is not on the hot-reload path and `FlutterError.ResetErrorCount` is never called. | `src/Plumix/HotReload.cs`, `src/Plumix/FlutterHost.cs`, `src/Plumix/WidgetHost.cs` vs `foundation/binding.dart`, `widgets/binding.dart`, `rendering/binding.dart` (`performReassemble`) | Add `BindingBase.ReassembleApplication` running `Scheduler.LockEvents` over widgets-then-renderer `PerformReassemble`, call it from `HotReloadManager`, and drop the host overrides. |
| `ScrollableDetails` lacks Dart's deprecated `clipBehavior` constructor parameter, `copyWith` and diagnostics `toString`, so the three `ScrollableDetails` tests of `scrollable_helpers_test.dart` are not ported. | `src/Plumix/Widgets/ScrollConfiguration.cs` (`ScrollableDetails`) vs `widgets/scrollable_helpers.dart` | Port the three members, then the three tests next to `ScrollableMiscDartParityTests`. |

## Pending runtime verification

| Item | Where | Next step |
| --- | --- | --- |
| Sliver-protocol desktop verification is open: after `RenderSliver` moved onto `RenderObject` (2026-09-22) the desktop sample starts and runs without errors, but this environment cannot capture the screen, so tapping list items, pinned/floating headers and pull-to-refresh on a real host is unobserved. Viewport hit tests in all four directions pass framework tests. | `src/Plumix/Rendering/Sliver.cs`, `Viewport.RenderViewport.cs` | In an interactive desktop session, scroll and tap items in the list, grid, sliver app bar and Cupertino refresh demos, and compare against `dart_sample`. |
| Platform launch-route desktop visual verification remains open: the sample stays running, but the UI tool rejects its unbundled executable. Initial-route precedence, Router parsing and history replacement pass framework tests. | `src/Plumix/UI/PlatformDispatcher.cs`, `src/Plumix/Widgets/App.cs`, `src/Plumix/Widgets/Router.cs` | Run the desktop host interactively with `PlatformDispatcher.Instance.DefaultRouteName` set before mounting; confirm the initial screen and first history report for a non-root launch route. |
| Semantics empty-string sample verification is open: desktop launch stays running, but the UI tool cannot resolve the unbundled executable. | `src/Sample/Plumix.Sample/Demos/General/StateStorageDemoPage.cs`, `dart_sample/lib/demos/general/state_storage_demo_page.dart` | Open PageStorage + SharedAppData interactively and verify the readout still renders and updates under its identified semantics wrapper; non-null text and merge geometry are covered by framework tests. |
| `SingleChildScrollView` desktop visual verification remains open: the sample enters the native event loop, but the UI tool cannot attach to its window. | `src/Sample/Plumix.Sample/Demos/General/ScrollPhysicsDemoPage.cs`, `dart_sample/lib/demos/general/scroll_physics_demo_page.dart` | Open Scroll physics in an interactive desktop session and verify the horizontal strip, clipping and directional padding; framework tests and Dart analysis pass. |
| The renderer binding now lays out, paints and composites every registered view from its persistent frame callback (`RendererBinding.DrawFrame` → `RenderView.CompositeFrame` → `FlutterView.Render`, which invalidates the host), and the host skips compositing while the first frame is deferred; unverified on a real host because Avalonia.Native cannot start its render timer in this environment (`-6661`, with or without the sandbox). | `src/Plumix/RendererBinding.cs`, `src/Plumix/FlutterHost.cs` (`Render`, `HandleViewRenderRequested`), `src/Plumix/RenderView.cs` | In an interactive desktop session, open the gallery, scroll, resize the window and trigger a rebuild; confirm frames appear without a one-frame lag and that nothing is drawn twice. |
| The paragraph engine switch (`TextPainter` over `UI/AvaloniaParagraph.cs`) and the rich-text demo's fade, ellipsis and strut section and its painted-span section (`backgroundColor`, `foreground` paint, tabular `fontFeatures`, 2026-09-24) are unverified on a real host: in this environment the desktop sample exits at once with code 0 and no output, exactly like the unmodified previous commit. | `src/Plumix/UI/AvaloniaParagraph.cs`, `src/Sample/Plumix.Sample/Demos/General/RichTextDemoPage.cs`, `dart_sample/lib/demos/general/rich_text_demo_page.dart` | Open the RichText demo in an interactive desktop session and compare wrapping, the one-line fade and ellipsis, the strut line spacing, the highlight/painted/tabular spans and span taps with `dart_sample`. |
| `EditableText`'s own `Scrollable` (2026-09-25) and its Actions-based keys with the host's emulated text input plugin (macOS arrow/delete selectors, Enter) are unverified on a real host: the desktop sample starts and runs without errors, but the window cannot be observed here. Reveal, padding, keyboard-inset, toolbar, shortcut, selector and gesture behavior pass framework tests. | `src/Plumix/Widgets/EditableText.cs`, `src/Sample/Plumix.Sample/Demos/General/EditableTextDemoPage.cs`, `dart_sample/lib/demos/general/editable_text_demo_page.dart` | Open the EditableText demo interactively, watch the caret blink (and the fading caret field), type into the PIN field on a phone (the last character shows for ~1.5 s), type past the end of the 220px field and the 3-line notes, move with arrows/Option+arrows, press Enter and Cmd+Z in the undo field, and compare with `dart_sample`. |

## Host-level gaps (platform adapters, `src/Plumix/FlutterHost.cs` and per-host projects)

| Item | Where | Next step |
| --- | --- | --- |
| `MediaQueryData.GestureSettings` exists (recognizers read it through `MediaQuery.MaybeGestureSettingsOf`) but no host ever sets it, so every recognizer falls back to the hard-coded `GestureConstants.TouchSlop`. Dart's `MediaQuery.fromView` fills it from `view.gestureSettings` (Android's `ViewConfiguration` touch slop). | `src/Plumix/FlutterHost.cs`, `src/Plumix/Widgets/MediaQuery.cs` vs `widgets/media_query.dart`, `dart:ui` `FlutterView.gestureSettings` | Expose a per-view touch slop from the host (Android `ViewConfiguration.ScaledTouchSlop`, default elsewhere) and pass it into the `MediaQueryData` the host builds. |
| No native accessibility bridge on any host. `FlutterHost` exposes `SemanticsRoot`, `SemanticsUpdated` and `PerformSemanticsAction(nodeId, action)`, but nothing consumes them: no Avalonia automation peers (desktop), no ARIA overlay tree (browser), no `AccessibilityNodeProvider` (Android), no accessibility elements (iOS). | `src/Plumix/FlutterHost.cs`, `src/Sample/Plumix.{Desktop,Browser,Android,iOS}` | Attach the host pipeline to a `HostPipelineManifold` and drive `SetSemanticsEnabled` from the platform state (this replaces the always-on semantics divergence); report the platform's state through `PlatformDispatcher.UpdateSemanticsEnabled` and deliver actions through `PlatformDispatcher.DispatchSemanticsActionEvent`, which `SemanticsBinding` already listens to. Then per host: consume `PlumixHost.SemanticsUpdateProduced` (already one batch of changed nodes per flushed frame), map `Id`/`Rect`/`Label`/`Flags`/`Actions`/`IsHidden`/children to the platform tree, and route every platform action back through `SemanticsOwner.PerformAction` (never call framework callbacks directly); keep focused node aligned with `FocusManager.PrimaryFocus`. Start with desktop (Avalonia `AutomationPeer`). |
| No host feeds `WidgetsBinding.HandleAccessibilityFeaturesChanged`: the framework carries `AccessibilityFeatures` (`ReduceMotion`, `DisableAnimations`) and `CupertinoMenuAnchor` consumes it, but the value stays at its default on every platform. | `src/Sample/Plumix.{Desktop,Browser,Android,iOS}`, `src/Plumix/FlutterHost.cs` | Read the platform reduce-motion / disable-animations settings per host (macOS `NSWorkspace`/Windows animation setting via Avalonia, browser `prefers-reduced-motion`, Android/iOS accessibility managers) and call `HandleAccessibilityFeaturesChanged` on change. |
| `SystemChrome.SetSystemUIOverlayStyle` per-bar colours are applied only on Android, through reflection over Avalonia internals (`TrySetInsetsManagerSystemBarTheme`, private `_activity`/`Window`); iOS and desktop get no per-bar styling. | `src/Plumix/FlutterHost.cs` (system-bar section) | Replace the reflection path with a public Avalonia insets API when one exists; add the iOS status-bar style path. Low priority. |

## Upstream re-port backlog — Flutter 3.44.0 → 3.47.0

From the pin-move audit of 2026-08-13 (97 upstream files changed, 66 intersect ported files, 40
behaviour-bearing deltas). The pass of 2026-08-29 walked every row against the 3.47.0 sources: the
behaviour-bearing deltas were absorbed (see `CHANGELOG.md`) and the rest were confirmed already
present. Only the rows below are still open, and each is blocked on a subsystem that Plumix does not
model the way Dart does — none is a straight delta absorb.

| Dart file | Delta | Blocked on |
| --- | --- | --- |
| `gestures/binding.dart`, `gestures/hit_test.dart` | The framework now exposes `PlatformDispatcher.OnHitTest` and `INativeHitTestTarget`, but no host calls the query for platform-view handoff. | Connect a host platform-view hit-test request to `OnHitTest` when native platform views land. |
