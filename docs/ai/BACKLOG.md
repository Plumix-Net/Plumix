# Backlog — open work not tied to a single control row

The one place for work that is known to be open but does not fit `docs/CUPERTINO_TODO.md` (a
Cupertino file), `docs/ai/DIVERGENCES.md` (an intentional, documented divergence) or a qualified
marker in `docs/ai/PORT_MAP.md` (an admitted approximate port). Created 2026-08-16 from an audit of
the retired per-iteration notes; everything else in those notes was verified closed or already tracked.

Rules:

- One row per item: **what is open**, **where** (C# file + Dart source), **next step**. No history, no
  rationale beyond one clause.
- Remove the row in the same change that closes it (and add the `CHANGELOG.md` line).
- An iteration that ends blocked adds a row here (`PORT_PLAYBOOK.md` Step 8) instead of a note file.
- `PORT_PLAYBOOK.md` Step 0 reaches this file only after the Cupertino TODO, the divergence registry
  and the qualified-marker list are empty — but a row here may be picked any time it unblocks a control.

## Control-level gaps found outside their rows

| Item | Where | Next step |
| --- | --- | --- |
| `FollowerLayer` computes the leader transform at paint time from a render-leader registry instead of at composition time. | `src/Plumix/Rendering/Layer.cs` (`LayerLink.RegisterRenderLeader`, `FollowerLayer`), `CompositedTransform.cs` vs `rendering/layer.dart` (`_establishTransform`, `linkedOffset`, `getLastTransform`, `_debugCheckLeaderBeforeFollower`) | Blocked on three primitives: headless pumps and hit tests must composite (today `PipelineOwner.CompositeFrame` runs only from `FlutterHost` with an Avalonia `DrawingContext`, so `_lastTransform` would stay null and menu/dropdown/tooltip/selection-toolbar followers would stop hit testing); every layer needs Dart's virtual `applyTransform` (`TransformLayer` also needs `_lastEffectiveTransform`, since `AddToScene` still takes an accumulated offset); `Layer` needs `depth` and `nextSibling`. Land those, port the layer, then drop the registry and the `DIVERGENCES.md` row. |
| `RenderObject` does not report Dart's "Attempted to set a layer to a repaint boundary render object." error when a repaint boundary overwrites its own `layer`. | `src/Plumix/Rendering/Object.RenderObject.cs` (`Layer` setter) vs `rendering/object.dart` (`set layer`), `test/rendering/repaint_boundary_test.dart` | Port the assert with its text, then port the repaint_boundary_test case that expects it. |
| `RenderRepaintBoundary.toImage`/`toImageSync` have no counterpart, because `OffsetLayer` cannot rasterize itself. | `src/Plumix/Rendering/RepaintBoundary.cs`, `src/Plumix/Rendering/Layer.cs` (`OffsetLayer`) vs `rendering/proxy_box.dart` (3486-3560), `rendering/layer.dart` | Give `OffsetLayer` a `ToImage(Rect, pixelRatio)` over an Avalonia `RenderTargetBitmap` (`FilterLayerRasterizer` already rasterizes a subtree), then add the two boundary methods and the `assert(!debugNeedsPaint)` guard. |
| `PipelineOwner.Request*For` conflate dirty-list insertion with `requestVisualUpdate`, so `markNeedsLayout`/`markNeedsPaint` ask for a frame twice and `scheduleInitialLayout`/`markNeedsCompositingBitsUpdate` ask where Dart does not. | `src/Plumix/PipelineOwner.cs` (`RequestLayoutFor`, `RequestPaintFor`, `RequestCompositingBitsUpdateFor`) vs `rendering/object.dart` (2683–2750, 3228, 3338–3418) | Make the `Request*For` methods pure list inserts, move `RequestVisualUpdate` to the exact `RenderObject` call sites Dart uses, and re-check the host's frame scheduling. |
| Flutter's `debugCheckHas*` helpers have no counterpart: `widgets/debug.dart` (`debugCheckHasDirectionality`, `debugCheckHasMediaQuery`, `debugCheckHasTable`, `debugCheckHasWidgetsLocalizations`, `debugCheckHasOverlay`) and `material_ui/debug.dart` (`debugCheckHasMaterial`, `debugCheckHasMaterialLocalizations`, `debugCheckHasScaffold`, `debugCheckHasScaffoldMessenger`) are not ported, so the widgets that need those ancestors either fail late or throw a bare exception. | `src/Plumix/Widgets` (no `Debug.cs`), `src/Plumix.Material` (no `Debug.cs`) vs `widgets/debug.dart`, `material_ui/lib/src/debug.dart` | Port both files over the `BuildContext.Describe*` members (which shipped 2026-09-07), then call each helper from the `Of` accessors that currently throw on their own. |
| `-c Release` and `-c Profile` are broken, so `AGENTS.md`'s claim that both configurations are green is wrong: ~10 tests fail (`FrameworkParityTests`, `BuildScopeTests`, the two `DebugFillProperties_*` tests) and the run then aborts on a stack overflow that recurses through `ComponentElement.PerformRebuild`, so most of the suite never runs. Pre-existing; unrelated to any current port. | `src/Plumix.Tests/FrameworkParityTests.cs`, `BuildScopeTests.cs`, `ScrollableTests.cs:154`, `ScrollViewTests.cs` vs `src/Plumix/Widgets/Framework.*.cs` | Fix the stack overflow first (it truncates every non-Debug run), then split the rest: mark the assert-only tests `DebugOnlyFact`, and decide whether `DebugFillProperties` should be unconditional. |
| `RelayoutWhenSystemFontsChangeMixin` is not ported; `Scheduler.ScheduleFrameCallback` shipped 2026-08-29, but the `PaintingBinding.systemFonts` listenable still does not exist. | `src/Plumix/Painting/*` vs `rendering/object.dart` (4693–4753) | Add a system-fonts `ChangeNotifier` fed by the hosts, then port the mixin onto `RenderParagraph` and friends. |
| `FlutterTimeline` is not ported, so `RenderingDebug.ProfileLayoutsEnabled`/`ProfilePaintsEnabled`/`Enhance*TimelineArguments` and `SchedulerDebug.DebugTracePostFrameCallbacks` and `WidgetsDebug.DebugProfileBuildsEnabled*`/`DebugEnhanceBuildTimelineArguments` are carried but emit no events, and `DiagnosticsNode.ToTimelineArguments` does not exist. | `src/Plumix/Foundation` (no `Timeline.cs`), `src/Plumix/Rendering/Debug.cs`, `src/Plumix/Scheduler.cs` vs `foundation/timeline.dart`, `foundation/diagnostics.dart:1605`, `scheduler/binding.dart` | Port `FlutterTimeline` over a .NET tracing sink, add `DiagnosticsNode.ToTimelineArguments`, then wrap `PipelineOwner.FlushLayout`/`FlushPaint`, `RenderObject.Layout` and `_paintWithContext` the way `object.dart` does, plus `framework.dart`'s `BUILD`/`FINALIZE TREE`/per-widget events (`_isProfileBuildsEnabledFor` in `BuildScope._tryRebuild`, `updateChild`, `inflateWidget`) and the scheduler's `Frame`/`Animate`/`POST_FRAME` tasks and `_TaskEntry.run`'s `Flow`. |
| `RenderBox.DebugAssertDoesMeetConstraints` only runs the `RenderingDebug.CheckIntrinsicSizes` branch; Dart also asserts the size is finite, that unbounded constraints are reported with the offending ancestor, and that `constraints.isSatisfiedBy(size)`. | `src/Plumix/Rendering/Box.RenderBox.cs` vs `rendering/box.dart` (2560–2650) | Port the two remaining assertion blocks, then fix whatever render objects they catch. |
| `TextStyle` has no `debugFillProperties`, so Dart's `InlineSpan.debugFillProperties` line `style?.debugFillProperties(properties)` has no counterpart and a span dump shows no style properties. | `src/Plumix/Painting/TextSpan.cs`, `Plumix/UI/Text.cs` (`TextStyle`) vs `painting/text_style.dart` | Port `TextStyle.debugFillProperties`/`getDiagnosticsProperties`, then call it from `InlineSpan.DebugFillProperties`. |
| `FlutterMemoryAllocations` is ported and `Element`/`State` dispatch their created/disposed events, but Dart's other dispatch sites (`ChangeNotifier`, `RenderObject`, `Layer`, `Ticker`, `AnimationController`, `TextPainter`, ...) do not, and the drawing backend has no image/picture creation hooks for `_subscribeToSdkObjects`. | `src/Plumix/Foundation/MemoryAllocations.cs`, `Debug.cs` vs `foundation/memory_allocations.dart` and every `debugMaybeDispatchCreated` call site in `flutter/lib/src` | Grep the pinned sources for `debugMaybeDispatch` and add each call at the matching C# constructor/`Dispose`. |
| `SliverMultiBoxAdaptorElement.ForgetChild` and `TwoDimensionalViewportElement.ForgetChild` keep weakened asserts because `Element.DeactivateChild` used to call `ForgetChild`; since the framework.dart tightening it no longer does. | `src/Plumix/Widgets/Scroll.cs`, `TwoDimensionalViewport.cs` vs `widgets/sliver.dart`, `two_dimensional_viewport.dart` | Restore Dart's `assert(_childElements.containsKey(child.slot))`/`assert(!_debugIsDoingLayout)`, drop the defensive lookups, and trim the matching `DIVERGENCES.md` row. |
| `BoxConstraints.ToString` prints `min≤w≤max, min≤h≤max` with trimmed decimals, while Dart prints `BoxConstraints(biggest)`/`(unconstrained)`, `w=1.0` for tight axes, `0.0<=w<=100.0` otherwise and a `; NOT NORMALIZED` suffix, so element and render dumps differ (`FrameworkDartParityTests.ElementDiagnostics` is skipped on it). `Offset`/`Size` print the same way (`350, 200` vs Dart's `Offset(350.0, 200.0)`), which keeps the `limited_box_test` `toStringDeep` dumps unported. | `src/Plumix/Rendering/Box.cs` vs `rendering/box.dart` (`BoxConstraints.toString`) | Port Dart's `toString` verbatim, then update the tests that assert the current format (`RenderingParityTests` and the dump tests) and un-skip the framework test. |
| Flutter's framework tests are partly ported: `FrameworkDartParityTests.cs` covers most of `framework_test.dart`, but its GlobalKey correct cases 1-5, duplications 19-20, the flutter#62055 re-attach case, the non-`RenderObjectElement` child mount/update errors, `scheduleBuild while debugBuildingDirtyElements` and the invalid-`visitChildren` error are open, as are `global_keys_duplicated_test`, `global_keys_moving_test`, `reparent_state*_test`, `layout_builder_and_global_keys_test`, `stateful_component(s)_test`, `build_scope_test` (legal setState times, dirty sort order), `key_test`, `set_state_1/2/3_test`, `parent_data_test`, `inherited_test`, `render_object_element_test`, `error_widget*_test` and `unique_widget_test`. | `src/Plumix.Tests/FrameworkDartParityTests.cs`, `FrameworkDartTester.cs` vs `flutter/packages/flutter/test/widgets/` | Port the listed tests with `FrameworkDartTester` (`PumpWidget`/`TakeException`); a test that exposes a production difference gets `Skip = "Parity gap: ..."` plus its own row here. |
| `RenderEditable` dumps no `textScaler`/`locale`/`offset`: the properties Dart's `debugFillProperties` reads do not exist on the C# render object. (`RenderParagraph.DevicePixelRatio` shipped 2026-08-29.) | `src/Plumix/Rendering/Editable.cs` vs `rendering/editable.dart` | Add the missing properties, then extend `DebugFillProperties`. |
| `ButtonThemeData` has no `colorScheme` field, so `ThemeData`'s `buttonTheme` default cannot pass one as Dart does | `src/Plumix.Material/ButtonTheme.cs` (`material_ui/lib/src/button_theme.dart`); consumed by `ThemeData` | Port `ButtonThemeData.colorScheme` (and the `getTextColor`/`getFillColor` paths that read it), then pass `colorScheme` from `ThemeData`'s `buttonTheme` default. |
| Two debug-only pieces of the `Hero` port are missing: Dart's `NavigatorState._updateHeroController` reports a `FlutterError` from a post-frame callback when one `HeroController` ends up owned by two navigators, and `Hero`/`HeroMode` implement `debugFillProperties` (`tag`, `mode`). | `src/Plumix/Widgets/Navigation.NavigatorState.cs` (`UpdateHeroController`), `src/Plumix/Widgets/Hero.cs` vs `flutter/.../widgets/navigator.dart`, `heroes.dart` | Port the ownership check over `FlutterError.ReportError` (now available); add the two `DebugFillProperties` overrides. Neither affects runtime behavior. |
| `BuildOwner.FlushBuild` drains the transient frame callbacks itself (`Scheduler.RunScheduledFrameCallbacksOutsideFrame`), because a widget-test harness pumps by flushing build/layout/paint by hand and never produces a frame — and `LayoutBuilder`'s scope defers its rebuild request to one. The same gap is already worked around in `RenderOverlayChildLayoutBox.PerformLayout`. | `src/Plumix/Widgets/Framework.BuildOwner.cs` (`FlushBuild`), `src/Plumix/Scheduler.cs`, `src/Plumix/Rendering/Overlay.cs` | Drive the harness pumps through `Scheduler.HandleFrame` so a real frame runs the transient callbacks, then drop both work-arounds. |
| `ListTile`/`ListTileThemeData.IconColor`/`TextColor` and `ToggleButtons`/`ToggleButtonsThemeData.FillColor` are typed `WidgetStateProperty<Color?>?` where Dart declares a plain `Color?` that may happen to be a `WidgetStateColor`; callers now have to write `WidgetStateProperty<Color?>.All(color)`. | `src/Plumix.Material/ListTile.cs`, `ListTileTheme.cs`, `ToggleButtons.cs`, `ToggleButtonsTheme.cs` vs `material_ui/lib/src/list_tile.dart`, `toggle_buttons.dart` | Retype the four slots to `WidgetStateColor?` (which converts implicitly from `Color`, like `AppBar.backgroundColor` already does) and keep the state resolution inside, the way Dart's `_IndividualOverrides` does. |

| There is no `BindingBase` service-extension registry (`registerServiceExtension`/`registerNumericServiceExtension`) and no `postEvent`, so `SchedulerBinding.initServiceExtensions`' `ext.flutter.timeDilation` and its `Flutter.Frame` timing event have no counterpart even though `Scheduler.TimeDilation` and the timings callbacks now exist. | `src/Plumix/Scheduler.cs`, `src/Plumix/Foundation` (no `Binding.cs`) vs `foundation/binding.dart`, `scheduler/binding.dart` (372, 1378), `scheduler/service_extensions.dart` | Add a VM-service-shaped extension registry and a `PostEvent` sink, then register `timeDilation` and post `Flutter.Frame` from a `!kReleaseMode` timings callback the way `initInstances` does. |
| `RenderView.HitTest` bounds-checks the view itself through `RenderBox.HitTest`, while Dart's `RenderView.hitTest` never checks its size and always adds itself; twelve Plumix tests read a `false` root result as a miss. | `src/Plumix/RenderView.cs` vs `rendering/view.dart` (`hitTest`) | Port Dart's `hitTest`, then change those tests (`GesturePipelineTests`, `MaterialCheckboxTests`, `MaterialButtonsTests`, `MaterialRadioTests`) to assert on `result.Path` instead of the returned bool. `ProxyBoxTransformParityTests.Transform_TranslatedChildIntoTranslatedBox_HitTest` is skipped on it. |
| Several render objects still drive Avalonia `TextLayout` directly instead of `TextPainter`, so they keep the `TextLayoutFallback` estimate in headless runs: `RenderEditable`, the `Slider`/`RangeSlider` value indicators, `TimePickerDialog`, `RenderButton`, `Banner`, `DebugOverflowIndicator` and `EditableText` measuring. | `src/Plumix/Rendering/Editable.cs`, `src/Plumix.Material/Slider.cs`, `RangeSlider.cs`, `TimePickerDialog.cs`, `src/Plumix/RenderButton.cs`, `Widgets/Banner.cs`, `Rendering/DebugOverflowIndicator.cs`, `Widgets/TextInput.cs` | Move each onto `TextPainter` the way its Dart source does, then delete `UI/TextLayoutFallback.cs`. |

## Pending runtime verification

| Item | Where | Next step |
| --- | --- | --- |
| Semantics empty-string sample verification is open: desktop launch stays running, but the UI tool cannot resolve the unbundled executable. | `src/Sample/Plumix.Sample/Demos/General/StateStorageDemoPage.cs`, `dart_sample/lib/demos/general/state_storage_demo_page.dart` | Open PageStorage + SharedAppData interactively and verify the readout still renders and updates under its identified semantics wrapper; non-null text and merge geometry are covered by framework tests. |
| `SingleChildScrollView` desktop visual verification remains open: the sample enters the native event loop, but the UI tool cannot attach to its window. | `src/Sample/Plumix.Sample/Demos/General/ScrollPhysicsDemoPage.cs`, `dart_sample/lib/demos/general/scroll_physics_demo_page.dart` | Open Scroll physics in an interactive desktop session and verify the horizontal strip, clipping and directional padding; framework tests and Dart analysis pass. |
| The paragraph engine switch (`TextPainter` over `UI/AvaloniaParagraph.cs`) and the rich-text demo's fade, ellipsis and strut section are unverified on a real host: in this environment the desktop sample exits at once with code 0 and no output, exactly like the unmodified previous commit. | `src/Plumix/UI/AvaloniaParagraph.cs`, `src/Sample/Plumix.Sample/Demos/General/RichTextDemoPage.cs`, `dart_sample/lib/demos/general/rich_text_demo_page.dart` | Open the RichText demo in an interactive desktop session and compare wrapping, the one-line fade and ellipsis, the strut line spacing and span taps with `dart_sample`. |

## Host-level gaps (platform adapters, `src/Plumix/FlutterHost.cs` and per-host projects)

| Item | Where | Next step |
| --- | --- | --- |
| `MediaQueryData.GestureSettings` exists (recognizers read it through `MediaQuery.MaybeGestureSettingsOf`) but no host ever sets it, so every recognizer falls back to the hard-coded `GestureConstants.TouchSlop`. Dart's `MediaQuery.fromView` fills it from `view.gestureSettings` (Android's `ViewConfiguration` touch slop). | `src/Plumix/FlutterHost.cs`, `src/Plumix/Widgets/MediaQuery.cs` vs `widgets/media_query.dart`, `dart:ui` `FlutterView.gestureSettings` | Expose a per-view touch slop from the host (Android `ViewConfiguration.ScaledTouchSlop`, default elsewhere) and pass it into the `MediaQueryData` the host builds. |
| No native accessibility bridge on any host. `FlutterHost` exposes `SemanticsRoot`, `SemanticsUpdated` and `PerformSemanticsAction(nodeId, action)`, but nothing consumes them: no Avalonia automation peers (desktop), no ARIA overlay tree (browser), no `AccessibilityNodeProvider` (Android), no accessibility elements (iOS). | `src/Plumix/FlutterHost.cs`, `src/Sample/Plumix.{Desktop,Browser,Android,iOS}` | Attach the host pipeline to a `HostPipelineManifold` and drive `SetSemanticsEnabled` from the platform state (this replaces the always-on semantics divergence; `RenderTapRegionSurface` then has to re-register its semantics-action listener whenever the `SemanticsOwner` is recreated). Then per host: consume `PlumixHost.SemanticsUpdateProduced` (already one batch of changed nodes per flushed frame), map `Id`/`Rect`/`Label`/`Flags`/`Actions`/`IsHidden`/children to the platform tree, and route every platform action back through `SemanticsOwner.PerformAction` (never call framework callbacks directly); keep focused node aligned with `FocusManager.PrimaryFocus`. Start with desktop (Avalonia `AutomationPeer`). |
| No host feeds `WidgetsBinding.HandleAccessibilityFeaturesChanged`: the framework carries `AccessibilityFeatures` (`ReduceMotion`, `DisableAnimations`) and `CupertinoMenuAnchor` consumes it, but the value stays at its default on every platform. | `src/Sample/Plumix.{Desktop,Browser,Android,iOS}`, `src/Plumix/FlutterHost.cs` | Read the platform reduce-motion / disable-animations settings per host (macOS `NSWorkspace`/Windows animation setting via Avalonia, browser `prefers-reduced-motion`, Android/iOS accessibility managers) and call `HandleAccessibilityFeaturesChanged` on change. |
| `SystemChrome.SetSystemUIOverlayStyle` per-bar colours are applied only on Android, through reflection over Avalonia internals (`TrySetInsetsManagerSystemBarTheme`, private `_activity`/`Window`); iOS and desktop get no per-bar styling. | `src/Plumix/FlutterHost.cs` (system-bar section) | Replace the reflection path with a public Avalonia insets API when one exists; add the iOS status-bar style path. Low priority. |

## Upstream re-port backlog — Flutter 3.44.0 → 3.47.0

From the pin-move audit of 2026-08-13 (97 upstream files changed, 66 intersect ported files, 40
behaviour-bearing deltas). The pass of 2026-08-29 walked every row against the 3.47.0 sources: the
behaviour-bearing deltas were absorbed (see `CHANGELOG.md`) and the rest were confirmed already
present. Only the rows below are still open, and each is blocked on a subsystem that Plumix does not
model the way Dart does — none is a straight delta absorb.

| Dart file | Delta | Blocked on |
| --- | --- | --- |
| `gestures/binding.dart`, `gestures/hit_test.dart` | Engine hit-test query hook (`platformDispatcher.onHitTest`, `NativeHitTestTarget`). | No host exposes a native hit-test query. Land it with the host work in *Host-level gaps* above. |
| `widgets/editable_text.dart` | Toolbar-on-screen scheduling (`_dataWhenToolbarShowScheduled`, the scroll-notification observer, the `schedulerPhase` switch). | The whole "re-show the toolbar once the selection scrolls back into view" mechanism is absent; `Scheduler.ScheduleFrameCallback` (the primitive it needs) shipped 2026-08-29. Port the mechanism, then pick the phase-dependent scheduling. |
