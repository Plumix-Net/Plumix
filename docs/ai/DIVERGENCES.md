# Divergence Registry

Single source of truth for intentional behavior/structure divergences from Flutter (Dart).

Rules:

- Every intentional divergence from Flutter behavior must have exactly one row here, added in the same iteration that introduces it (see `docs/ai/PORTING_MODE.md`).
- A row is removed only when the divergence is closed; record the closure in `CHANGELOG.md`.
- The pre-2026-07-01 per-iteration notes that used to hold older divergences were retired on 2026-08-16 (their still-open items were audited and moved here or to `docs/ai/BACKLOG.md`). If you find an undocumented divergence while touching a control, add a row here in that iteration; do not go looking for it in git history.

Row format:

- **Area/File**: framework file(s) the divergence lives in.
- **Divergence**: what differs from Flutter and why (platform/runtime constraint).
- **Expected delta**: user-visible or behavioral difference.
- **Close condition**: what must land to remove the divergence.

## Active Divergences

| Area/File | Divergence | Expected delta | Close condition |
| --- | --- | --- | --- |
| `src/Plumix/Widgets/Gestures.cs` (`RawGestureDetectorState.GetTapHandler`) | Dart's `_DefaultSemanticsGestureDelegate._getTapHandler` replays a semantic tap with empty details, so the synthesized down/up land at the global origin. Plumix's `TapGestureRecognizer` callbacks take real pointer events, and the synthesized pair is placed at the render object's center instead. | A widget whose tap handler checks where the release landed (`CupertinoButton`'s tap-move slop) activates from a semantic tap wherever it sits on screen; in Flutter it only activates when its inflated bounds happen to contain the origin. | Port Dart's `TapDownDetails`/`TapUpDetails` so a semantic tap can carry Dart's zero offset, and decide then whether to keep the center (accessibility) or match Dart exactly. |
| `src/Plumix/Widgets/RadioGroup.cs`, `RawRadio.cs`, `src/Plumix.Material/Radio.cs`, `src/Plumix.Cupertino/CupertinoRadio.cs` | Dart's `T?` on an unconstrained type parameter is a real nullable type, so `Radio<int>` deselects to `null`. In C# `T?` on an unconstrained `T` is only a nullability annotation: for a value-type argument `Action<T?>`/`T? groupValue` collapse to `Action<int>`/`int`, and the toggleable/registry "no selection" value becomes `default(T)`. | Radio groups over reference types (or an explicitly nullable argument such as `Radio<int?>`) match Dart exactly. A group over a bare value type reports `default(T)` — `0` for `int` — instead of `null` when a toggleable radio is deselected, and cannot represent "nothing selected". | No runtime close condition while the group value must stay one type parameter; document the `T?` argument requirement, or split the registry into `TValue`/`TGroupValue` so the group value can be nullable independently. |
| `src/Plumix.Material/Theme.cs` (`Build`) | Flutter's Material `Theme` wraps its child in a `CupertinoTheme` carrying `MaterialBasedCupertinoThemeData`, so Cupertino descendants (for example the `CupertinoCheckbox` behind `Checkbox.Adaptive`) inherit material brightness and colors. Plumix's `Theme` wraps only the widget-library themes; `MaterialBasedCupertinoThemeData` is not ported. | An adaptive Cupertino control under a dark Material theme does not pick up dark brightness or material colors unless the app supplies an explicit `CupertinoTheme`. | Port `MaterialBasedCupertinoThemeData` (`material_ui/lib/src/theme.dart`) and wrap `Theme`'s child in a `CupertinoTheme` carrying it. |
| `src/Plumix.Cupertino/CupertinoNavigationBar*.cs`, `CupertinoSliverNavigationBar.cs` | The API, constants, composition, sliver behavior and transition choreography match, with three shape deltas: Dart's named constructors (`CupertinoNavigationBar.large`, `CupertinoSliverNavigationBar.search`) are static factories, and the non-const `border` default (`_kDefaultNavBarBorder` with null meaning "no border") becomes an overload pair per entry point; the mid-transition back-label opacity pickup reads an ancestor `RenderOpacity` because Plumix's `AnimatedOpacity` drives that type and no distinct `RenderAnimatedOpacity` exists; and the hero shuttle's animation is the navigator's flight `CurvedAnimation` (EaseInOut over the flight controller), so `routeAnimation` unwraps to the flight controller's timeline rather than the route animation Flutter hands its hero flights. | Behavior and geometry match; transition sub-curves driven off `routeAnimation` follow the flight controller's linear timeline instead of the route transition's, which shifts component timing slightly during flights. | Drive hero flights from the route animation the way Flutter's `HeroController` does (also closes the hero baseline divergence), and add a `RenderAnimatedOpacity` type if `FadeTransition` is ever rebased onto a dedicated render object. |
| `src/Plumix.Cupertino/CupertinoMenuAnchor.cs` | The public API, defaults, colors, width tables, layout attachment and animation-status flow match, but the internal composition is flattened: Dart's stateless `CupertinoMenuItem` + `_CupertinoMenuItemLabel` + `_CupertinoMenuItemInteractionHandler` split is one stateful widget; the overlay resolves motion values inline (`AnimatedBuilder` + `Opacity`/`Align`) instead of caching `_resolveMotion`'s animation objects and swapping `kAlwaysCompleteAnimation` parents; the shadow is a `DecoratedBox` box shadow rather than `_ShadowPainter`; the implicit divider is a solid ~25 % `ColoredBox` hairline without Dart's `BlendMode.overlay` + `srcOver` double pass; the swipe region owns swipe state directly instead of Dart's per-down recognizer rebuild; and accessibility features come from `WidgetsBinding.AccessibilityFeatures` because Plumix has no per-view `platformDispatcher`. | Behavior and geometry match the source; the implicit divider color composites slightly differently over saturated backgrounds, and reduce-motion/disable-animations react to the binding-level flag rather than a per-view one. | Port `_ShadowPainter` and the blend-mode divider painter once the paint layer exposes per-primitive blend modes, and split the menu item back into Dart's three classes in a structure-tightening pass. |
| `src/Plumix/Widgets/ListWheelScrollView.cs`, `Rendering/ListWheelViewport.cs` | `Element.UpdateChild` is not virtual, so `ListWheelElement.updateChild` is a private wrapper; there is no `owner.buildScope`, `sizedByParent` is emulated by sizing from the constraints at the top of `PerformLayout`, and `PaintingContext.pushTransform`'s offset sandwich is folded into the matrix handed to Plumix's offset-less `PushTransform`. | Layout, paint matrices, hit testing, reveal, physics and selection reporting match. | Make `Element.UpdateChild` overridable and add `sizedByParent`/`computeDryLayout` to `RenderBox`, then port the element and layout entry points directly. |
| `src/Plumix/Gestures/DragGestureRecognizer.cs` | Dart's `DragGestureRecognizer.acceptGesture` starts the drag as soon as the arena resolves in its favour (at pointer down for a sole member), so under `DragStartBehavior.start` no slop is lost; Plumix's recognizer waits for the pointer to travel the touch slop before starting even after acceptance. `onlyAcceptDragOnThreshold` is not ported. | The first 18 px of every drag on a scrollable are swallowed; `ListWheelScrollViewTests` compensate in their gesture helper so Flutter's literal offsets still hold. | Port `monodrag.dart`'s `acceptGesture`/`_checkDrag`/`_hasSufficientGlobalDistanceToAccept` split (with `onlyAcceptDragOnThreshold`), then drop the compensation from the tests. |
| `src/Plumix/Rendering/Proxy.RenderBox.cs` (`RenderColoredBox`) | Dart's `_RenderColoredBox extends RenderProxyBoxWithHitTestBehavior` with `behavior: HitTestBehavior.opaque`; Plumix's `RenderColoredBox` is a plain `RenderProxyBox`, so a childless `ColoredBox` is not hittable. | Taps on a bare `ColoredBox` fall through to whatever is behind it; tests wrap tap targets in `GestureDetector(behavior: Opaque)`. | Rebase `RenderColoredBox` on `RenderProxyBoxWithHitTestBehavior(Opaque)`. |
| `src/Plumix/Widgets/Navigation.NavigatorState.cs` | Dart defers route disposal to a `toBeDisposed` list drained after `_flushHistoryUpdates` returns, so a removal never re-enters the flush. Plumix disposes inside the scan, so `FlushHistoryUpdates` is re-entrancy guarded and a `RemoveRoute` issued from inside a build (Dart's `_DropdownButtonState.build` does exactly this on an orientation change) marks the route inactive but leaves its overlay entry in the tree until the next frame rebuilds the overlay. | The removed route stops being current/active immediately and no longer takes input, but its content can still paint for one frame. Every removal issued outside a build matches Dart. | Defer route disposal to a post-scan list the way Dart does, then drop the re-entrancy guard and rearrange the overlay from the removal path. |
| `src/Plumix/Rendering/Viewport.RenderViewport.cs` | Flutter's `RenderViewportBase.visitChildrenForSemantics` walks `childrenInPaintOrder` (last sliver first) and lets the geometry-driven traversal sort put the nodes back into reading order. Plumix's traversal sort is ported but only engages when an ancestor supplies a `textDirection`, and no Plumix host or app puts one above a viewport yet, so the viewport keeps first-to-last sliver order to preserve reading order and `scrollIndex`. | Semantics children of a viewport are ordered by sliver order rather than paint order; every other viewport semantics behavior matches. | Propagate a `textDirection` into the semantics tree from `Directionality` the way Flutter's text and `Semantics` widgets do, then switch the walk to paint order. |
| `src/Plumix/Rendering/Sliver.cs` | Flutter's `SliverGeometry.visible` is a settable field defaulting to `paintExtent > 0`; Plumix's `SliverGeometry` is a `readonly record struct` and derives `Visible` from `PaintExtent` instead. | Every viewport paint, hit-test and semantics visibility decision matches for slivers that do not override the flag. A sliver cannot report itself visible while painting nothing, or invisible while painting. | Add the field to the geometry value and thread it through the sliver constructors. |
| `src/Plumix/Widgets/Router.cs`, `App.cs`, `src/Plumix.Material/App.cs` | C# generics are invariant and C# has no named constructors or mixins. Dart's statics on `Router<T>` live on a non-generic `Router` companion class; `Router.withConfig`, `WidgetsApp.router` and `MaterialApp.router` are static factories (`WithConfig`/`Router<T>`), and the apps capture the typed delegates in an internal `RouterHost` closure instead of storing `RouterDelegate<object>` fields, exposing them as `object?` for inspection. `Router.of<Object?>(context).backButtonDispatcher` becomes `Router.BackButtonDispatcherOf`. `RouterDelegate`/`RouteInformationProvider` extend `ChangeNotifier` rather than bare `Listenable`, `PopNavigatorRouterDelegateMixin` is an intermediate base class, and Dart's private `_CallbackHookProvider` is public with an internal constructor. `RouteInformation`'s deprecated `location` surface is not ported. | Every delegate, parser, provider, dispatcher and reporting behavior matches; only the spelling of the constructors/statics differs, and `Router.Of<T>` must be given the router's own configuration type instead of `object`. | No runtime close condition exists; generated Dart-to-C# interop metadata may map the source names without changing the CLR members. |
| `src/Plumix/UI/SystemNavigator.cs`, `src/Plumix/Widgets/Router.cs`, `AppLifecycleListener.cs` | `SystemNavigator` now sends Flutter's real `flutter/navigation`/`flutter/platform` messages and `WidgetsBinding.InitInstances` answers the inbound `popRoute`/`pushRoute`/`pushRouteInformation` calls, but Plumix still has no `PlatformDispatcher`: `SystemNavigator.DefaultRouteName` replaces `platformDispatcher.defaultRouteName` and hosts assign it before the first frame. Dart also has no `SynchronousFuture`: already-completed `Task`s keep the pop chain synchronous and the parse continuation is deferred through `Scheduler.ScheduleMicrotask`, but a genuinely asynchronous parser/delegate resumes on the thread that completes its `Task` rather than on a single isolate. Report scheduling also schedules the frame, which Flutter leaves to the surrounding frame. | Reporting types/`replace` decisions, back-button priority, deep-link push handling, the wire payloads and synchronous pop results match. The initial route still comes from a host-assigned property rather than the engine, asynchronous parsers observe .NET continuation scheduling, and a router that never changes still costs one extra frame at startup. | Add a platform dispatcher carrying the engine's default route name, and a single-threaded future scheduler, then route the initial route and the parse continuations through them. |
| `src/Plumix/Rendering/NestedScrollView.cs` (`RenderSliverOverlapInjector`) | Plumix has no `debugPaintSizeEnabled`/`debugPaint` infrastructure, so Flutter's `paintZigZag` marker for an injected overlap is not ported. | Debug builds draw no zig-zag over the injected gap; layout and release painting match. | Add the debug-paint hooks to `RenderObject` and reinstate `paintZigZag`. |
| `src/Plumix/Widgets/ScrollMetrics.cs`, `PageView.cs` (`PageMetrics`), `NestedScrollView.cs` (`NestedScrollMetrics`) | C# has no mixins and `ScrollPosition` already extends `ViewportOffset`, so Dart's `ScrollMetrics` mixin is the `IScrollMetrics` interface plus the `ScrollMetricsUtils` companion that carries the mixin's bodies: a class member matching an interface member replaces its default implementation, so redeclaring one and calling back through the interface would recurse. C# also forbids widening an override's parameter list, so `copyWith`'s extra arguments (`viewportFraction` on `PageMetrics`, `minRange`/`maxRange`/`correctionOffset` on `NestedScrollMetrics`) move to the front of a separate overload with no defaults. | Every value, `has*` flag, derived extent, `copyWith` rule and subclass identity matches, including `notification.Metrics is PageMetrics`. Custom metrics implementations call `ScrollMetricsUtils` instead of inheriting the bodies, and a widened `CopyWith` is reached by naming its leading argument. | No runtime close condition exists; both shapes follow from the language. |
| `src/Plumix/Widgets/WidgetState.cs`, `RawRadio.cs`, `src/Plumix.Material/InputDecorator.cs`, `InputDecoratorTheme.cs`, `CheckboxTheme.cs`, `src/Plumix.Cupertino/CupertinoSwitch.cs` | Flutter's state-resolving values subclass the value they stand for (`WidgetStateTextStyle extends TextStyle`, `WidgetStateColor extends Color`, `WidgetStateBorderSide extends BorderSide`), so a resolver can be stored in a plain `TextStyle?`/`Color?`/`BorderSide?` field and recovered with `value is WidgetStateProperty<T>`. Plumix's `TextStyle` is a sealed record, `BorderSide` a readonly record struct and Avalonia's `Color` a sealed struct, so none can be subclassed: affected slots use wrapper types with implicit plain-value conversions. `CupertinoSwitch` retains direct `CupertinoDynamicColor` values through an internal state-color subtype so each Dart resolution path can preserve or resolve them. `WidgetStateBorderSide` and `WidgetStateMouseCursor` live in core with set-based resolvers; Material call sites keep their `[Flags] MaterialState` spelling through `WidgetStateMaterialExtensions` and `MaterialStateSet`. | Plain-value construction and state resolution match. Reading an affected slot returns its wrapper, and a dynamic color returned from a generic `WidgetStateProperty<Color?>` has already decayed to its current value. | Give the framework its own non-sealed `TextStyle`, `BorderSide` and `Color` value hierarchies, then expose Flutter's subtype relationships and one state-set signature directly. |
| `src/Plumix.Material/InputDecorator.cs` | Dart's `InputDecoration.applyDefaults(Object)` accepts either an `InputDecorationTheme` or an `InputDecorationThemeData` and throws `ArgumentError` for anything else; C# expresses the same contract as two overloads, so the error case is unrepresentable. The deprecated surfaces are also not ported: `InputDecoration.maintainHintHeight` (never read by Flutter either), `InputDecoration.collapsed`'s deprecated `floatingLabelBehavior`/`floatingLabelAlignment` passthrough, and `copyWith`'s accepted-but-unused `semanticsService`. `InputDecorationThemeData` has no `debugFillProperties`: the diagnostics layer now exists, but the theme is a `record` and has not been given the `IDiagnosticable` surface yet. | Both accepted argument types resolve identically and every defaulting rule matches. A wrong argument type is a compile error instead of a runtime `ArgumentError`, and code migrating from the deprecated Dart parameters must drop them. | Nothing to close for the overloads; port the deprecated members if source-shaped migration compatibility is needed, and implement `IDiagnosticable.DebugFillProperties` on the theme. |
| `src/Plumix.Material/MenuStyle.cs`, `MenuTheme.cs`, `MenuThemes.cs` | `MenuStyle`, `MenuThemeData`, `MenuBarThemeData` and `MenuButtonThemeData` carry no `debugFillProperties`: the diagnostics layer now exists, but these types have not been given the `IDiagnosticable` surface yet. The panel's mouse cursor is also a plain `MouseCursor` resolved against the empty state set rather than Dart's `_MouseCursor` wrapper, and it falls back to `MouseCursor.Defer` where Dart uses `MouseCursor.uncontrolled`, which core does not model. | Every field, default, merge, lerp and equality result matches, and a `MouseRegion` resolves the same cursor Dart's wrapper would (Flutter's `WidgetStateMouseCursor` also resolves against the empty set when handed to a region). A menu panel with no cursor of its own defers to the region behind it instead of leaving the cursor uncontrolled, and the theme objects cannot be dumped into a diagnostics tree. | Implement `IDiagnosticable.DebugFillProperties` on the four types, and port `MouseCursor.uncontrolled` into core. |
| `src/Plumix/Painting/ColorSwatch.cs`, `src/Plumix.Material/MaterialColor.cs`, `Colors.g.cs`, `ColorScheme.cs`, `ThemeData.cs` | Dart's `ColorSwatch<T> extends Color`, so a swatch *is* a colour and `ColorScheme.primary`/`ThemeData.primaryColor` keep the swatch object (which `Color.==`'s `runtimeType` check then reports as unequal to its own `shade500`). Avalonia's `Color` is a sealed struct, so `ColorSwatch<T>` is a class exposing `Primary`/`Value` plus an implicit conversion to `Color`; a swatch stored in a `Color` slot decays to its primary colour. Dart's `ColorSwatch.hashCode` also hashes the swatch *map identity* while `==` compares contents; Plumix hashes the contents so equal swatches hash equally. `Colors` is generated (`scripts/generate_material_colors.py`) rather than hand-written, and shadows `Avalonia.Media.Colors` for anything importing both namespaces. | Every palette value, shade lookup, `FromSwatch` role, `primarySwatch` derivation and swatch equality result matches. `ThemeData.indicatorColor`/`secondaryHeaderColor` (both unported) are the only Dart behaviours that depend on the swatch surviving inside a `Color` slot. Consumers importing `Avalonia.Media` and `Plumix.Material` together must alias `Colors`. | Give the framework its own non-sealed `Color` hierarchy, then let `ColorSwatch<T>` derive from it. |
| `src/Plumix.Material/TextSelectionTheme.cs`, `Theme.cs`, `TextField.cs`, `SelectableText.cs` | `TextSelectionThemeData` carries no `debugFillProperties`: the diagnostics layer now exists, but the theme has not been given the `IDiagnosticable` surface yet. Plumix also has no `ThemeData.cupertinoOverrideTheme` and no `MaterialBasedCupertinoThemeData`, so `Theme` installs no Material-derived `CupertinoTheme`: `TextField`/`SelectableText` apply Dart's iOS/macOS branch as `CupertinoTheme.Of(context).NoDefault().PrimaryColor ?? colorScheme.primary` rather than reading the bridge, and `MaterialBasedCupertinoThemeData`'s deferral of `selectionHandleColor` to `textSelectionTheme.selectionHandleColor` has no counterpart. Plumix's `Theme` additionally inserts a `DefaultTextStyle` (Dart's `_wrapsWidgetThemes` inserts only `IconTheme` and `DefaultSelectionStyle`). | Every field, default, `CopyWith`, `Lerp`, equality and resolution result matches, including the apple-platform fallback with no Cupertino override present. The theme objects cannot be dumped into a diagnostics tree, a `cupertinoOverrideTheme` cannot be expressed, and Cupertino controls under a Material `Theme` keep their own defaults instead of the Material-derived ones. | Implement `IDiagnosticable.DebugFillProperties` on the theme, then port `ThemeData.cupertinoOverrideTheme` with the `MaterialBasedCupertinoThemeData`/`CupertinoBasedMaterialThemeData` bridge and install it from `Theme`. |
| `src/Plumix/UI/TextInput.cs`, `TextInputConfiguration.cs`, `TextInputValues.cs`, `TextEditingDelta.cs` | The whole of `text_input.dart`/`text_editing_delta.dart` is ported, but Plumix's IME keystrokes still travel the host focus adapter rather than a `TextInputConnection`, so no host installs a `TextInputControl` and `EditableText` never receives an inbound `updateEditingValue`/`updateEditingValueWithDeltas` in practice. Language/runtime shapes: Dart's `TextInputAction` is `TextInputActionType` (the name is taken by the property that carries it) and its `_toTextInputAction` throw is an `ArgumentException` rather than a `FlutterError`; the `-1` "any client" id is accepted in every build because C# has no assert elision, as is the delta family's `_debugTextRangeIsValid` (an `ArgumentOutOfRangeException`); Plumix has no `FlutterError.reportError`, so channel and dispatch failures go to `TextInput.OnError` with Dart's context string; `TextInputConfiguration.HintLocales` is non-nullable (Dart's nullable slot with a `const []` default is unrepresentable as a C# optional argument) and compares by element where Dart compares by list identity; `TextInputStyle.FontWeight` is Avalonia's numeric enum mapped onto Dart's 0-8 `fontWeightIndex`; `RawFloatingCursorPoint.StartLocation` is a tuple; and `TextEditingValue.Composing` stays `TextRange?` (Dart uses `TextRange.empty`) with a constructor that clamps selection/composing into the text where Dart asserts. `IScribbleClient` is registered and dispatched but no host raises the scribble messages. | Every value type, default, `ToJson`, dedup cache, control fan-out, input-control switching, deferred hide and inbound dispatch matches, so a host that speaks `flutter/textinput` is wire-compatible. Keystrokes and IME geometry still reach `EditableText` through the focus adapter, `EditableText`'s floating-cursor, private-command and autocorrect-rect client members are no-ops, and an out-of-bounds composing range is clamped instead of reported. | Route `EditableText`'s keystrokes, composition and geometry through the connection by shipping the host adapter as a `TextInputControl`, then drop the clamping from `TextEditingValue` and add a build-mode surface so the debug-only checks can be elided. |
| `src/Plumix/Widgets/TextInput.cs` (`EditableText.AutofillDisabled`) | Dart defaults `EditableText.autofillHints` to `const <String>[]` (autofill on, no hints) and disables autofill with `autofillHints: null`. C# cannot give an optional reference parameter a non-null default, so an omitted argument means the empty list and the `EditableText.AutofillDisabled` sentinel means Dart's `null`. | The default behaviour matches — a field with no hints still autofills — and every hint list resolves identically. Only the spelling of "off" differs: `EditableText.AutofillHints` still reports `null` for a disabled field the way Dart does, while `TextField.AutofillHints` reports the sentinel it was handed and forwards it unchanged. | No runtime close condition exists; generated Dart-to-C# interop metadata (or an option value type) could model the `const []` default directly. |
| `src/Plumix/UI/Autofill.cs`, `src/Plumix/Widgets/Autofill.cs` | C# has no mixins, so Dart's `AutofillScopeMixin` body lives on the `AutofillScopeMixin` static companion and `AutofillGroupState` forwards `Attach` to it (the `ScrollMetricsUtils` precedent). `AutofillScope`'s "every client must enable autofill" debug assert becomes a thrown `InvalidOperationException`, and `AutofillGroupState.Unregister`'s assert on an unknown id is dropped in favour of Dart's release behaviour (a silent no-op). `Dictionary<TKey, TValue>` has no contractual enumeration order, so the group keeps an explicit registration-order list beside its client map. | Registration order, first-registration-wins on a duplicate id, the enabled-client filter, topmost-group tracking and both dispose actions match. A third-party scope calls the companion instead of mixing the body in, and the enabled-client violation throws in release builds too. | No runtime close condition; the shapes follow from the language. |
| `src/Plumix/Foundation/Diagnostics.cs`, `Diagnostics.Node.cs`, `Diagnostics.TextTree.cs`, `DiagnosticProperties.cs`, `Diagnosticable.cs` | Plumix has no `kReleaseMode`/`kProfileMode`, and C# has no assert elision, so every method Dart wraps in `assert(() {...}())` always runs its debug body: `DiagnosticPropertiesBuilder.Add` always records, `Diagnostics.ObjectRuntimeType` never returns `<optimized out>`, `DiagnosticsNode.ToString`/`ToStringDeep`/`ToJsonMap` never fall back to the identity string/`""`/`{}`, `Level` is never forced to `Hidden`, and `ToTimelineArguments` cannot raise Dart's profile-mode `FlutterError`. | Every debug-mode result matches Dart, which is what Flutter's own tests pin. A release build pays for diagnostics Flutter would have compiled out, and code that branches on the release fallbacks sees the debug values instead. | Add a build-mode surface (and a way for hosts to set it) mirroring `foundation/constants.dart`, then gate the same bodies on it. |
| `src/Plumix/Foundation/DiagnosticProperties.cs` | C# cannot use a non-constant sentinel as a default parameter value, so Dart's `kNoDefaultValue` is inverted: an omitted `defaultValue` argument means "no default value" and Dart's meaningful `defaultValue: null` is written `defaultValue: DiagnosticsDefaults.NullValue`. `DiagnosticsDefaults.NoDefaultValue` can still be passed explicitly (which is what `FlagProperty` needs, since Dart's `FlagProperty` defaults that slot to `null` rather than the sentinel). | Every level, filter and `defaultValue` JSON result matches, including the "null is the boring value" downgrade to `DiagnosticLevel.Fine`. Only the spelling of an explicit null default differs. | No runtime close condition; generated Dart-to-C# interop metadata could map the sentinel default directly. |
| `src/Plumix/Foundation/Diagnostics.TextTree.cs`, `DiagnosticProperties.cs`, `Diagnosticable.cs` | Language shapes with no C# spelling. Dart's named constructors are static factories (`DiagnosticsProperty<T>.Lazy`, `DoubleProperty.Lazy`, `ObjectFlagProperty<T>.Has`, `DiagnosticPropertiesBuilder.FromProperties`, `DiagnosticsNode.Message`, `DiagnosticsSerializationDelegate.Create`); `toString({...})` cannot extend `Object.ToString`, so `DiagnosticsNode.ToString(parentConfiguration, minLevel)` and `Diagnosticable.ToString(minLevel)` are overloads beside the parameterless override; an override cannot narrow a return type, so Dart's `T? get value` is `TypedValue` beside the inherited `object? Value`; the module-level `*TextConfiguration` values live on the `TextTreeConfigurations` static class; the `Diagnosticable`/`DiagnosticableTree` mixins are the `IDiagnosticable`/`IDiagnosticableTree` interfaces plus the matching abstract classes; `_NumProperty<T extends num>` is `NumProperty<T> where T : struct`; `DiagnosticsProperty<void>` is `DiagnosticsProperty<object>`; and `FlagsSummary`'s `Map<String, T?>` is an ordered `IReadOnlyList<KeyValuePair<string, T?>>` because `Dictionary<TKey, TValue>` has no contractual enumeration order. | Every rendered tree, description, level, filter and JSON key matches Dart, and `Diagnostics.EnumName` emits lower camel case so serialized `level`/`style` values stay wire-compatible. Only the spellings differ, and `EnumProperty`/`DescribeEnum` lower-camel the C# member name rather than a Dart identifier. | No runtime close condition; the shapes follow from the language. |
| `src/Plumix/Foundation/DiagnosticProperties.cs` | `ColorProperty` is not ported: it lives in `painting/colors.dart`, not `foundation/diagnostics.dart`. Material themes use `DiagnosticsProperty<Color?>` where Dart uses `ColorProperty`. | Colours render through `Color.ToString()` instead of Dart's `Color(0x...)` formatting, and the JSON carries no `valueProperties` colour breakdown. Levels, filtering and names match. | Port `ColorProperty` with `painting/colors.dart`. |
| `src/Plumix/FlutterHost.cs` | Flutter runs one engine per isolate, so `drawFrame` can only reach a view on that isolate's thread. Plumix's `Scheduler` is a process-wide static and a frame can be pumped from any thread, while an Avalonia `Control` may only be touched from the thread that created it, so `PlumixHost.HandleSchedulerDrawFrame` ignores frames pumped from any other thread. | Single-threaded hosts (every real app, and every test that pumps on its own thread) are unaffected. A host whose frames are pumped from a foreign thread silently stops drawing instead of throwing. | Give the scheduler a per-view binding so a frame is dispatched only to the views that belong to it. |
| `src/Plumix.Cupertino/CupertinoColors.cs`, `CupertinoTheme.cs`, `CupertinoTextTheme.cs` | Dart's `CupertinoDynamicColor implements Color`, so any `Color` slot can hold one and `CupertinoDynamicColor.resolve` recovers it with a runtime type test. Avalonia's `Color` is a sealed struct, so `CupertinoDynamicColor` is a class with two implicit conversions instead: a plain colour converts to a dynamic colour whose eight variants are all that colour (resolving it is a no-op, exactly like Dart's non-dynamic branch), and a dynamic colour converts to its effective `Value`. `CupertinoThemeData`/`NoDefaultCupertinoThemeData` therefore type their colour slots as `CupertinoDynamicColor?`, `resolve` returns the plain `Color` while `ResolveFrom` returns the dynamic one, and `CupertinoThemeData.ApplyThemeToAll` hides rather than overrides the nullable base (C# covariant returns do not cover `bool?` -> `bool`). Core `TextStyle.Color` stays a plain `Color?`, so the text theme's dynamic label/action colours live in the defaults builder and Dart's `_resolveTextStyle` has no counterpart. | Every variant selection, default, `CopyWith`, `NoDefault`, equality and resolution result matches, and a plain colour can still be passed anywhere a theme slot is declared. A dynamic colour placed inside a *caller-supplied* `TextStyle` is captured at its light/base variant instead of re-resolving per context, and reading a theme colour returns the wrapper rather than a bare `Color`. | Give the framework its own non-sealed `Color` hierarchy (the same close condition as `ColorSwatch<T>` and `WidgetStateColor`), then let `CupertinoDynamicColor` derive from it and drop the wrapper. |
| `src/Plumix/Rendering/Object.RenderObjectSemantics.cs` (`MergeSiblingGroup`) | Flutter's `_mergeSiblingGroup` lets any fragment in a merge group donate and adopt `cachedSemanticsNode`. For an `_IncompleteSemanticsFragment` the owner is the render object that ran the delegate, so when that render object also forms a node of its own, Flutter hands the sibling group the owner's own node and then overwrites the owner's cache with it. Plumix skips donation/adoption for exactly that case. | Sibling node identity, tags and geometry match. A sibling group made only of synthesized configurations gets a fresh node instead of stealing the delegate owner's, which Flutter would corrupt. | Upstream the guard (or the equivalent assertion) into Flutter, then drop the special case. |
| `src/Plumix/Rendering/Viewport.Reveal.cs`, `Viewport.RenderViewport.cs`, `SingleChildViewport.cs` | C# interfaces cannot require a base class, so Flutter's `abstract interface class RenderAbstractViewport extends RenderObject` becomes the `IRenderAbstractViewport` contract plus a `RenderAbstractViewport` static companion (`MaybeOf`/`Of`/`DefaultCacheExtent`/`ShowInViewport`), and `showInViewport` takes the offset as a parameter instead of reading it off the viewport. `RenderViewportBase<TParentData>` composes Plumix's container mixin rather than mixing it in, `RenderSliver` is a `RenderBox` so hit testing runs through the box protocol instead of `SliverHitTestResult`/`hitTest(mainAxisPosition:, crossAxisPosition:)` and `getOffsetToReveal`'s pivot walk has to exclude slivers explicitly where Dart's type test does it, and Plumix has no `sizedByParent`, so `RenderViewport.PerformLayout` sets `Size = Constraints.Biggest` itself where Dart uses `computeDryLayout`. `crossAxisDirection` is optional and defaults to the perpendicular reading-order direction, and `RenderViewport.Insert` keeps the constructor's `center ?? firstChild` fallback alive as children arrive, because a render object built without the widget element has no `_ViewportElement` to assign one. | Every layout, correction, growth-direction, cache, paint-order, reveal, clip and semantics behavior matches, including the `center`-anchored two-directional layout. A viewport cannot be hit tested through a sliver-specific result object, and the render objects cannot be asked for a dry layout. | Give `RenderSliver` its own hit-test result and main/cross-axis hit-test entry point, and model `sizedByParent`/`computeDryLayout` in `RenderBox`; the interface shape and the optional cross-axis direction follow from the language and from render-object-only construction. |
| `src/Plumix/Widgets/View.cs` | C# forbids a member with the same name as its enclosing type, so Flutter's `View.view` property is exposed as `View.ViewHandle`; the constructor parameter and `View.Of`/`View.MaybeOf` APIs retain their source names. | View identity, physical metrics, lookup dependencies, and `LookupBoundary` hiding match; only direct widget-property access uses the language-shaped name. | No runtime close condition exists; generated Dart-to-C# interop metadata may map the source name without changing the CLR member name. |
| `src/Plumix/RenderParagraph.cs`, `src/Plumix/Painting/ParagraphSource.cs` | Avalonia's `TextLayout` exposes `HitTestPoint`/`HitTestTextPosition`/`HitTestTextRange` but no `getClosestGlyphInfoForOffset` with grapheme-cluster layout bounds, no direction-carrying `getBoxesForSelection`, and no `BoxHeightStyle`/`BoxWidthStyle`. Span hit testing therefore resolves the text position and checks the reported glyph rect, and `AssembleSemanticsNode` unions the range rects without a per-run writing direction. `RenderParagraph` also keeps Plumix's flat `FontSize`/`FontWeight`/`Foreground`/`Height`/`LetterSpacing`/`TextDecoration` accessors, which project the root span's `TextStyle` instead of existing on the span only, and it exposes `PlainText` because C# cannot overload `Text` on both `InlineSpan` and `string`. | Span-level hit testing, `TextSpan.recognizer` dispatch, mouse-cursor resolution, the `RenderComparison` invalidation switch, per-run semantics nodes with `OrdinalSortKey` and tap/long-press actions, and inline-widget layout/paint/hit testing match. A tap in a justified or wide-letter-spaced gap can miss where Flutter's advance-width cluster bounds would hit, per-run semantic nodes carry no writing direction, and node rects can differ by a pixel from Flutter's floor/ceil-of-engine-boxes. | Add a backend glyph-info API with grapheme-cluster bounds plus direction-carrying selection boxes and height/width styles, then route hit testing and `AssembleSemanticsNode` through them. |
| `src/Plumix/Painting/ParagraphSource.cs`, `src/Plumix/Rendering/InlineChildren.cs` | Core semantics now has `SemanticsTag`, but `WidgetSpan` children are still not tagged with `PlaceholderSpanIndexSemanticsTag` and Flutter's `childConfigurationsDelegate` path is unavailable; `InlineSpanSemanticsInformation.StringAttributes` (spell-out/locale) also has no `AttributedString` to reach the semantics bridge. Placeholder boxes are resolved by walking Avalonia's laid-out lines rather than the engine's `getBoxesForPlaceholders`. | Placeholder sizing, the six `PlaceholderAlignment` rules, truncated-child exclusion, paint and hit-test order match, and the span model records locale/spell-out attributes. Inline-widget semantics merge into the paragraph instead of interleaving as tagged sibling nodes, and assistive technologies do not receive spell-out or locale attributes. | Tag the placeholder spans (core `SemanticsTag` propagation is now available) and add an `AttributedString` semantics surface, then port `_childSemanticsConfigurationsDelegate` and the tagged placeholder nodes. |
| `src/Plumix/Widgets/Text.cs`, `src/Plumix/Widgets/RichText.cs` | Core has no `StrutStyle`, `MediaQuery.boldTextOf`/spacing overrides, `Localizations.maybeLocaleOf`, or `SelectionContainer`-driven `_SelectableTextContainer`. `Text` therefore keeps its flat convenience parameters alongside `style`, resolves its exact scaler from `MediaQuery.TextScalerOf`, and hands the ambient `SelectionContainer` to `RichText` through an internal constructor so the caret configuration still reaches the paragraph. | The `DefaultTextStyle` merge order, `Text.Rich` nesting the supplied span under the resolved style, semantics-label exclusion, exact nonlinear text scaling, and every `RichText` field mapping match. `strutStyle`, bold-text and spacing overrides, per-call locale resolution, and the selectable-container composition are unavailable. | Add `StrutStyle`, the `MediaQuery` accessibility overrides, `Localizations.maybeLocaleOf`, and `_SelectableTextContainer`, then drop the flat parameters and the internal selection constructor. |
| `src/Plumix/Ticker.cs`, `src/Plumix/AnimationController.cs`, `src/Plumix/Scheduler.cs` | C# has no `Future` interface to implement, so `TickerFuture` exposes Dart's future as `Task` plus `OrCancel` instead of being one, and `WhenCompleteOrCancel` runs through `Scheduler.ScheduleMicrotask` because .NET continuations would otherwise run inside the tick that resolved it. `Animation<T>.Value` declares no setter to override, so Dart's `value` setter is `SetValue`; `AnimationController.unbounded` is the static `Unbounded` factory and `vsync` is optional (a bare `Ticker` when omitted). Plumix has no bindings, so `SemanticsBinding.disableAnimations` is the static `AnimationController.DisableAnimations` hook and `SchedulerBinding.framesEnabled` is `Scheduler.FramesEnabled`; `Ticker.Start` also checks `Scheduler.IsHandlingFrame` because `Scheduler.BuildScope` reports the build phase for builds driven outside a frame. Debug creation stacks and `describeForError` are not ported. | Every ticker lifecycle, elapsed-time, mute, absorb, cancellation and simulation behavior matches, including `TickerCanceled`, the zero-duration short circuits, `AnimationBehavior` scaling and `Velocity`. Awaiting a `TickerFuture` uses its `Task`, so a cancelled animation hangs exactly as Dart's does unless the caller uses `OrCancel`, and error messages carry no creation stack. | No runtime close condition for the future and value shapes; a binding object would let `DisableAnimations`/`FramesEnabled` move onto it. |
| `src/Plumix.Material/Scaffold.cs`, `AppBarTheme.cs`, `Tabs.cs`, `TabBarTheme.cs`, `BottomSheet.cs`, `BottomSheetTheme.cs` | Avalonia's sealed `Color` value type cannot implement Flutter's `WidgetStateColor`, so `AppBar.backgroundColor`, `TabBar`/`TabBarThemeData.labelColor`, and bottom-sheet drag-handle colors use `WidgetStateColor?` with implicit conversion from `Color`; Dart runtime type tests become `IsConstantColor` checks where required. The shared preferred-size contract also uses Avalonia `Size` and cannot retain Flutter's private `_PreferredAppBarSize` toolbar/bottom metadata. | Constant and state-resolving colors construct and render identically. Reading these public colors returns the state-color wrapper, and `AppBar.PreferredHeightFor` cannot substitute `AppBarTheme.toolbarHeight` when the widget toolbar height was null. | Introduce framework-owned color and preferred-size value hierarchies, migrate the public contracts away from Avalonia's sealed structs, then expose Flutter's subtype relationships and themed-height metadata directly. |
| `src/Plumix/Widgets/TextSelection.cs`, `src/Plumix.Material/TextSelectionControls.cs` | C# has no mixins, so Flutter's `TextSelectionHandleControls` mixin becomes the marker interface `ITextSelectionHandleControls`; concrete controls declare it and override the four `canX`/`handleX` members plus `buildToolbar` themselves. | Toolbar suppression, the `selectionControls is TextSelectionHandleControls` branch in `SelectionOverlay`, and every default match. A third-party control must repeat the overrides instead of mixing them in. | No runtime close condition; a shared abstract base could carry the overrides once the deprecated `buildToolbar` surface is removed. |
| `src/Plumix/Widgets/App.cs`, `Localizations.cs`, `Restoration.cs`, `Scroll.cs`, `src/Plumix.Material/App.cs`, `src/Plumix.Cupertino/CupertinoApp.cs` | The Navigator- and Router-based `WidgetsApp`/`MaterialApp`/`CupertinoApp` compositions are ported, but core does not yet provide asynchronous localization loading/binding-observer locale updates, restoration-bucket adoption in `WidgetsApp`/`Navigator`/`Scrollable` (the buckets themselves are ported), performance/semantics inspector overlays, or native high-contrast reporting. Scrollables retain restoration-ID-keyed offsets in shared `PageStorage` (`ScrollableState.SaveOffset`, the `IScrollContext` restoration hook, is therefore a no-op), while the corresponding classic-constructor debug/service fields remain accepted metadata where applicable. | Named/home/generated/unknown/deep-link routing, router-delegate and router-config apps, app builders/title, synchronous localization/directionality, shortcuts/actions, Material and Cupertino themes/selection/scroll behavior, Material messenger, Cupertino interface level and system-overlay policy, in-process restoration-ID scroll persistence, host light/dark updates, injected high-contrast policy, and checked/grid debug banners match. Asynchronous resource delegates, automatic runtime locale changes, native high-contrast changes, process-restored scroll state, and specialized debug overlays are unavailable. | Add async localization resolution plus host locale notifications, restoration-bucket adoption in the app/navigator/scroll layers, native contrast plumbing, and the missing debug overlay widgets, then wire the retained fields directly. |
| `src/Plumix/Rendering/Layer.cs`, `FilterLayerRasterizer.cs`, `src/Plumix/PipelineOwner.cs` | Avalonia exposes no native scene-builder backdrop-filter layer or backdrop-ID fusion. Plumix therefore captures the already-painted scene prefix once per `BackdropKey` and applies each grouped output through the CPU image-filter backend. | Widget/render/layer APIs, sampling order, grouping input, bounds, blend modes, and pixels match covered cases; a scene with many grouped filters repeats the final filter/raster operation and can be slower than Flutter's engine-fused path. | Add a backend-native backdrop-filter layer with stable group IDs and retained filter output, then replace the CPU prefix-capture path while preserving the public contracts. |
| `src/Plumix/Widgets/Title.cs` | C# forbids a member with the same name as its enclosing type, so Flutter's `Title.title` property is exposed as `Title.TitleText`; the constructor parameter remains `title`. | Construction, update comparison, opaque-color validation, application-switcher metadata, host dispatch, and About ancestry behavior match; only direct property access uses the language-shaped name. | No runtime close condition exists; a future source generator could expose a Dart-name mapping in generated interop metadata without changing the CLR member name. |
| `src/Plumix/Widgets/MetaData.cs` | C# forbids a member with the same name as its enclosing type, so Flutter's `MetaData.metaData` property is exposed as `MetaData.Metadata`; the constructor parameter remains `metaData`, and `RenderMetaData.MetaData` retains the direct name. | Construction, render updates, payload identity, layout, and all three hit-test behaviors match; only direct widget-property access uses the language-shaped name. | No runtime close condition exists; a future source generator could expose a Dart-name mapping in generated interop metadata without changing the CLR member name. |
| `src/Plumix/Widgets/ConstraintsTransformBox.cs`, `Rendering/Proxy.RenderBox.cs`, `Rendering/Layer.cs` | Avalonia exposes hard-edge and anti-aliased rectangular clips but no public isolated save-layer operation. `ConstraintsTransformBox` therefore maps `Clip.AntiAliasWithSaveLayer` to the same anti-aliased rectangular clip used by `Clip.AntiAlias`. | Constraint transforms, layout, directional alignment, hit testing, ordinary clipping, semantics clips, and debug overflow indicators match. Translucent overflowing children can blend differently at the clipped edge only when `antiAliasWithSaveLayer` is requested. | Add a backend isolated save-layer primitive, then give `ClipRectLayer` a distinct `AntiAliasWithSaveLayer` composition path. |
| `src/Plumix/Widgets/OverscrollIndicator.cs` | Avalonia exposes no Flutter/Impeller fragment-shader filter path for Android's nonlinear stretch effect, so `StretchEffect` always uses Flutter's own documented affine fallback composition. | Stretch direction, strength, origin, clipping, interaction, and return motion match, but pixels differ from the nonlinear shader on Flutter renderers where shader filters are supported. | Add backend fragment-shader/image-filter support and port Flutter's `stretch_effect.frag` uniforms, retaining the affine branch for unsupported hosts. |
| `src/Plumix/UI/RawKeyboard.cs`, `src/Plumix/UI/KeyEventManager.cs`, `src/Plumix/HostKeyboardMap.cs` | Flutter ships one `RawKeyEventData` subclass per embedding, each decoding that platform's native modifier bitmask, plus a `keyboard_maps.g.dart` scan-code table per embedding and a `KeyDataTransitMode` that arbitrates between engine key data and raw channel messages. Plumix has a single Avalonia host and no message channels, so there is one `HostRawKeyEventData` carrying already-normalized modifiers, one `HostKeyboardMap`, and only the `rawKeyData` conversion path. Consequently: modifier sides are reported as `KeyboardSide.Any`/`All` rather than left/right; `_synchronizeModifiers`' recovery for a modifier down whose own flag is missing is applied for every modifier instead of the four platform-gated cases; logical keys are resolved from Avalonia's layout-independent `Key` (US positions) rather than the platform's layout-mapped keyval; and `FlutterError.reportError`/`debugPrint` become the `KeyboardDebug.OnError`/`DebugPrint` hooks. | Logical/physical identities, `keyLabel`/`debugName`, synonyms, lock modes, synthesized/device metadata, the `KeyDownEvent`/`KeyUpEvent`/`KeyRepeatEvent` hierarchy and both listener paths all match. Consumers cannot tell which side a modifier is on from a raw event, cannot read platform-native payloads, and get US-layout logical keys for punctuation on non-US layouts. | Expose per-side modifier state and the layout-mapped unmodified keyval from the host adapter, then split `RawKeyEventData` per platform and gate the modifier recovery the way Flutter does. |
| `src/Plumix/Widgets/Actions.cs` | C# resolves `Action<T>` to the ubiquitous CLR callback delegate, so Flutter's command class uses the explicit name `FlutterAction<TIntent>` while `Actions`, `CallbackAction`, `ContextAction`, intents, dispatchers, and behavior retain their source names. | Lookup, override, enablement, invocation, listener, dispatcher, and key-consumption behavior match; only custom action declarations inherit from `FlutterAction<TIntent>` instead of `Action<TIntent>`. | No runtime close condition exists; generated Dart-to-C# interop metadata may map the source name without shadowing `System.Action<T>`. |
| `src/Plumix/RenderParagraph.cs`, `Widgets/Baseline.cs` | Avalonia `TextLayout` exposes one baseline metric rather than separate Flutter alphabetic and ideographic paragraph baselines. Plumix therefore uses the native baseline for both `TextBaseline` values while preserving distinct values supplied by other render boxes. | Alphabetic placement and all `Baseline`/`IgnoreBaseline` layout contracts match. Ideographic text can sit a few pixels differently when a font's ideographic baseline differs from its alphabetic baseline. | Add a paragraph backend that exposes separate alphabetic and ideographic metrics, then select the requested metric in `RenderParagraph.ComputeDistanceToActualBaseline`. |
| `src/Plumix/Widgets/Magnifier.cs`, `Rendering/Magnifier.cs`, `Rendering/Layer.cs`, `src/Plumix.Material/Magnifier.cs`, `src/Plumix.Cupertino/CupertinoMagnifier.cs` | Avalonia exposes no Flutter-equivalent public backdrop-filter layer; core has root `Overlay` and captured themes, but the route-backed `MagnifierController` cannot preserve Flutter's exact overlay-entry ordering. The compositor builds one vector backdrop snapshot with magnifier layers excluded, and border radii are uniform rather than elliptical/per-corner. | Editable/selectable touch and handle drags now present the adaptive lens with source caret/line/field geometry. Multiple lenses do not magnify lower lenses, transformed ancestors can sample an axis-aligned source, `below` ordering is unavailable, and Cupertino's ellipse resolves to a uniform radius. | Add backend backdrop-filter primitives, transform-aware sampling, overlay-entry ordering, and elliptical/per-corner radii; then switch the lens layer/controller to direct Flutter composition. |
| `src/Plumix.Cupertino/CupertinoTextSelectionToolbar.cs`, `CupertinoDesktopTextSelectionToolbar.cs` | Core now ports the engine's `RoundSuperellipseParam`; borders, clips and popup surfaces use the exact path. The mobile toolbar paints/clips its source arrow path but casts the light-mode shadow from the rounded body, while the desktop toolbar still uses `ContinuousRectangleBorder`; each mobile repaint creates a fresh clip layer. | Rounded-superellipse hit/clip/paint contours match. The light mobile toolbar arrow has no shadow and clip retention can allocate more during animation. | Add backend arbitrary-path shadows and a retained `ClipPathLayer`, then route both toolbar shadows and clips through those primitives. |
| `src/Plumix/Rendering/Proxy.RenderBox.cs` (`RenderTransform`), `src/Plumix/Rendering/Layer.cs` | Flutter's `RenderTransform` is not a repaint boundary: it decides per paint between a plain offset paint (pure-translation matrix), `PaintingContext.pushTransform` and an `ImageFilterLayer` carrying `ui.ImageFilter.matrix(storage, filterQuality)`. Plumix's render objects express compositing through the retained `CreateCompositedLayer`/`UpdateCompositedLayer` protocol instead, so `RenderTransform` is a repaint boundary that always owns a `TransformOffsetLayer`, and `filterQuality` becomes an Avalonia `RenderOptions.BitmapInterpolationMode` on that layer rather than an image filter. | The effective matrix, `origin`/`alignment` conjugation, `transformHitTests`, the singular/non-finite short circuit (`PaintsChild`), `ApplyPaintTransform` and the rendered pixels of an affine or perspective transform all match. Layer counts and types differ from Flutter's, so a test asserting "a pure translation inserts no transform layer" does not hold, and `filterQuality` controls sampling rather than an isolated filtered layer. | Add an `ImageFilter.Matrix` backend entry point and Flutter's `pushTransform(needsCompositing, offset, transform, painter, oldLayer)` contract, then move `RenderTransform` off the composited-layer protocol onto the per-paint decision. |
| `src/Plumix/Rendering/RepaintBoundary.cs` | Flutter's `RenderRepaintBoundary` can synchronously/asynchronously rasterize its current layer through `toImage`; Plumix's Avalonia compositor records reusable display lists but has no render-subtree raster capture API. | Repaint isolation and layer reuse match, but consumers cannot capture a repaint-boundary subtree as a bitmap. | Add an offscreen compositor target and pixel-ratio-aware layer rasterization, then expose synchronous and asynchronous image capture on `RenderRepaintBoundary`. |
| `src/Plumix/Gestures/TapAndDrag.cs`, `GestureRecognizer.cs`, `Events.cs`, `GestureTimer.cs` | `PointerEvent` carries no per-event `transform` matrix (the hit-test path resolves `LocalPosition`/`LocalDelta` directly), so `PointerEventUtils.TransformDeltaViaPositions` is called with a null transform and reduces to the identity. `OneSequenceGestureRecognizer` has no `GestureArenaTeam` and the arena has no `hold`/`release`, and C# has no mixins, so Dart's private `_TapStatusTrackerMixin` is folded into `BaseTapAndDragGestureRecognizer` (every member it contributes keeps its source name). Dart's `Timer.isActive` and `FakeAsync` become `GestureTimer` with a swappable `Factory`. | Every recognizer state transition, callback order, slop and consecutive-tap rule matches for the untransformed pointer paths Plumix's hit testing produces. A recognizer under a scaled or rotated ancestor accumulates drag distance in local rather than global pixels, and teamed recognizers cannot share a single arena entry. | Add a per-event pointer transform plus `GestureArenaTeam` and arena hold/release, then pass the real matrix into the delta transform. |
| `src/Plumix/Widgets/Selection.cs`, `src/Plumix.Material/SelectionArea.cs` | `tap_and_drag.dart`, `tap_region.dart`, `DefaultTextEditingShortcuts` and the `SelectionOverlay` wiring are ported, but core still has no `ProcessTextService`, no `BrowserContextMenu`/`PlatformSelectableRegionContextMenu` bridge and no `Action.overridable`, and the context menu is a route rather than an overlay entry. The region's `_*Action` classes are therefore plain `ContextAction`s (an ancestor override is reached through `Actions.MaybeFind`'s nearest-first lookup instead of a calling-action chain), `SelectionArea` always uses the Material handle controls because the desktop/Cupertino handle-control singletons are not visible to that package, and `SelectableRegion` keeps Plumix's extra `onTap`/`mouseCursor` parameters. | Flutter's platform tap-count, right-click, long-press and drag tables, the consecutive-tap counter, drag handles, magnifier, toolbar anchors, handle-drag geometry, `hideToolbar(hideHandles:)`, orientation hiding, keyboard select-all/copy/granular/directional extension, the `[Copy, SelectAll]` menu, `SelectableRegionSelectionStatus` and the `onSelectionChanged` firing rules all match. Text-processing menu items and the web/native context menu are absent. Because the menu is a route it takes focus and owns a modal barrier, so `SelectableRegionState` suppresses focus-loss selection clearing (and re-entrant hides) while the menu is showing or shown, and a second right click while the menu is open is consumed by the barrier instead of toggling the menu off. | Port `ProcessTextService`, the browser context-menu bridge and `Action.overridable`, move the context menu onto an overlay entry, then expose the desktop/Cupertino handle-control singletons to `SelectionArea`. |
| `src/Plumix/RenderParagraph.Selection.cs`, `RenderParagraph.Text.cs` | Avalonia's `TextLayout` exposes per-line `TextBounds` but no `BoxHeightStyle`/`BoxWidthStyle` beyond tight/line-height, no ICU word iterator, and no layout at all on hosts without a font manager. `GetBoxesForSelection` implements `Tight` and `Max` (the line-spacing and strut styles fall back to `Max`), `GetWordBoundary` segments by letter/digit vs whitespace vs symbol runs, and every metric has a monospaced estimation fallback. The placeholder-aware paragraph-granularity paths (`_isPlaceholder`, `_getOriginParagraph`, `_getParagraphContainingPosition` and their edge updaters) are not ported. | Fragment splitting, geometry, handle types, highlight/handle paint order, edge updates, word/paragraph boundary selection and granular and directional extension match. Word boundaries differ from ICU for scripts without spaces, non-tight box height styles collapse to line height, and a paragraph-granularity drag that starts inside a `WidgetSpan`'s nested paragraph does not absorb the enclosing paragraph. | Expose real box height/width styles and an ICU word iterator through the text backend, then port the four placeholder-aware edge updaters and drop the estimation fallbacks. |
| `src/Plumix.Material/Scaffold.cs`, `ScaffoldLayout.cs`, `src/Plumix/Rendering/Box.cs` | Every `_ScaffoldSlot` is now laid out by `_ScaffoldLayout`, but `BoxConstraints` stays a `readonly record struct`, so `_BodyBoxConstraints` is its `IBoxConstraintsMetadata` payload (`BodyBoxMetrics`) rather than a subclass; no Plumix host reports the iOS/macOS status-bar gesture yet, so `WidgetsBinding.HandleStatusBarTap` is only ever raised by the app or a test; the scaffold root is a `Container(color:)` where Dart uses `Material(color:)`; and `_drawerOpened`/`_endDrawerOpened` are plain fields instead of `RestorableBool`, so `Scaffold` has no `restorationId`. | Slot measurement and order, persistent-footer geometry/alignment/decoration, `_BodyBuilder`'s padding restoration, the status-bar hit-test target and its 1000 ms `easeOutCirc` scroll, drawer layout/paint order and `DrawerController` wiring all match. A status-bar tap does nothing on a real device, the body sits on a plain colored box instead of a Material surface, drawer state is not restored across a process restart, and a constraints type test reads `constraints.Metadata is BodyBoxMetrics`. | Report the platform status-bar gesture from each host adapter, adopt the ported restoration buckets for both drawer flags, and swap the root container for `Material`; the metadata carrier follows from the value type. |
| `src/Plumix/UI/Restoration.cs`, `src/Plumix/Widgets/Restoration.cs`, `RestorationProperties.cs`, `src/Plumix/Scheduler.cs` | The manager now speaks Flutter's `flutter/restoration` `OptionalMethodChannel` (`get`/`put`/`push`) and exchanges `StandardMessageCodec` bytes, but Plumix has no `ServicesBinding` lifecycle, no `Future`/`SynchronousFuture`, no `RendererBinding.deferFirstFrame` and no `num` supertype; C# also has no mixins and reserves `Finalize`. The root bucket is therefore delivered through a callback that runs synchronously once the data is valid instead of a future, the manager is reached through `RestorationManager.Instance` instead of a binding, the first frame is not deferred while loading, `finalize` is `FinalizeBucket`, Dart's private `_RestorablePrimitiveValue(N)` bases are public types with internal constructors, `RestorableNum`/`RestorableNumN` are constrained to `INumber<T>`, and `RestorationMixin` is the abstract `RestorationState : State`. `Scheduler.AddPostFrameCallback` gained a `scheduleFrame` flag so the manager can queue Flutter-style callbacks without forcing a frame. | Bucket value/child APIs, claim/adopt/rename/drop, duplicate-id detection, serialization scheduling, `isReplacing`, `flushData`, scope/root-scope composition, the mixin ordering rules, the channel protocol and every `Restorable*` round-trip match. No Plumix host answers the restoration channel yet, so restoration is off by default; an engine answer that races a host `push` must be discarded by the host adapter rather than by the manager; there is no `RestorableNum<num>`; and the first frame is not held back while the root bucket loads. | Add a first-frame deferral hook and a future-shaped root bucket, then ship a restoration handler in the host adapters. |
| `src/Plumix/Widgets/PlatformMenuBar.cs` | C# forbids a static factory named `Character` alongside the `Character` property, so Dart's `ShortcutSerialization.character` constructor is exposed as `ShortcutSerialization.ForCharacter`; `.modifier` keeps its source name. `ShortcutSerialization.Trigger` and the `shortcutTrigger` channel entry also carry Plumix's normalized key string instead of Flutter's numeric `LogicalKeyboardKey.keyId`, because core models logical keys as strings (see the keyboard-events row). | Serialization fields, the four modifier bit masks, the channel map keys and every shortcut label match. A host bridging to a native menu channel receives a key name rather than a key id, and the character factory uses the language-shaped name. | Add a `LogicalKeyboardKey` value type carrying `keyId`/`valueMask`/`planeMask` (shared with the keyboard-events row), then serialize the id directly; the factory name follows from the language. |
| `src/Plumix/Widgets/Form.cs` | Modern `canPop`/`onPopInvokedWithResult` delegate through Flutter-shaped route pop entries and `FormField.restorationId` now persists `error_text`/`has_interacted_by_user` through the ported restoration buckets, but the deprecated asynchronous `WillPopScope` is not ported. | Validation, autovalidation, save/reset/error, focus-loss behavior, modern route-pop veto/results, and restored field state match. Deprecated `onWillPop` remains unavailable. | Port `WillPopScope` if legacy compatibility is still required. |
| `src/Plumix.Material/InputDatePickerFormField.cs`, `DatePickerDialog.cs`, `DateRangePickerDialog.cs` | The date pickers have not adopted the ported restoration buckets, and core does not yet expose platform formatter configuration, display-feature anchoring, or the complete Flutter shortcut/action layer. Date picker state is therefore not persisted, anchor-point placement is unavailable, and range keyboard traversal is limited to ordinary focus order. | Localized parsing and explicit/theme locale overrides, validation, lazy multi-month range selection, entry-mode switching, exact M2/M3 geometry/theming, semantics, and typed results match. Foldable placement, process restoration, formatter services, and specialized arrow-key traversal differ. | Adopt the restoration buckets and add formatter, display-feature, and shortcut/action primitives, then port the remaining service fields and private keyboard actions directly. |
| `src/Plumix.Material/TimePickerDialog.cs` | `_TimePickerDialogState` has not adopted the ported restoration buckets and core has no `TextInputFormatter` chain, The state machine therefore uses plain fields instead of `RestorableTimeOfDay`/`RestorableEnum`, the hour/minute fields limit length through `maxLength` with a suppressed counter instead of `LengthLimitingTextInputFormatter(2)`, and the dial still converts pointer positions through `RenderObject.GetPaintOffsetToRoot` instead of `globalToLocal`. | Defaults, theme resolution, dial ring/label/geometry and tap/drag selection with 5-minute rounding, entry-mode switching, input parsing/validation and auto-advance, tap-target redirection, sizing, and the full semantics surface match. Process restoration is unavailable and the web-only select-all-on-focus behavior is absent. | Adopt the restoration buckets and add a `TextInputFormatter` chain, then port `_TimePickerDialogState`'s restorable fields, `_HourMinuteTextFieldState`'s formatters and web focus action, and `globalToLocal` directly. |
| `src/Plumix/Rendering/ImageProvider.cs`, `ImageStream.cs` | Avalonia's public bitmap decoder exposes a single decoded image rather than Flutter's codec/frame API, so providers currently complete through `OneFrameImageStreamCompleter`. | Animated image formats render as one static frame; frame timing and repetition-count behavior are unavailable. | Add a platform codec abstraction that exposes frame count/durations and port `MultiFrameImageStreamCompleter` scheduling. |
| `src/Plumix/Widgets/Image.cs`, `Rendering/Image.cs` | Avalonia's `IImage` has no Flutter-like cloneable native handle, and the browser host has no HTML platform-view image bridge. `RawImage` therefore borrows its image reference, `Image` resolves providers without `ScrollAwareImageProvider` (the scroll-velocity recommendation itself now exists as `Scrollable.RecommendDeferredLoadingForContext`), and `Image.Network` omits `webHtmlElementStrategy`. | `Image` owns decoded lifetime safely, but direct `RawImage` callers must keep the supplied image alive while mounted; rapid offscreen flings can decode images that Flutter would defer, and cross-origin browser images cannot opt into the HTML-element escape hatch. | Add a reference-counted image-handle abstraction and an HTML platform-view bridge; then clone handles in `RawImage`, port `ScrollAwareImageProvider` on top of the existing recommendation, and add the browser strategy branch. |
| `src/Plumix/Rendering/Decoration.cs` (`ShapeDecorationPainter`) | Avalonia's drawing backend paints `BoxShadows` only through its rounded-rectangle primitive, with no shadow operation for an arbitrary geometry. `ShapeDecoration` therefore paints its shadows through the rounded rectangle the shape reduces to (rounded rect, stadium, circle, `BoxBorder`), and skips them for shapes that have no such equivalent. | Fill, outline, image clip, hit testing and clip paths match for every shape. A `ShapeDecoration` whose shape is a star, bevel, continuous or compound border paints no shadow where Flutter would. | Add a backend path-shadow primitive, then paint every shadow from `shape.getOuterPath` regardless of shape. |
| `src/Plumix/Rendering/DecorationImage.cs`, `Rendering/Image.cs`, `Object.PaintingContext.cs` | Avalonia's public `DrawingContext` path does not expose Flutter-equivalent per-draw color matrices, smart inversion, or an isolated `saveLayer` for additive decoration-image blending. Sampling quality and edge antialiasing are now mapped through Avalonia `RenderOptions`. | `colorFilter` and `invertColors` do not change pixels; mid-transition image crossfades use source-over rather than Flutter's isolated additive blend and can have a small alpha difference. | Add a backend image-effect/save-layer bridge, then wire color matrices, inversion, and isolated additive blending. |
| `src/Plumix/Rendering/Gradient.cs` | Avalonia's gradient brushes cannot express three parts of dart:ui's shader contract. `RadialGradient.FocalRadius` is dropped (`RadialGradientBrush` has a focal point but no focal radius, so a focal gradient is the backend's zero-radius two-point conical); `TileMode.Decal` maps to `Pad` because `GradientSpreadMethod` has no decal; and the Skia conic brush ignores the spread method, so `SweepGradient`'s sector is folded into the stop offsets (`Angle = 90 + startAngle`) and only `Clamp` behaves as Dart specifies. The shader matrix is also an Avalonia 2D affine `Matrix` rather than the framework `Matrix4` the transform pipeline uses, rebased onto the painted rectangle's origin because the backend applies brush transforms in the primitive's own space. | Colors, stops, alignment resolution, radius/center geometry, rotation transforms, `Clamp`/`Repeated`/`Mirror` linear and radial tiling, and every lerp/scale/opacity value match. A focal gradient draws with a point focus, `decal` paints as `clamp`, and a sweep gradient's non-clamp tiling and out-of-sector region paint clamped. | Add backend focal-radius, decal and conic spread/sector support (or a raw shader path), then map `createShader` onto it directly. |
| `src/Plumix/Rendering/BoxShadow.cs` | Avalonia's drawing backend takes shadows as `Avalonia.Media.BoxShadows` (offset/blur/spread/color) with no mask-filter style, so `BoxShadow.BlurStyle` is carried and interpolated but always painted as `Normal`, and `Shadow.BlurSigma`/`ToPaint` have no backend consumer. C# equality is also symmetric, so a `Shadow` never equals a `BoxShadow`, where Dart's `Shadow.==` (which omits the `runtimeType` check) does. | Color, offset, blur radius, spread radius, list interpolation and the `blurStyle` propagation rule match. `solid`/`outer`/`inner` shadows render as `normal`, and a mixed `Shadow`/`BoxShadow` comparison is false in both directions. | Add a backend mask-filter/blur-style primitive, then paint each shadow through it. |
| `src/Plumix/Rendering/Decoration.cs` | Dart's `BoxDecoration.boxShadow` is a `List<BoxShadow>`; a positional record parameter named `BoxShadow` would shadow its own element type inside the record's own parameter list, so the property keeps the plural name `BoxShadows`. | Construction, lerp, painting and equality match; only the property spelling differs. | No runtime close condition exists; generated Dart-to-C# interop metadata may map the source name. |
| `src/Plumix/RenderParagraph.cs`, `Widgets/DefaultTextStyle.cs` | Avalonia `TextLayout` does not expose Flutter's first-ascent/last-descent height trimming controls. `TextHeightBehavior` is propagated through `DefaultTextStyle` and retained by `RenderParagraph`, but both flags currently use Avalonia's native line-height behavior. | Style, alignment, wrapping, overflow, max-lines, width-basis inheritance, and animation match; disabling height on the first ascent or last descent can leave a small extra leading difference. | Add a paragraph backend that exposes first/last line metric trimming, then apply both `TextHeightBehavior` flags during measurement and paint. |
| `src/Plumix/Rendering/PhysicalModel.cs`, `Object.PaintingContext.cs`, `src/Plumix.Cupertino/CupertinoSlidingSegmentedControl.cs` | Avalonia's public `DrawingContext` exposes neither Flutter's isolated save-layer clip nor `drawShadow` for arbitrary paths. `PhysicalModel`/`PhysicalShape` preserve source clip API and geometry; non-`none` geometry clips use backend sampling, while arbitrary-path elevation and the sliding segmented control's exact superellipse thumb use deterministic blurred-outline composition. | Layout, fill, rectangular/circular/custom-path clipping, animation, hit testing, and ordinary shadows match. `antiAliasWithSaveLayer` child blending and complex-path shadow falloff can differ at the edge. | Add backend clip-quality/save-layer and path-shadow primitives, then map every `Clip` value and Flutter physical shadow directly. |
| `src/Plumix/UI/Path.cs`, `Rendering/CustomClip.cs`, `Rendering/Layer.cs` | Avalonia exposes hard-edge and anti-aliased geometry sampling but no public isolated save-layer operation. The framework path surface currently covers rect/rounded-rect/oval plus line/quadratic/cubic contours; curve containment and paint are flattened deterministically. | `ClipOval` and ordinary `ClipPath` layout, paint, hit testing, semantics, and reclip behavior match. `antiAliasWithSaveLayer` uses ordinary anti-aliasing, and points extremely close to complex curved boundaries can differ slightly. | Add an isolated backend save-layer primitive plus an exact backend-neutral path command/containment engine, then map `antiAliasWithSaveLayer` and unflattened curves directly. |
| `src/Plumix/UI/Path.cs` (`Combine`) | The framework path model stores flattened contours and has no boolean path solver, so `Path.Combine` keeps its two operands and defers the operation to Avalonia's `CombinedGeometry` when painting, resolving `Contains`/`GetBounds` set-wise. | Painting, containment and bounds match for the difference/intersect/union/xor operations; a combined path cannot be extended with further contours. | Add a contour-level boolean path solver and flatten the result. |
| `src/Plumix.Material/InkFeatures.cs` (`InkSparkle`) | Avalonia's public drawing backend does not expose Flutter's bundled `FragmentProgram`/SkSL runtime shader path. | The 617ms radius, center, splash/sparkle alpha sequences, clipping, color, and deterministic turbulence seed match; the organic noise field is approximated with layered circles and deterministic white sparkles, so pixels differ from Android 12's shader. | Add a backend fragment-shader abstraction and port Flutter's `ink_sparkle.frag` uniforms directly. |
| `src/Plumix/Painting/TextPainter.cs` | Flutter's `TextPainter` is also the engine behind `RenderParagraph`; Plumix retains and paints an Avalonia `TextLayout`, while paragraph render objects still own selection boxes, caret metrics and hit testing. Headless hosts use the monospaced `TextLayoutFallback`, which has nothing to paint. | Measurement and retained-layout paint match. Selection/caret/hit-test callers still use `RichText`/`RenderParagraph`, and fallback paint is empty. | Move `RenderParagraph` layout onto the shared painter and expose selection, caret and hit-test metrics plus fallback glyph runs there. |
| `src/Plumix/Widgets/Icon.cs`, `src/Plumix.Cupertino/CupertinoSwitch.cs` | Avalonia's public text API exposes discrete font weights but not Flutter's `FILL`/`wght`/`GRAD`/`opsz` variation-axis paint path or text-shadow list. `Icon` carries the source fields; the switch maps weight to the nearest discrete value and omits icon glyph shadows. | Ordinary Cupertino switch icons match; variable-symbol fill/grade/optical-size and glyph-shadow pixels can differ. | Add font-axis and text-shadow support to `TextStyle`/`ParagraphSource`, then pass all icon and ambient icon-theme values through `TextPainter`. |
| `src/Plumix.Material/SnackBar.cs` (Hero tag) | Dart tags the snack bar's `Hero` with `'<SnackBar Hero tag - ${widget.content}>'`, which is content-specific because every Dart widget's `toString` prints its fields. Plumix widgets inherit `object.ToString`, so the interpolation yields the content widget's type name and the tag collapses to one per content *type* rather than per content value. | The snack bar still heroes across a route push, and because the root `ScaffoldMessenger` presents one bar at a time the shared tag never produces a duplicate-tag Hero throw. Two snack bars whose contents differ only in text now share a tag, so a route transition between them animates as one hero where Dart would treat them as unrelated. | Give the widget hierarchy Dart-shaped `ToString`/`DebugFillProperties` output, then the existing interpolation becomes content-specific with no change here. |
| `src/Plumix/Widgets/Navigation.cs`, `Navigation.NavigatorState.cs`, `Navigation.RouteEntry.cs` | `TickerFuture` is now ported but routes do not yet hand it back, and core has no `WidgetsBinding.cancelPointer` and no accessibility-focus channel, and .NET dictionaries reject the `null` key Flutter uses for "the bottom of the stack". A route therefore reports its entering transition through an internal `PushSettled` signal instead of returning a future from `DidPush`; the navigator does not wrap its overlay in `Listener`/`AbsorbPointer` to cancel active pointers after a navigation, `handleDidPopNext` does not re-issue the platform accessibility focus event, and the declarative page maps use the null-tolerant `RouteRecordMap<TKey, TValue>` behind the Flutter-shaped `IReadOnlyDictionary<RouteTransitionRecord?, T>` contract. The deprecated `onPopPage`/`WillPopScope` surfaces are not ported, and `Pop` on the only remaining route throws instead of emptying the history the way Flutter's debug assert forbids. | The route-entry lifecycle, `_flushHistoryUpdates` ordering, observer queues and `DidChangeTop`, declarative `Pages`/`TransitionDelegate` routing, the seven `ModalRouteAspect`s, the modal scope's `RestorationScope`/focus-scope composition, and the `Restorable*` route family all match. A navigation does not cancel in-flight pointers, and assistive technologies do not get focus restored onto the revealed route after a pop, and a custom `TransitionDelegate` receives Plumix's map implementation. | Return the route's ticker future from `DidPush`, add a pointer-cancel hook plus an accessibility channel in every host adapter, then route `DidPush`, `_cancelActivePointers` and `handleDidPopNext` through them. |
| `src/Plumix/Widgets/ModalBarrier.cs`, `src/Plumix/UI/SystemSound.cs`, `HapticFeedback.cs` | System-sound and haptic requests now travel Flutter's `flutter/platform` channel (`SystemSound.play`, `HapticFeedback.vibrate`) and `PlumixHost` answers it, but no platform host invokes native sound/vibration APIs yet; the shared semantics model also has no text-direction metadata field. | Non-dismissible barriers issue the Flutter-shaped alert message and Cupertino scrollbars request medium-impact feedback with source timing, and the payload strings match Dart, but default host adapters remain silent. Barrier labels/actions and scrollbar interaction behavior match; bidi announcement direction is left to the host. | Implement native click/alert/haptic output in each platform host override and add text direction to the shared semantics configuration/node bridge. |
| `src/Plumix/Rendering/SliverPersistentHeader.cs`, `src/Plumix/Widgets/SliverPersistentHeader.cs` | C# has no mixins, so Flutter's `_RenderSliverPersistentHeaderForWidgetsMixin` is folded into the base class: `MinExtent`/`MaxExtent` are settable properties the element pushes from the delegate instead of live delegate reads, and `updateChild` is reached through an internal `ChildBuilder` callback the element installs. | Every extent, geometry, stretch, snap and reveal behavior matches, because a delegate's extents are contractually constant and the element pushes new ones whenever `ShouldRebuild` fires. | Expose the delegate reads directly once a source generator can model the mixin. |
| `src/Plumix/Widgets/TextInput.cs`, `src/Plumix.Material/TextField.cs`, `TextFormField.cs`, `src/Plumix.Cupertino/CupertinoTextField.cs`, `CupertinoTextFormFieldRow.cs` | Core text editing owns retained editable geometry, pointer/handle selection, adaptive magnifiers, explicit/default spell-check contracts, the input-formatter chain and the autofill workflow, but not autofill *services* beyond the channel messages, restoration-bucket adoption, or scroll-controller integration. `TextCapitalization`/`SmartDashesType`/`SmartQuotesType` and `scrollPadding` are carried as IME/scroll metadata only, there is no `SystemContextMenu` (iOS system menu) branch, a null `onTapOutside` performs no default unfocus, and `onTap` fires on every tap regardless of `onTapAlwaysCalled` because the selection-gesture builder is not split per tap kind. | Keyboard/IME/pointer/handle editing, focus, context menus, misspelling results/actions, read-only/obscured/multiline input, grapheme max length, submission, form validation, formatter application, autofill registration/hints and semantics match. Restoration IDs are ignored, desktop hosts do not apply auto-capitalization/smart punctuation themselves, and a long single-line value cannot be scrolled independently of the field. | Add a scroll-controller service, adopt the restoration buckets, plus host-side capitalization/smart-punctuation and the system-context-menu channel, then wire them through `EditableText`/`TextField`. |
| `src/Plumix.Material/InputBorder.cs` | `ShapeBorder.Paint` cannot be overridden with the extra gap parameters Flutter adds in `InputBorder.paint`, because C# forbids widening an override's parameter list. The inherited three-argument `Paint` is therefore sealed and forwards to a gap-aware overload whose `gapStart` has no default value. | Painting, gap geometry and every `ShapeBorder` contract match; only a positional `Paint(context, rect, null)` call is ambiguous and has to name its argument. | Nothing to close: the shape follows from the language. |
| `src/Plumix.Material/DataTable.cs` | The semantics compiler now hands `childConfigurationsDelegate` the descendant merge-up configurations, so `InputDecorator`'s affixes form sibling nodes exactly as in Flutter. `DataTable` still places its column-header boundary outside its `InkWell`, because the compiler loses an inner header role while bucketing cells. | Data-table roles, labels and actions match, while a sortable header carries its action in a nested node instead of Flutter's single merged header node. | Retain an inner role while bucketing table cells, then restore the source wrapper position. |
| `src/Plumix.Cupertino/CupertinoDialog.cs`, `CupertinoActionSheet.cs` | The sliding-tap gesture tracks the raw primary pointer instead of porting `_SlidingTapGestureRecognizer`/`_TargetSelectionGestureRecognizer`, which arbitrate against scrollables in the gesture arena and re-hit-test the whole view. | Press highlight, slide-between-buttons, slide-from-content, disabled skipping, confirm-on-release and the action sheet's slide-only selection haptics match; a vertical drag on a long actions list both scrolls and keeps sliding selection instead of yielding to the scrollable, and non-primary pointers are ignored rather than scroll-capable. | Port the arena-participating recognizers (pointer-up keeps the arena, global re-hit-test per move) onto the shared drag pipeline. |
| `src/Plumix.Cupertino/CupertinoTheme.cs` | `CupertinoDynamicColor` resolves brightness and interface elevation only; the host accessibility high-contrast flag is not surfaced, so the four high-contrast variants are stored but never chosen. | Normal-contrast rendering matches; high-contrast users see the normal palette. | Surface the host high-contrast accessibility flag through `MediaQuery` and consult it in `CupertinoDynamicColor.ResolveFrom`. |
| `src/Plumix.Material/DialogTheme.cs` | Dart's legacy field-based `DialogTheme` constructor plus its obsolete widget-level `copyWith`/`lerp` back-compat shims are not ported; only the `data`-based surface exists. The feature-flagged desktop windowing branch of `showRawDialog` (full-window dialogs) is likewise not ported. | Code migrating from the deprecated Dart surface must pass `DialogThemeData` explicitly; default (non-windowing) dialog behavior is unaffected. | Add the legacy members if a consumer needs source-shaped migration compatibility; port the windowing shell when the framework grows multi-window support. |
| `src/Plumix/Scheduler.cs`, `src/Plumix/SchedulerPriority.cs` | Plumix has no `BindingBase`, no `FlutterError.reportError` and no `dart:developer` `Flow`/`Timeline`. `Scheduler` is static, so `SchedulingStrategy` receives only the priority instead of `(priority, scheduler)`, `locked`/`unlocked` have no counterpart, and the `debugLabel`/flow arguments are accepted but not emitted to a timeline. A task that throws faults the returned `Task` instead of leaving Dart's future permanently uncompleted. | `ScheduleTask` ordering (priority descending, insertion order within a priority), the default strategy's animation-priority gate while transient callbacks are pending, one task per event-loop turn, and `Priority`'s values and clamped `+`/`-` offsets match. Profiling timeline events are absent, and `await`ing a failed task throws rather than hanging. | Add a binding object, an error-reporting surface and a timeline abstraction, then pass the scheduler to the strategy and report failures Flutter's way. |
| `src/Plumix.Material/PageTransitionsTheme.cs` (`FadeUpwardsPageTransitionsBuilder`) | Dart widens `buildTransitions`' `route`, `context` and `secondaryAnimation` to nullable in this subclass and ignores all three; C# forbids widening an override's parameter list, so the null-argument form is a separate two-argument `BuildTransitions(animation, child)` overload. | The slide (`Offset(0, 0.25)` → zero, `Curves.fastOutSlowIn`) and fade (`Curves.easeIn`) composition matches, and the inherited five-argument override forwards to the same widget. Only a caller passing nulls uses the shorter overload. | No runtime close condition; the shape follows from the language. |
| `src/Plumix/Rendering/Sliver.cs`, `src/Plumix/Rendering/SliverFixedExtentList.cs` | Flutter derives `RenderSliverFixedExtentList` from `RenderSliverFixedExtentBoxAdaptor`. Plumix's `RenderSliverFixedExtentBoxAdaptor` (added for `CarouselView`) is a strict port of the Dart base, but the pre-existing `RenderSliverFixedExtentList`/`RenderSliverVariableExtentList` pair still carries its own simplified layout loop and an extent-strategy object instead of deriving from it. | Both paths lay out uniform and varied fixed-extent lists identically; only carousel-style subclasses that need the per-index layout hooks must derive from the new adaptor. Two implementations of the same Dart class have to be kept in step. | Rebase `RenderSliverFixedExtentList` (and the varied-extent list) onto `RenderSliverFixedExtentBoxAdaptor` and delete the duplicated layout loop. |
| `src/Plumix/Rendering/TableBorder.cs` | Avalonia exposes no isolated `drawDRRect` or per-corner rounded-ring primitive, and `BorderSide` has no `strokeAlign`. Interior divisions are drawn as one stroked line per division instead of a single stroked path, and both radius branches build the ring from a combined geometry deflated by a single uniform inset. | Uniform and non-uniform border sides, interior division placement, colors, widths, `BorderStyle.None` suppression, paint order, `Scale`/`Lerp` and `Dimensions` match. A non-uniform border radius on a table whose visible outer sides differ in width can round its inner edge slightly differently, and hairline (`width == 0`) outer sides paint as a stroked edge rather than a filled quad. | Add a backend `drawDRRect`/per-corner ring primitive and a `strokeAlign`-carrying `BorderSide`, then paint the outer border from Flutter's inset/outset rounded rects. |
| `src/Plumix/Rendering/Table.cs` | Dart's `TableColumnWidth` hierarchy declares no `==`, so Flutter compares column widths by identity and relies on `const` canonicalization; the C# port models them as records with value equality. `IntrinsicColumnWidth.flex` is also exposed as `FlexFactor` because C# forbids a property named `Flex` alongside the inherited `Flex(cells)` method. | Every width computation, flex distribution and relayout trigger matches for `const`-equivalent values. A freshly constructed but structurally equal `TableColumnWidth` does not dirty layout where Flutter's would, and the constructor parameter stays `flex` while direct property access uses the language-shaped name. | No runtime close condition; generated Dart-to-C# interop metadata could expose the source name, and dropping value equality would only add redundant relayouts. |
| `src/Plumix/UI/BinaryMessenger.cs`, `PlatformChannel.cs`, `MessageCodec.cs` | Plumix has no engine process, so the platform side of a channel is a second handler table on the same messenger: `BinaryMessenger` gains `SetPlatformMessageHandler` and `MethodChannel` gains `SetPlatformMethodCallHandler`, and `Send` completes inline whenever that handler is synchronous. There is also no binding hierarchy (`ServicesBinding.Instance` is a settable object, not a binding, and no assert fires when a channel is used before initialization), no `ChannelBuffers`, no background-isolate messenger, and no channel profiling (`debugProfilePlatformChannels`/timeline events). `Exception.Message` is not nullable in .NET, so Dart's `PlatformException.message`/`MissingPluginException.message` are `ErrorMessage`, and the `error` envelope a failing handler produces carries `Exception.Message` rather than Dart's `toString()`. | Channel names, codecs, envelope bytes, missing-plugin behavior, handler-to-envelope mapping and reply routing match, so ported plugin code is wire-compatible. A host registers the platform side in-process instead of through an engine, the envelope's error text lacks the .NET exception type, and no channel traffic appears in a timeline. | Give the framework a binding hierarchy with channel buffers and a profiling surface; the exception-property names follow from the language. |
| `src/Plumix/Foundation/TypedData.cs`, `Serialization.cs`, `src/Plumix/UI/MessageCodecs.cs` | `dart:typed_data` has no .NET equivalent, so `Endian` and `ByteData` are framework-owned types and the typed-list getters (`GetUint8List`, `GetInt32List`, ...) copy instead of returning views over the source buffer — which also means an unaligned offset never throws the way Dart's `asInt32List` does. Dart's `Map<Object?, Object?>` accepts a `null` key; `Dictionary<object, object?>` does not, so `StandardMessageCodec` rejects a null map key on decode with `FormatException`. `JSONMessageCodec` is a hand-written writer plus `System.Text.Json` reader instead of `dart:convert`, and decodes integral JSON numbers as `int` when they fit and `long` otherwise. | Every type discriminator, size encoding, alignment rule and envelope byte matches Dart, so messages round-trip between the two runtimes unchanged. Decoded typed lists are independent copies, a message carrying a null map key fails to decode, and JSON integers arrive as the narrowest .NET integer type. | Add a null-tolerant map type for decoded payloads and a buffer-view abstraction; the codec's wire behavior is already closed. |
| `src/Plumix/UI/PlatformChannel.cs` (`EventChannel`) | Dart's `Stream`/`StreamController.broadcast` is modelled as `IObservable<T>` plus an internal broadcast subject, the way `StreamBuilder` already models Dart streams. Unlike the `IObserver<T>` contract — but exactly like Dart — an error delivered through `OnError` does not end the subscription. | `listen`/`cancel` timing, arguments, end-of-stream on a null message and `PlatformException` delivery match. A subscriber written against Rx conventions may not expect further events after an error. | No runtime close condition; the shape follows from the framework's stream model. |
