# Divergence Registry

Single source of truth for intentional behavior/structure divergences from Flutter (Dart).

Rules:

- Every intentional divergence from Flutter behavior must have exactly one row here, added in the same iteration that introduces it (see `docs/ai/PORTING_MODE.md`).
- A row is removed only when the divergence is closed; record the closure in `CHANGELOG.md`.
- Divergences recorded before 2026-07-01 live in archived feature notes under `docs/ai/notes/`. Migration rule: when you touch a control/subsystem, check its notes for still-active divergences, move them into this table, and do not add new divergence text to notes.

Row format:

- **Area/File**: framework file(s) the divergence lives in.
- **Divergence**: what differs from Flutter and why (platform/runtime constraint).
- **Expected delta**: user-visible or behavioral difference.
- **Close condition**: what must land to remove the divergence.

## Active Divergences

| Area/File | Divergence | Expected delta | Close condition |
| --- | --- | --- | --- |
| _(migrate entries from `docs/ai/notes/` as controls are touched)_ | | | |
| `src/Plumix/Rendering/Box.RenderBox.cs`, `Proxy.RenderBox.cs`, `Flow.cs`, `Image.cs`, `CustomMultiChildLayout.cs`, `SliverFill.cs`, `SliverHeaders.cs`, `src/Plumix/Widgets/Intrinsic.cs` | Core `RenderBox` does not yet expose Flutter's cached min/max intrinsic-dimension and dry-layout query protocols. `RenderRotatedBox`, `RenderFlow`, `RenderImage`, and `RenderCustomMultiChildLayoutBox` therefore cannot provide the source intrinsic/dry-layout overrides; non-scrollable `SliverFillRemaining` and child-sized `SliverResizingHeader` use the established speculative-layout fallback to resolve natural extents. | Ordinary layout and covered intrinsic/fill/header geometry match. A future/custom parent cannot query the affected intrinsic dimensions or dry layout, and speculative sizing performs a real child layout; an expanding child that reports infinity under loose constraints is treated as having no intrinsic minimum before its final constrained layout. | Port `getMin/MaxIntrinsicWidth/Height`, `getDryLayout`, dry baseline, caching, and invalidation across `RenderBox`; then add direct source overrides and migrate `IntrinsicWidth`/`IntrinsicHeight`, `RenderSliverFillRemaining`, and `RenderSliverResizingHeader` to those protocols. |
| `src/Plumix/UI/KeyboardEvents.cs`, `src/Plumix/Widgets/KeyboardListener.cs` | The host input bridge currently exposes a normalized key string plus modifier/down-up state rather than Flutter's complete `LogicalKeyboardKey`, `PhysicalKeyboardKey`, platform `RawKeyEventData`, repeat-event, synthesized-event, and device-type metadata. `RawKeyboardListener` therefore receives source-shaped down/up event classes backed by that normalized payload. | Focus ownership, modern/raw callback ordering, down/up delivery, modifiers, semantics, and listener lifecycle match. Consumers cannot distinguish physical scan codes, repeat/synthesized events, device type, or platform-native raw data. | Extend every host adapter and `KeyEvent` with logical/physical identities, repeat/synthesized/device metadata, and platform raw data; then expose the complete Flutter keyboard event hierarchy from both listeners. |
| `src/Plumix/RenderParagraph.cs`, `Widgets/Baseline.cs` | Avalonia `TextLayout` exposes one baseline metric rather than separate Flutter alphabetic and ideographic paragraph baselines. Plumix therefore uses the native baseline for both `TextBaseline` values while preserving distinct values supplied by other render boxes. | Alphabetic placement and all `Baseline`/`IgnoreBaseline` layout contracts match. Ideographic text can sit a few pixels differently when a font's ideographic baseline differs from its alphabetic baseline. | Add a paragraph backend that exposes separate alphabetic and ideographic metrics, then select the requested metric in `RenderParagraph.ComputeDistanceToActualBaseline`. |
| `src/Plumix/Widgets/Magnifier.cs`, `Rendering/Magnifier.cs`, `Rendering/Layer.cs`, `src/Plumix.Material/Magnifier.cs`, `src/Plumix.Cupertino/CupertinoMagnifier.cs` | Avalonia exposes no Flutter-equivalent public backdrop-filter layer or root `Overlay`/captured inherited-theme service. The framework compositor therefore builds one vector backdrop snapshot with all magnifier layers excluded, while `MagnifierController` presents through a root non-opaque Navigator route; the shared border-radius model is uniform rather than elliptical/per-corner. | One ordinary lens matches source zoom/focal/style geometry. Multiple overlapping lenses do not magnify lower lenses, transformed lens ancestors can sample an axis-aligned source, `below` ordering/local inherited-theme capture is unavailable, and Cupertino's default elliptical radius resolves to the supported uniform radius. Editable/selectable controls expose adaptive magnifier configuration but do not present the lens automatically during touch drag. | Add backend backdrop-filter primitives, root Overlay/captured themes and transform-aware scene sampling, plus elliptical/per-corner radii; then switch the lens layer/controller to the direct Flutter composition and drive it from selection gesture state. |
| `src/Plumix.Material/AdaptiveTextSelectionToolbar.cs`, `SpellCheckSuggestionsToolbar.cs`, `src/Plumix/Widgets/TextInput.cs`, `Selection.cs` | Cupertino selection-toolbar widgets, draggable handle overlays, and platform spell-check services are not yet available. Adaptive routing uses the matching Material mobile/desktop toolbar geometry on iOS/macOS, and spell-check suggestions are supplied explicitly instead of being derived from `EditableTextState`. | Android/Fuchsia/Linux/Windows adaptive buttons, automatic editable/selectable menu ownership, Copy/Cut/Paste/Select all policies, localization, anchors, overflow, spell-check styling, and safe/keyboard inset placement match. Apple platforms use Material visuals and spelling suggestions are not populated automatically. | Add Cupertino mobile/desktop toolbar primitives, root follower-owned handles, and spell-check suggestion spans/services; then route Apple branches and port automatic spell-check factories directly. |
| `src/Plumix/Widgets/SliverDecoratedPinned.cs`, `src/Plumix/Rendering/Semantics.cs` | Core semantics does not yet expose Flutter's `SemanticsTag` propagation or the viewport's scrolling/non-scrolling semantic partitions. `PinnedHeaderSliver` retains the source `Semantics(container: true, explicitChildNodes: true)` wrapper but cannot tag a partially scrolled header's descendants with `RenderViewport.excludeFromScrolling`. | Layout, paint, hit testing, pinned overlap, and the explicit semantics container match. Assistive technologies receive the header subtree, but cannot distinguish its pinned descendants from the viewport's scrolling semantic group. | Add semantics tags to configurations/nodes, propagate parent tags to child nodes, and port the viewport's scrolling/non-scrolling semantics partition before applying the source exclusion tag in `RenderPinnedHeaderSliver`. |
| `src/Plumix/Widgets/ReorderableList.cs`, `src/Plumix.Material/ReorderableListView.cs` | Core has no root `Overlay`, custom semantics actions, or continuously ticking edge-drag auto-scroller. The dragged proxy therefore remains a transformed sliver child, edge scrolling advances on pointer updates, and reorder actions are gesture-only. | Keyed state, gesture-arena ownership, immediate/long-press start, animated gaps/pickup elevation, fixed/varied/prototype extents, callbacks, handles, and padding match. A proxy can be clipped or painted below a later sibling, stationary edge holds do not keep scrolling, screen-reader move actions are unavailable, and drop completion is immediate. | Add root overlay/captured-theme support, labeled custom semantics actions, and ticker-backed edge auto-scroll; then move the proxy/drop animation and semantics branches to the Dart structure. |
| `src/Plumix/Widgets/Transitions.cs`, `src/Plumix/Widgets/Basic.cs`, `src/Plumix/Widgets/Flow.cs`, `src/Plumix/Rendering/Proxy.RenderBox.cs`, `src/Plumix/Rendering/Flow.cs` | Flutter transform APIs accept a 4x4 matrix and can express perspective or 3D-axis transforms; the Avalonia-backed framework transform pipeline currently uses a 2D affine `Matrix`. | `ScaleTransition`, Z-axis `RotationTransition`, and affine `FlowDelegate` transforms match paint, hit testing, and semantics. Custom matrix transitions and flow delegates cannot express perspective or X/Y-axis 3D rotation. | Add a framework `Matrix4` value plus backend 3D/perspective transform support, then widen transform widgets/render objects, callbacks, and `FlowPaintingContext.PaintChild`. |
| `src/Plumix/Rendering/RepaintBoundary.cs` | Flutter's `RenderRepaintBoundary` can synchronously/asynchronously rasterize its current layer through `toImage`; Plumix's Avalonia compositor records reusable display lists but has no render-subtree raster capture API. | Repaint isolation and layer reuse match, but consumers cannot capture a repaint-boundary subtree as a bitmap. | Add an offscreen compositor target and pixel-ratio-aware layer rasterization, then expose synchronous and asynchronous image capture on `RenderRepaintBoundary`. |
| `src/Plumix/Widgets/Selection.cs`, `RenderParagraph.cs`, `src/Plumix.Material/SelectableText.cs`, `SelectionArea.cs` | Core has no `InlineSpan`/`TextSpan` tree, draggable selection-handle ownership, bidi-aware selection box styles, or Flutter's complete selection shortcut/action layer. Context menus are route-backed because the framework still lacks a root `Overlay` service; because `SelectableText` currently composes `SelectableRegion`, its context-menu builder receives `SelectableRegionState` rather than Flutter's `EditableTextState`. | Single and multi-widget pointer selection, highlight paint, focus/cursor, callbacks, select-all/copy, adaptive automatic context menus, and theme precedence match. Rich selectable spans, draggable handles, automatic magnifiers, triple-click platform nuances, granular keyboard extent movement, selectable non-text delegates, and `BoxHeightStyle`/`BoxWidthStyle` customization are unavailable. | Add rich spans, generic selection delegates/events, root overlay ownership, and the complete shortcut/action layer; then rebuild `SelectableText` on the complete editable renderer, move context menus to direct overlays, wire handles/magnifiers, and replace the narrowed gesture/service branches with the Dart composition. |
| `src/Plumix.Material/FloatingActionButtonLocation.cs`, `Scaffold.cs` | Standard FAB locations and the animator contract are ported, but `Scaffold` currently applies location changes immediately and does not yet measure live snackbar/bottom-sheet geometry. Core has no live scaffold-geometry notifier or shared FAB transition host to drive animator scale/rotation/interrupt choreography. | Static start/center/end, top/float/docked/contained, mini, RTL, and insets resolve through the Flutter formulas. Changing `floatingActionButtonLocation` jumps to its destination rather than using the 200ms scaling motion, and dynamic snackbar/bottom-sheet avoidance uses no measured height. | Add a live scaffold-geometry/listenable layer and transition host, then wire measured feature bounds plus `FloatingActionButtonAnimator` offset/scale/rotation/restart values into `ScaffoldState`. |
| `src/Plumix.Material/Badge.cs`, `Dropdown.cs`, `DropdownButtonFormField.cs`, `src/Plumix/Widgets/AnimatedSwitcher.cs` | Core now exposes `AlignmentGeometry`/`AlignmentDirectional`, and `Align`/`AlignTransition` resolve them through ambient `Directionality`, but these older alignment-taking APIs still use concrete `Alignment`. Default top-end/center-start behavior still resolves against ambient `Directionality`. | Custom directional alignment values cannot yet be supplied to the listed APIs; callers must choose an LTR/RTL concrete alignment. | Migrate the remaining alignment-taking widgets to the shared `AlignmentGeometry` hierarchy. |
| `src/Plumix/Widgets/Transitions.cs`, `AnimatedSwitcher.cs`, `Visibility.cs` | `TickerMode` exposes Flutter's inherited enabled state, but framework tickers are not yet registered against element ancestry, so disabling a ticker-mode subtree does not mute descendant tickers. | `AnimatedCrossFade` still blocks pointer/focus/semantics participation for its bottom child, and hidden `Visibility`/`SliverVisibility` subtrees preserve the source composition, but descendant animations can continue advancing and consume frame work instead of pausing when `maintainAnimation` is false. | Associate ticker registrations with their owning element/ticker-mode notifier and mute/unmute them when the nearest inherited `TickerMode` changes. |
| `src/Plumix.Material/DropdownMenu.cs`, `DropdownMenuFormField.cs` | Core now has the baseline `MenuAnchor`/`MenuController`, but `DropdownMenu` still uses its established isolated route and lacks restoration, complete text-input, outlined-border, directional-inset, and nested closure plumbing. | Entries, editable/select-only behavior, filtering/search, keyboard traversal, controller state, sizing, principal M3 colors/shape/border/shadow/padding, semantics, selection callbacks, and form lifecycle match. Persisted state, IME/cursor configuration, ancestor `all` closure, and exotic style geometry can differ. | Integrate `DropdownMenu` with the cascading anchor hierarchy after adding restoration, complete text-input, outlined-border/directional-inset, and nested-menu primitives. |
| `src/Plumix.Material/MenuAnchor.cs` | Core now has composited target/follower layers but no root `Overlay` service or menu shortcut/action system. `MenuTheme`, `MenuBarTheme`, and `MenuButtonTheme` resolve global and local submenu-panel, disclosure-icon, surface, and button styles, while `MenuBar`/`SubmenuButton` register nested controllers, close siblings, and lay panels below or alongside their anchors; panels still paint locally. | Nested pointer/controller interaction and menu-theme precedence work, but menus may be clipped by ancestors and do not auto-flip at window edges; `useRootOverlay`, reserved padding, outside-tap consumption, animated choreography, and directional keyboard traversal remain unavailable or baseline-only. | Add root overlay ownership, menu focus/shortcut actions, outside-tap handling, and animation primitives; then move panel composition to the Flutter-shaped follower overlay pipeline. |
| `src/Plumix.Material/SearchAnchor.cs` | Core has no public render-object global-coordinate route geometry, captured inherited-theme route transfer, generic route-transition animation stack, async `FutureOr` suggestion surface, or full text-input configuration plumbing. The search view therefore uses a framework-owned non-opaque route with current theme snapshots, synchronous suggestions, and metadata-only text action/type/smart-dash/smart-quote/capitalization fields. | `SearchBar` visuals, controller open/close, suggestions, selection close flow, view/bar theming, constraints, fullscreen toggle, and callbacks match. The non-fullscreen view appears from the route alignment instead of growing/fading exactly from the anchor rect; async suggestion debounce/network flows and advanced IME behavior are not represented yet. | Add render global-coordinate lookup, captured inherited themes, route transition primitives, async suggestion adapters, and full text-input configuration services, then port Flutter's anchored grow/fade route choreography and remaining input fields directly. |
| `src/Plumix.Material/SearchDelegate.cs` | Core now exposes Flutter-shaped `Animation<T>`, but `SearchDelegate.transitionAnimation` still exposes the active framework `AnimationController`; complete text-input configuration (`TextInputType`, action, autocorrect and suggestions) is also not wired through `TextField`. | Full-screen route ownership, fade, query cursor placement, suggestions/results switching, focus, AppBar slots/theme, typed close result, and single-active-delegate contract match. Delegate consumers cannot substitute an arbitrary animation implementation, and platform keyboard configuration can differ. | Migrate the delegate transition contract to `Animation<double>` and complete text-input configuration plumbing. |
| `src/Plumix/Widgets/Autocomplete.cs`, `src/Plumix.Material/Autocomplete.cs` | Core has composited target/follower geometry but no Flutter-shaped `OverlayPortal`/`TapRegion`, captured inherited-theme transfer, or semantics-announcement service. Autocomplete options therefore use a non-opaque anchored route, preserve ordinary page hit testing, and resolve the axis-aligned global field bounds through the current render tree. | Filtering, async race suppression, focus visibility, selection, highlighted-option inheritance, keyboard/page navigation, split fields, Material defaults, and up/down/most-space placement match. A locally overridden inherited value below the Navigator is not automatically transferred into the options route, rotated/skewed anchors use an axis-aligned bounding box, and result-count announcements are not emitted. | Add shared overlay-portal child layout, tap-region grouping, captured inherited themes, and semantics announcements; then move the options view from the route adapter to direct composited-follower overlay composition. |
| `src/Plumix/Widgets/Form.cs` | Modern `canPop`/`onPopInvokedWithResult` now delegate through Flutter-shaped route pop entries, but core still has no restoration buckets/mixins or deprecated asynchronous `WillPopScope`. `FormField.restorationId` remains API metadata without persisted state. | Validation, autovalidation, save/reset/error, focus-loss behavior, and modern route-pop veto/results match. Deprecated `onWillPop` and process restoration remain unavailable. | Add restoration buckets/mixins and, if legacy compatibility is still required, port `WillPopScope`; then persist the remaining `FormFieldState` properties. |
| `src/Plumix.Material/InputDatePickerFormField.cs`, `DatePickerDialog.cs`, `DateRangePickerDialog.cs`, `TimePickerDialog.cs` | Core does not yet expose restoration buckets, platform `TextInputType`/formatter configuration, locale override scopes, display-feature anchoring, haptic feedback, or the complete Flutter shortcut/action layer. Picker APIs therefore omit keyboard-type/locale/anchor parameters and persisted state; time commits do not vibrate, and range/time keyboard traversal is limited to ordinary focus order. | Localized parsing, validation, dial drag/tap, lazy multi-month range selection, entry-mode switching, M2/M3 geometry/theming, semantics, and typed results match. IME hints, per-call locale, foldable placement, process restoration, haptics, and specialized arrow-key traversal differ. | Add shared restoration, text-input configuration/formatters, localization override, display-feature, haptic, and shortcut/action primitives, then port the remaining service fields and private keyboard actions directly. |
| `src/Plumix.Material/Tooltip.cs` | Tooltip content is composed in a local `Stack`, and plaintext is supported, because shared root `Overlay`/`RawTooltip`, `InlineSpan` rich text, and pointer-ignoring primitives are not yet available in core. | Tooltips cannot escape all ancestor bounds or auto-flip against window edges; `richMessage`, custom position delegate, and interactive rich tooltip content are unavailable. | Land core overlay/raw-tooltip positioning, rich-text spans, and ignore-pointer primitives, then adopt Flutter's exact `RawTooltip` composition. |
| `src/Plumix/Rendering/ImageProvider.cs`, `ImageStream.cs` | Avalonia's public bitmap decoder exposes a single decoded image rather than Flutter's codec/frame API, so providers currently complete through `OneFrameImageStreamCompleter`. | Animated image formats render as one static frame; frame timing and repetition-count behavior are unavailable. | Add a platform codec abstraction that exposes frame count/durations and port `MultiFrameImageStreamCompleter` scheduling. |
| `src/Plumix/Widgets/Image.cs`, `Rendering/Image.cs` | Avalonia's `IImage` has no Flutter-like cloneable native handle, core scrolling has no velocity-based deferred-loading recommendation, and the browser host has no HTML platform-view image bridge. `RawImage` therefore borrows its image reference, `Image` resolves providers without `ScrollAwareImageProvider`, and `Image.Network` omits `webHtmlElementStrategy`. | `Image` owns decoded lifetime safely, but direct `RawImage` callers must keep the supplied image alive while mounted; rapid offscreen flings can decode images that Flutter would defer, and cross-origin browser images cannot opt into the HTML-element escape hatch. | Add a reference-counted image-handle abstraction, scroll-velocity loading recommendations, and an HTML platform-view bridge; then clone handles in `RawImage`, port `ScrollAwareImageProvider`, and add the browser strategy branch. |
| `src/Plumix/Rendering/DecorationImage.cs`, `Rendering/Image.cs`, `Object.PaintingContext.cs` | Avalonia's public `DrawingContext` path does not expose Flutter-equivalent per-draw color matrices, smart inversion, or an isolated `saveLayer` for additive decoration-image blending. Sampling quality and edge antialiasing are now mapped through Avalonia `RenderOptions`. | `colorFilter` and `invertColors` do not change pixels; mid-transition image crossfades use source-over rather than Flutter's isolated additive blend and can have a small alpha difference. | Add a backend image-effect/save-layer bridge, then wire color matrices, inversion, and isolated additive blending. |
| `src/Plumix/Widgets/ImplicitAnimations.cs`, `Rendering/Decoration.cs` | The current core `BoxDecoration` exposes Avalonia `IBrush`/`BoxShadows` rather than Flutter's gradient/shadow value hierarchy, so `AnimatedContainer` interpolates colors, borders, radii, images, and constraints but selects arbitrary brushes/shadow collections at the midpoint. | Animated brush or shadow changes snap at 50%; `CircleAvatar` is unaffected because it animates color, images, and constraints. | Introduce framework-native gradient/shadow lerp contracts and use them from `BoxDecoration.Lerp`. |
| `src/Plumix/RenderParagraph.cs`, `Widgets/DefaultTextStyle.cs` | Avalonia `TextLayout` does not expose Flutter's first-ascent/last-descent height trimming controls. `TextHeightBehavior` is propagated through `DefaultTextStyle` and retained by `RenderParagraph`, but both flags currently use Avalonia's native line-height behavior. | Style, alignment, wrapping, overflow, max-lines, width-basis inheritance, and animation match; disabling height on the first ascent or last descent can leave a small extra leading difference. | Add a paragraph backend that exposes first/last line metric trimming, then apply both `TextHeightBehavior` flags during measurement and paint. |
| `src/Plumix/Rendering/PhysicalModel.cs` | Avalonia's public `DrawingContext` clip path does not expose Flutter's distinct hard-edge, anti-aliased, and anti-aliased-save-layer modes. `PhysicalModel` preserves `Clip` API/state and shape geometry, but all non-`none` values use the backend geometry clip. | Layout, fill, elevation shadow, rectangular/circular clipping, animation, and hit testing match; edge sampling and translucent child compositing can differ for `antiAliasWithSaveLayer`. | Add backend clip-quality and isolated save-layer primitives, then map each `Clip` value directly in `RenderPhysicalModel.Paint`. |
| `src/Plumix/UI/Path.cs`, `Rendering/CustomClip.cs`, `Rendering/Layer.cs` | Avalonia exposes hard-edge and anti-aliased geometry sampling but no public isolated save-layer operation. The framework path surface currently covers rect/rounded-rect/oval plus line/quadratic/cubic contours; curve containment and paint are flattened deterministically. | `ClipOval` and ordinary `ClipPath` layout, paint, hit testing, semantics, and reclip behavior match. `antiAliasWithSaveLayer` uses ordinary anti-aliasing, and points extremely close to complex curved boundaries can differ slightly. | Add an isolated backend save-layer primitive plus an exact backend-neutral path command/containment engine, then map `antiAliasWithSaveLayer` and unflattened curves directly. |
| `src/Plumix.Material/Chips.cs`, `FilterInputChips.cs` | Flutter's `RawChip` uses a private three-slot render object; the current framework lacks slotted render-object widgets, so the same avatar/checkmark/label/delete composition uses `Row`/`Stack`/factor layout while selection and delete drawers use framework controllers. | Extreme text scaling and custom avatar/delete constraints can differ by a few pixels; enable and avatar-drawer timing is not yet exact. | Add shared slotted render-object widget support and port `_RenderChip` geometry plus enable/avatar animation choreography. |
| `src/Plumix.Material/ToggleButtons.cs`, `SegmentedButton.cs`, `BottomAppBar.cs` | Core painting currently exposes uniform `BorderRadius`/`ShapeBorder` rather than Flutter's full per-corner `OutlinedBorder` hierarchy. | Default stadium/rounded geometry, circular bottom-app-bar notches, and state borders match, but asymmetric custom segment and automatic-notch shapes cannot be supplied. | Add the shared `OutlinedBorder`/directional-radius hierarchy, then widen segmented-control and automatic-notch shape APIs. |
| `src/Plumix.Material/Ink.cs`, `InkWell.cs` | Core has no ancestor-owned `MaterialInkController`, so `Ink` composes a local `DecoratedBox` and ink is painted by the response's local render object; the current controller retains one active splash and applies hover/focus highlights immediately. | Ordinary `Ink` + `InkWell` composition, pluggable splash factories, factory precedence, and splash timing/geometry match, but a decoration cannot register on a shared ancestor Material, overlapping splashes do not finish independently, hover/focus fade duration is not animated, and nested ink ownership can differ. | Add ancestor Material ink-feature registration and multi-feature lifetime/fade choreography, then move response paint and `Ink` decorations into that controller and honor `hoverDuration` exactly. |
| `src/Plumix.Material/InkFeatures.cs` (`InkSparkle`) | Avalonia's public drawing backend does not expose Flutter's bundled `FragmentProgram`/SkSL runtime shader path. | The 617ms radius, center, splash/sparkle alpha sequences, clipping, color, and deterministic turbulence seed match; the organic noise field is approximated with layered circles and deterministic white sparkles, so pixels differ from Android 12's shader. | Add a backend fragment-shader abstraction and port Flutter's `ink_sparkle.frag` uniforms directly. |
| `src/Plumix.Material/Material.cs` | Core now has public `ClipOval`, but `Material` has not yet migrated its complete shape/ink composition and still lacks an ancestor-owned `MaterialInkController`. Circle material therefore retains its rounded-rectangle clip composition, while ink features remain locally owned by `InkResponse`. | Canvas/card/button colors, shapes, borders, elevation shadows, surface tint, clipping policy, default text propagation, text-style interpolation, and supported animated visual values match. Non-square circular child clipping and shared ink ownership can differ. | Move circle clipping to the available `ClipOval`, add ancestor material-ink registration, and migrate shape/ink composition together to Flutter's structure. |
| `src/Plumix.Material/MaterialBanner.cs` | `ScaffoldMessenger` now provides snackbar queue/controllers, but banner feature presentation is not yet connected. Core exposes `Animation<T>`, while this control still accepts `AnimationController`; insets use physical `Thickness`, with directional defaults resolved at build time. | Direct-tree banner visuals/actions/transitions match, but custom animation implementations, queued banner show/hide/remove lifecycle, and closed-reason futures remain unavailable. | Extend the shared messenger feature queue to `MaterialBanner`, migrate its animation contract, add directional edge-insets, and route dismissal through the messenger controller. |
| `src/Plumix.Material/SnackBar.cs`, `Scaffold.cs` | Core now exposes `Animation<T>` and Flutter-shaped `Dismissible`, but snackbar still accepts `AnimationController` and has not adopted the shared dismissible composition; live `ScaffoldGeometry` is also unavailable. Swipe therefore removes after the threshold without drag-follow translation, and floating placement cannot continuously avoid a moving FAB/bottom surface. | Default fixed/floating bars, actions, queue lifecycle, overflow, and semantics match; custom animation implementations are unavailable and swipe/FAB relocation motion is simpler. | Migrate the snackbar animation contract, compose swipe behavior through core `Dismissible`, add `ScaffoldGeometry` listenables, and adopt the exact Flutter transition and avoidance choreography. |
| `src/Plumix.Material/BottomSheet.cs` | Core currently has uniform `BorderRadius` and no `DisplayFeatureSubScreen`/foldable geometry. Shared `ModalBarrier`, notifier-driven barrier semantics clipping, and `MouseRegion` primitives now exist, but the modal-sheet route has not yet adopted those source branches. The M3 sheet therefore uses a uniform 28px radius rather than top-only corners, omits `anchorPoint`/barrier semantics clipping, and resolves drag-handle colors for normal/dragged states without a hovered state. | Ordinary persistent/modal sizing, drag, barrier, SafeArea, results, and theming match; default M3 bottom corners are rounded, foldable placement is unavailable, and stateful handle hover tint does not activate. | Add per-corner/directional radius paint and clip plus display-feature sub-screen placement, then migrate the route barrier and handle hover composition to the available shared primitives and Dart structure. |
| `src/Plumix/Widgets/ModalBarrier.cs`, `src/Plumix/UI/SystemSound.cs` | `SystemSound.Play` dispatches through the framework host hook, but platform hosts do not yet invoke native sound APIs; the shared semantics model also has no text-direction metadata field. | Non-dismissible barriers issue the Flutter-shaped alert request, but the default desktop/mobile adapters remain silent. Barrier labels/actions exist with the correct platform policy, while bidi announcement direction is left to the host. Dismiss behavior, gestures, semantics clipping/blocking, and color animation match. | Implement native click/alert output in each platform host's `OnFrameworkSystemSound` override and add text direction to the shared semantics configuration/node bridge. |
| `src/Plumix/Widgets/Scroll.cs`, `Rendering/Sliver.cs`, `Rendering/SliverFill.cs`, `src/Plumix.Material/SliverAppBar.cs` | The viewport still clamps negative overscroll to zero, and the scroll pipeline does not expose Flutter's persistent-header snap configuration, show-on-screen protocol, or a render-layout builder callback. `SliverFillRemaining.fillOverscroll` honors overscroll-aware constraints when supplied, but the built-in viewport cannot currently generate them; app-bar `snap` and `stretch` retain their documented limits. | Fill viewport/remaining layout, shared user-scroll direction/end notifications, resizing/floating headers, collapsing, pinned/floating app-bar reveal, flexible-space composition, regular/medium/large variants, overlapping pinned layout, and scroll-under styling match. A standard `CustomScrollView` cannot visibly stretch `SliverFillRemaining` into leading overscroll; app-bar snap differs after partial release and stretch cannot grow the header. | Preserve negative overscroll in sliver constraints; add persistent-header snap/show-on-screen and render-layout callback contracts; then wire fill stretch plus Flutter's app-bar stretch/snap controllers into the shared pipeline. |
| `src/Plumix/Widgets/TextInput.cs`, `src/Plumix.Material/InputDecorator.cs`, `TextField.cs`, `TextFormField.cs` | Core text editing now provides pointer selection and automatic route-backed contextual toolbars, but not draggable handles, automatic touch magnifiers, input formatter/autofill/spellcheck/restoration services, scroll-controller integration, or private baseline-driven `_RenderDecoration`. Input decoration therefore composes public Row/Stack primitives, uses physical `Thickness` and uniform radii, and animates label withdrawal without the exact outline-gap/error-shake/border tween choreography. | Keyboard/IME and pointer editing, focus, context-menu actions, read-only/obscured/multiline input, grapheme max length, submission, form validation, primary M2/M3 border/fill states, slots, supporting text, theme precedence, and semantics match. Handle/magnifier interaction and extreme baseline/text-scale geometry differ. | Add root follower-owned handles/magnifier gesture integration, formatter/autofill/spellcheck/restoration and directional-insets services, then port `_RenderDecoration`, input-border gap painting, and the complete `TextField` service surface directly. |
| `src/Plumix.Material/Dialog.cs` | Cupertino alert-dialog primitives, display-feature anchoring, and closed-loop focus traversal are not yet available. Material `Dialog`/`AlertDialog` and root/nearest navigator selection match, while inset/alignment APIs use physical `Thickness`/concrete `Alignment`. | `AlertDialog.adaptive`/`showAdaptiveDialog`, foldable `anchorPoint`, and explicit traversal-edge policies are unavailable; ordinary Material dialogs are unaffected. | Port Cupertino dialog/action controls, display-feature sub-screens, directional geometry, and traversal-edge policies, then add the adaptive/advanced route APIs. |
| `src/Plumix.Material/Stepper.cs` | Core now exposes `AnimatedDefaultTextStyle`, `AnimatedSize`, and `AnimatedCrossFade`, but Stepper still uses its retained `Offstage` panels and framework controllers; `Scrollable.ensureVisible` remains unavailable. | Step selection, content, states, and core motion match, but tapping a distant vertical header does not auto-scroll it into view and title/horizontal-height changes still use the earlier transition structure. | Add shared ensure-visible geometry, then migrate Stepper to the available shared animated text/size/cross-fade primitives. |
| `src/Plumix.Material/About.cs` | Cupertino dialog actions, `Title` ancestry, `DraggableScrollableSheet`, and the private responsive master-detail license shell are not available. The adaptive constructor uses Material actions, the executable name is the default metadata fallback, and `LicensePage` uses Flutter's narrow package-list/detail route flow at every width. | Explicit metadata and ordinary dialog/license flows match; Apple adaptive actions and the >=840px side-by-side license browser differ. | Add Cupertino dialog actions, core title metadata lookup, draggable sheets, and the responsive master-detail shell, then switch the adaptive/wide branches to the Dart composition. |
| `src/Plumix/Widgets/Table.cs`, `src/Plumix/Rendering/Table.cs`, `src/Plumix.Material/DataTable.cs`, `PaginatedDataTable.cs` | Core currently provides the fixed/intrinsic/flex column-width modes required by default data-table composition plus shared `PageStorage`, but not Flutter's full `TableColumnWidth` algebra, baseline/fill `TableCell` alignment, arbitrary row `Decoration`, or primary-scroll-controller policy. | Standard tables, sorting/selection, row sizing, colors/dividers, and pagination match; custom column-width strategies, baseline cells, complex row decorations, and automatic restored page index after subtree removal are unavailable. | Complete the shared Table width/alignment/decoration surface, wire paginated index storage, and extend `SingleChildScrollView` primary-controller semantics, then replace the narrowed data-table paths. |
| `src/Plumix.Cupertino/CupertinoScrollbar.cs` | Core does not yet expose platform-brightness media data or haptic feedback, and the shared scrollbar resolver switches Cupertino drag thickness/radius immediately instead of animating the 100ms resize. | Light-mode geometry, fade, delayed drag, and interaction match; dark mode keeps the light thumb color, drag resize snaps, and iOS medium-impact feedback is absent. | Add platform brightness to `MediaQueryData`, a haptic service, and state-value interpolation in the raw scrollbar painter, then resolve Cupertino dynamic color and resize/haptic choreography exactly. |
| `src/Plumix/Widgets/PageView.cs`, `Rendering/PageView.cs`, `src/Plumix.Material/Tabs.cs`, `CalendarDatePicker.cs` | Core now has shared `PageStorage` and scroll-offset restoration, but not Flutter's sliver-backed lazy page viewport pipeline. General `Decoration`/`BoxPainter` contracts exist, but `TabBar.indicator` still accepts the concrete `BoxDecoration`; the page viewport keeps all supplied children mounted, and the calendar rotates a bounded previous/current/next page window instead of using `PageView.builder`. | Tab behavior and ordinary one-month calendar swipes/navigation match. Very large supplied page sets use more memory, tabs keep offscreen state alive, a high-velocity calendar fling cannot skip several months in one settle, and non-box custom tab indicators are not wired yet. | Add a lazy page-sliver adapter, route `PageView` and `CalendarDatePicker` through it with the available PageStorage restoration, and widen `TabBar.indicator` to the `Decoration`/`BoxPainter` contracts. |
