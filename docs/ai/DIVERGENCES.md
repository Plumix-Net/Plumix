# Divergence Registry

Single source of truth for intentional behavior/structure divergences from Flutter (Dart).

Rules:

- Every intentional divergence from Flutter behavior must have exactly one row here, added in the same iteration that introduces it (see `docs/ai/PORTING_MODE.md`).
- A row is removed only when the divergence is closed; record the closure in `CHANGELOG.md`.
- Divergences recorded before 2026-07-01 live in archived feature notes under `docs/ai/notes/`. Migration rule: when you touch a control/subsystem, check its notes for still-active divergences, move them into this table, and do not add new divergence text to notes.

Row format:

- **Area/File**: framework file(s) the divergence lives in.
- **Divergence**: what differs from Flutter and why (platform/runtime constraint).
- **Expected delta**: user-visible or behavioral difference.
- **Close condition**: what must land to remove the divergence.

## Active Divergences

| Area/File | Divergence | Expected delta | Close condition |
| --- | --- | --- | --- |
| _(migrate entries from `docs/ai/notes/` as controls are touched)_ | | | |
| `src/Plumix/Widgets/View.cs` | C# forbids a member with the same name as its enclosing type, so Flutter's `View.view` property is exposed as `View.ViewHandle`; the constructor parameter and `View.Of`/`View.MaybeOf` APIs retain their source names. | View identity, physical metrics, lookup dependencies, and `LookupBoundary` hiding match; only direct widget-property access uses the language-shaped name. | No runtime close condition exists; generated Dart-to-C# interop metadata may map the source name without changing the CLR member name. |
| `src/Plumix/RenderParagraph.cs`, `src/Plumix/Painting/ParagraphSource.cs` | Avalonia's `TextLayout` exposes `HitTestPoint`/`HitTestTextPosition`/`HitTestTextRange` but no `getClosestGlyphInfoForOffset` with grapheme-cluster layout bounds, no direction-carrying `getBoxesForSelection`, and no `BoxHeightStyle`/`BoxWidthStyle`. Span hit testing therefore resolves the text position and checks the reported glyph rect, and `AssembleSemanticsNode` unions the range rects without a per-run writing direction. `RenderParagraph` also keeps Plumix's flat `FontSize`/`FontWeight`/`Foreground`/`Height`/`LetterSpacing`/`TextDecoration` accessors, which project the root span's `TextStyle` instead of existing on the span only, and it exposes `PlainText` because C# cannot overload `Text` on both `InlineSpan` and `string`. | Span-level hit testing, `TextSpan.recognizer` dispatch, mouse-cursor resolution, the `RenderComparison` invalidation switch, per-run semantics nodes with `OrdinalSortKey` and tap/long-press actions, and inline-widget layout/paint/hit testing match. A tap in a justified or wide-letter-spaced gap can miss where Flutter's advance-width cluster bounds would hit, per-run semantic nodes carry no writing direction, and node rects can differ by a pixel from Flutter's floor/ceil-of-engine-boxes. | Add a backend glyph-info API with grapheme-cluster bounds plus direction-carrying selection boxes and height/width styles, then route hit testing and `AssembleSemanticsNode` through them. |
| `src/Plumix/Painting/ParagraphSource.cs`, `src/Plumix/Rendering/InlineChildren.cs` | Core semantics now has `SemanticsTag`, but `WidgetSpan` children are still not tagged with `PlaceholderSpanIndexSemanticsTag` and Flutter's `childConfigurationsDelegate` path is unavailable; `InlineSpanSemanticsInformation.StringAttributes` (spell-out/locale) also has no `AttributedString` to reach the semantics bridge. Placeholder boxes are resolved by walking Avalonia's laid-out lines rather than the engine's `getBoxesForPlaceholders`. | Placeholder sizing, the six `PlaceholderAlignment` rules, truncated-child exclusion, paint and hit-test order match, and the span model records locale/spell-out attributes. Inline-widget semantics merge into the paragraph instead of interleaving as tagged sibling nodes, and assistive technologies do not receive spell-out or locale attributes. | Tag the placeholder spans (core `SemanticsTag` propagation is now available) and add an `AttributedString` semantics surface, then port `_childSemanticsConfigurationsDelegate` and the tagged placeholder nodes. |
| `src/Plumix/Widgets/Text.cs`, `src/Plumix/Widgets/RichText.cs` | Core has no `StrutStyle`, `MediaQuery.boldTextOf`/spacing overrides, `Localizations.maybeLocaleOf`, or `SelectionContainer`-driven `_SelectableTextContainer`. `Text` therefore keeps its flat convenience parameters alongside `style`, resolves its exact scaler from `MediaQuery.TextScalerOf`, and hands the ambient `SelectionContainer` to `RichText` through an internal constructor so the caret configuration still reaches the paragraph. | The `DefaultTextStyle` merge order, `Text.Rich` nesting the supplied span under the resolved style, semantics-label exclusion, exact nonlinear text scaling, and every `RichText` field mapping match. `strutStyle`, bold-text and spacing overrides, per-call locale resolution, and the selectable-container composition are unavailable. | Add `StrutStyle`, the `MediaQuery` accessibility overrides, `Localizations.maybeLocaleOf`, and `_SelectableTextContainer`, then drop the flat parameters and the internal selection constructor. |
| `src/Plumix/Widgets/DraggableScrollableSheet.cs`, `src/Plumix/AnimationController.cs` | Flutter's `TickerFuture` never completes when its animation is cancelled, so `DraggableScrollableController.animateTo` returns a future that hangs forever on interruption. A `Task` that never completes is not a usable C# contract, so an interrupted `AnimateTo` completes its task instead. `AnimationController.Velocity` is also simulation-backed only, because Plumix's duration-driven animations do not run through a `Simulation`. | Sheet sizing, snapping, interruption by drags/jumps/other animations, and the ballistic hand-off velocity boost match. `await controller.AnimateTo(...)` resumes when the animation is interrupted rather than never returning, and reading `Velocity` during a duration-driven animation yields zero. | Port Flutter's `TickerFuture` primary/secondary completer split and route `AnimateTo`/`Forward`/`Reverse` through interpolation simulations, then surface both semantics directly. |
| `src/Plumix.Material/Scaffold.cs`, `AppBarTheme.cs`, `Tabs.cs`, `TabBarTheme.cs`, `BottomSheet.cs`, `BottomSheetTheme.cs` | Avalonia's sealed `Color` value type cannot implement Flutter's `WidgetStateColor`, so `AppBar.backgroundColor`, `TabBar`/`TabBarThemeData.labelColor`, and bottom-sheet drag-handle colors use `WidgetStateColor?` with implicit conversion from `Color`; Dart runtime type tests become `IsConstantColor` checks where required. The shared preferred-size contract also uses Avalonia `Size` and cannot retain Flutter's private `_PreferredAppBarSize` toolbar/bottom metadata. | Constant and state-resolving colors construct and render identically. Reading these public colors returns the state-color wrapper, and `AppBar.PreferredHeightFor` cannot substitute `AppBarTheme.toolbarHeight` when the widget toolbar height was null. | Introduce framework-owned color and preferred-size value hierarchies, migrate the public contracts away from Avalonia's sealed structs, then expose Flutter's subtype relationships and themed-height metadata directly. |
| `src/Plumix/Widgets/TextSelection.cs`, `src/Plumix.Material/TextSelectionControls.cs` | C# has no mixins, so Flutter's `TextSelectionHandleControls` mixin becomes the marker interface `ITextSelectionHandleControls`; concrete controls declare it and override the four `canX`/`handleX` members plus `buildToolbar` themselves. | Toolbar suppression, the `selectionControls is TextSelectionHandleControls` branch in `SelectionOverlay`, and every default match. A third-party control must repeat the overrides instead of mixing them in. | No runtime close condition; a shared abstract base could carry the overrides once the deprecated `buildToolbar` surface is removed. |
| `src/Plumix/Widgets/App.cs`, `Localizations.cs`, `Restoration.cs`, `src/Plumix.Material/App.cs` | The Navigator-based `WidgetsApp`/`MaterialApp` composition is ported, but core does not yet provide Flutter's Router-delegate/configuration subsystem, asynchronous localization loading/binding-observer locale updates, process restoration buckets, performance/semantics inspector overlays, native high-contrast reporting, or route-information engine reporting. The corresponding classic-constructor debug/service fields remain accepted metadata where applicable. | Named/home/generated/unknown/deep-link routing, app builders/title, synchronous localization/directionality, shortcuts/actions, Material themes/messenger/selection/scroll behavior, host light/dark updates, injected high-contrast policy, and checked/grid debug banners match. Router-based apps, asynchronous resource delegates, automatic runtime locale changes, native high-contrast changes, restored route stacks, and specialized debug overlays are unavailable. | Add Router and route-information protocols, async localization resolution plus host locale notifications, restoration buckets/manager, native contrast plumbing, and the missing debug overlay widgets; then add router constructors/factories and wire the retained fields directly. |
| `src/Plumix/Rendering/Layer.cs`, `FilterLayerRasterizer.cs`, `src/Plumix/PipelineOwner.cs` | Avalonia exposes no native scene-builder backdrop-filter layer or backdrop-ID fusion. Plumix therefore captures the already-painted scene prefix once per `BackdropKey` and applies each grouped output through the CPU image-filter backend. | Widget/render/layer APIs, sampling order, grouping input, bounds, blend modes, and pixels match covered cases; a scene with many grouped filters repeats the final filter/raster operation and can be slower than Flutter's engine-fused path. | Add a backend-native backdrop-filter layer with stable group IDs and retained filter output, then replace the CPU prefix-capture path while preserving the public contracts. |
| `src/Plumix/Widgets/Title.cs` | C# forbids a member with the same name as its enclosing type, so Flutter's `Title.title` property is exposed as `Title.TitleText`; the constructor parameter remains `title`. | Construction, update comparison, opaque-color validation, application-switcher metadata, host dispatch, and About ancestry behavior match; only direct property access uses the language-shaped name. | No runtime close condition exists; a future source generator could expose a Dart-name mapping in generated interop metadata without changing the CLR member name. |
| `src/Plumix/Widgets/MetaData.cs` | C# forbids a member with the same name as its enclosing type, so Flutter's `MetaData.metaData` property is exposed as `MetaData.Metadata`; the constructor parameter remains `metaData`, and `RenderMetaData.MetaData` retains the direct name. | Construction, render updates, payload identity, layout, and all three hit-test behaviors match; only direct widget-property access uses the language-shaped name. | No runtime close condition exists; a future source generator could expose a Dart-name mapping in generated interop metadata without changing the CLR member name. |
| `src/Plumix/Widgets/ConstraintsTransformBox.cs`, `Rendering/Proxy.RenderBox.cs`, `Rendering/Layer.cs` | Avalonia exposes hard-edge and anti-aliased rectangular clips but no public isolated save-layer operation. `ConstraintsTransformBox` therefore maps `Clip.AntiAliasWithSaveLayer` to the same anti-aliased rectangular clip used by `Clip.AntiAlias`. | Constraint transforms, layout, directional alignment, hit testing, ordinary clipping, semantics clips, and debug overflow indicators match. Translucent overflowing children can blend differently at the clipped edge only when `antiAliasWithSaveLayer` is requested. | Add a backend isolated save-layer primitive, then give `ClipRectLayer` a distinct `AntiAliasWithSaveLayer` composition path. |
| `src/Plumix/Widgets/OverscrollIndicator.cs` | Avalonia exposes no Flutter/Impeller fragment-shader filter path for Android's nonlinear stretch effect, so `StretchEffect` always uses Flutter's own documented affine fallback composition. | Stretch direction, strength, origin, clipping, interaction, and return motion match, but pixels differ from the nonlinear shader on Flutter renderers where shader filters are supported. | Add backend fragment-shader/image-filter support and port Flutter's `stretch_effect.frag` uniforms, retaining the affine branch for unsupported hosts. |
| `src/Plumix/UI/KeyboardEvents.cs`, `src/Plumix/Widgets/KeyboardListener.cs` | The host input bridge exposes normalized key strings, characters, modifier/down-up/repeat state, and NumLock input but not Flutter's complete `LogicalKeyboardKey`, `PhysicalKeyboardKey`, platform `RawKeyEventData`, synthesized-event, and device-type metadata. `RawKeyboardListener` therefore receives source-shaped down/up event classes backed by that normalized payload. | Focus ownership, modern/raw callback ordering, down/up/repeat delivery, modifiers, character shortcuts, semantics, and listener lifecycle match. Consumers cannot distinguish physical scan codes, synthesized events, device type, or platform-native raw data, and shortcut activators/serializations identify their trigger by normalized key string rather than key id. | Extend every host adapter and `KeyEvent` with logical/physical identities plus synthesized/device/platform raw metadata, then expose the complete Flutter keyboard event hierarchy from both listeners. |
| `src/Plumix/Widgets/Actions.cs` | C# resolves `Action<T>` to the ubiquitous CLR callback delegate, so Flutter's command class uses the explicit name `FlutterAction<TIntent>` while `Actions`, `CallbackAction`, `ContextAction`, intents, dispatchers, and behavior retain their source names. | Lookup, override, enablement, invocation, listener, dispatcher, and key-consumption behavior match; only custom action declarations inherit from `FlutterAction<TIntent>` instead of `Action<TIntent>`. | No runtime close condition exists; generated Dart-to-C# interop metadata may map the source name without shadowing `System.Action<T>`. |
| `src/Plumix/RenderParagraph.cs`, `Widgets/Baseline.cs` | Avalonia `TextLayout` exposes one baseline metric rather than separate Flutter alphabetic and ideographic paragraph baselines. Plumix therefore uses the native baseline for both `TextBaseline` values while preserving distinct values supplied by other render boxes. | Alphabetic placement and all `Baseline`/`IgnoreBaseline` layout contracts match. Ideographic text can sit a few pixels differently when a font's ideographic baseline differs from its alphabetic baseline. | Add a paragraph backend that exposes separate alphabetic and ideographic metrics, then select the requested metric in `RenderParagraph.ComputeDistanceToActualBaseline`. |
| `src/Plumix/Widgets/Magnifier.cs`, `Rendering/Magnifier.cs`, `Rendering/Layer.cs`, `src/Plumix.Material/Magnifier.cs`, `src/Plumix.Cupertino/CupertinoMagnifier.cs` | Avalonia exposes no Flutter-equivalent public backdrop-filter layer; core has root `Overlay` and captured themes, but the route-backed `MagnifierController` cannot preserve Flutter's exact overlay-entry ordering. The compositor builds one vector backdrop snapshot with magnifier layers excluded, and border radii are uniform rather than elliptical/per-corner. | Editable/selectable touch and handle drags now present the adaptive lens with source caret/line/field geometry. Multiple lenses do not magnify lower lenses, transformed ancestors can sample an axis-aligned source, `below` ordering is unavailable, and Cupertino's ellipse resolves to a uniform radius. | Add backend backdrop-filter primitives, transform-aware sampling, overlay-entry ordering, and elliptical/per-corner radii; then switch the lens layer/controller to direct Flutter composition. |
| `src/Plumix.Material/AdaptiveTextSelectionToolbar.cs`, `SpellCheckSuggestionsToolbar.cs`, `src/Plumix/Widgets/SpellCheck.cs` | Cupertino selection-toolbar widgets and host registrations for the default spell-check channel are not yet available. Adaptive routing uses Material mobile/desktop toolbar geometry on iOS/macOS; explicit spell services work, while the default service requires a host to register `PlatformHandler`. | Render-bound handles/magnifiers, editable/selectable menu ownership, standard actions, localization, anchors, overflow, misspelling paint, suggestion replacement, and safe placement match. Apple platforms use Material toolbar visuals, and unregistered hosts do not populate native suggestions. | Port Cupertino toolbar primitives and register each capable host's native spell-check service with `DefaultSpellCheckService.PlatformHandler`. |
| `src/Plumix/Widgets/SliverDecoratedPinned.cs`, `src/Plumix/Rendering/Semantics.cs` | Core semantics now propagates `SemanticsTag`s to the nodes passing through a configuration, but does not expose the viewport's scrolling/non-scrolling semantic partitions. `PinnedHeaderSliver` retains the source `Semantics(container: true, explicitChildNodes: true)` wrapper but cannot tag a partially scrolled header's descendants with `RenderViewport.excludeFromScrolling`. | Layout, paint, hit testing, pinned overlap, and the explicit semantics container match. Assistive technologies receive the header subtree, but cannot distinguish its pinned descendants from the viewport's scrolling semantic group. | Port the viewport's scrolling/non-scrolling semantics partition before applying the source exclusion tag in `RenderPinnedHeaderSliver`. |
| `src/Plumix/Widgets/Transitions.cs`, `src/Plumix/Widgets/Basic.cs`, `src/Plumix/Widgets/Flow.cs`, `src/Plumix/Rendering/Proxy.RenderBox.cs`, `src/Plumix/Rendering/Flow.cs` | Flutter transform APIs accept a 4x4 matrix and can express perspective or 3D-axis transforms; the Avalonia-backed framework transform pipeline currently uses a 2D affine `Matrix`. | `ScaleTransition`, Z-axis `RotationTransition`, and affine `FlowDelegate` transforms match paint, hit testing, and semantics. Custom matrix transitions and flow delegates cannot express perspective or X/Y-axis 3D rotation. | Add a framework `Matrix4` value plus backend 3D/perspective transform support, then widen transform widgets/render objects, callbacks, and `FlowPaintingContext.PaintChild`. |
| `src/Plumix/Rendering/RepaintBoundary.cs` | Flutter's `RenderRepaintBoundary` can synchronously/asynchronously rasterize its current layer through `toImage`; Plumix's Avalonia compositor records reusable display lists but has no render-subtree raster capture API. | Repaint isolation and layer reuse match, but consumers cannot capture a repaint-boundary subtree as a bitmap. | Add an offscreen compositor target and pixel-ratio-aware layer rasterization, then expose synchronous and asynchronous image capture on `RenderRepaintBoundary`. |
| `src/Plumix/Widgets/Selection.cs`, `RenderParagraph.cs`, `src/Plumix.Material/SelectableText.cs`, `SelectionArea.cs` | Core now has the `InlineSpan`/`TextSpan` tree, but no per-fragment `_SelectableFragment` split, bidi-aware selection box styles, or Flutter's complete selection shortcut/action layer. Generic `SelectionArea` context menus remain route-backed, while `SelectableText` now uses the source read-only `EditableText`/`RenderEditable` composition. | Editable `SelectableText` pointer/handle selection, highlight paint, focus/cursor, callbacks, select-all/copy, adaptive context menus/magnifiers, and theme precedence match. Selection still treats a paragraph as one flat run rather than per-placeholder fragments, and triple-click nuances, granular keyboard movement, non-text delegates, and box-style customization are unavailable. | Add `_SelectableFragment` splitting, generic selection delegates/events, complete shortcuts/actions, and bidi box styles, then replace the remaining narrowed selection branches. |
| `src/Plumix.Material/FloatingActionButtonLocation.cs`, `Scaffold.cs` | Standard FAB locations and the animator contract are ported, but `Scaffold` currently applies location changes immediately and does not yet measure live snackbar, bottom-sheet, or Material-banner geometry for `ScaffoldPrelayoutGeometry`. Core has no live scaffold-geometry notifier or shared FAB transition host to drive animator scale/rotation/interrupt choreography. | Static start/center/end, top/float/docked/contained, mini, RTL, and insets resolve through the Flutter formulas. Changing `floatingActionButtonLocation` jumps to its destination rather than using the 200ms scaling motion, and dynamic transient-feature avoidance exposes zero sizes. | Add a live scaffold-geometry/listenable layer and transition host, then wire measured feature bounds plus `FloatingActionButtonAnimator` offset/scale/rotation/restart values into `ScaffoldState`. |
| `src/Plumix.Material/DropdownMenu.cs`, `DropdownMenuFormField.cs` | `DropdownMenu` now attaches to the shared core `MenuController`/anchor tree, but still shows its entries through its established isolated route and lacks restoration, complete text-input, outlined-border, directional-inset, and nested closure plumbing. | Entries, editable/select-only behavior, filtering/search, keyboard traversal, controller state, sizing, principal M3 colors/shape/border/shadow/padding, semantics, selection callbacks, and form lifecycle match. Persisted state, IME/cursor configuration, ancestor `all` closure, and exotic style geometry can differ, and the route's modal barrier blocks the anchor's semantics while the menu is open where Flutter's anchor-based menu leaves them visible. | Integrate `DropdownMenu` with the cascading anchor hierarchy after adding restoration, complete text-input, outlined-border/directional-inset, and nested-menu primitives. |
| `src/Plumix/Widgets/PlatformMenuBar.cs` | C# forbids a static factory named `Character` alongside the `Character` property, so Dart's `ShortcutSerialization.character` constructor is exposed as `ShortcutSerialization.ForCharacter`; `.modifier` keeps its source name. `ShortcutSerialization.Trigger` and the `shortcutTrigger` channel entry also carry Plumix's normalized key string instead of Flutter's numeric `LogicalKeyboardKey.keyId`, because core models logical keys as strings (see the keyboard-events row). | Serialization fields, the four modifier bit masks, the channel map keys and every shortcut label match. A host bridging to a native menu channel receives a key name rather than a key id, and the character factory uses the language-shaped name. | Add a `LogicalKeyboardKey` value type carrying `keyId`/`valueMask`/`planeMask` (shared with the keyboard-events row), then serialize the id directly; the factory name follows from the language. |
| `src/Plumix.Material/SearchAnchor.cs` | Core now exposes render-object ancestor/global-coordinate geometry, `CapturedThemes`, and generic route animations, but `SearchAnchor` has not adopted them; async `FutureOr` suggestions and smart-dash/smart-quote/capitalization services also remain unavailable. The search view therefore uses a framework-owned non-opaque route with current theme snapshots and synchronous suggestions; keyboard type/action now reach platform input while the remaining advanced input fields are metadata. | `SearchBar` visuals, controller open/close, suggestions, selection close flow, view/bar theming, constraints, fullscreen toggle, callbacks, and keyboard/action configuration match. The non-fullscreen view appears from the route alignment instead of growing/fading exactly from the anchor rect; local inherited-theme transfer, async suggestion debounce/network flows, and advanced IME behavior are not represented yet. | Apply the available render geometry, captured themes, and route-transition stack; add async suggestion adapters plus smart punctuation/capitalization services; then port Flutter's anchored grow/fade choreography and remaining input fields directly. |
| `src/Plumix/Widgets/Form.cs` | Modern `canPop`/`onPopInvokedWithResult` now delegate through Flutter-shaped route pop entries, but core still has no restoration buckets/mixins or deprecated asynchronous `WillPopScope`. `FormField.restorationId` remains API metadata without persisted state. | Validation, autovalidation, save/reset/error, focus-loss behavior, and modern route-pop veto/results match. Deprecated `onWillPop` and process restoration remain unavailable. | Add restoration buckets/mixins and, if legacy compatibility is still required, port `WillPopScope`; then persist the remaining `FormFieldState` properties. |
| `src/Plumix.Material/InputDatePickerFormField.cs`, `DatePickerDialog.cs`, `DateRangePickerDialog.cs` | Core does not yet expose restoration buckets, platform formatter configuration, display-feature anchoring, or the complete Flutter shortcut/action layer. Date picker state is therefore not persisted, anchor-point placement is unavailable, and range keyboard traversal is limited to ordinary focus order. | Localized parsing and explicit/theme locale overrides, validation, lazy multi-month range selection, entry-mode switching, exact M2/M3 geometry/theming, semantics, and typed results match. Foldable placement, process restoration, formatter services, and specialized arrow-key traversal differ. | Add shared restoration, formatter, display-feature, and shortcut/action primitives, then port the remaining service fields and private keyboard actions directly. |
| `src/Plumix.Material/TimePickerDialog.cs` | Core has no restoration buckets and no `TextInputFormatter` chain, and `InputDecorationThemeData.FillColor` is a plain `Color` because Avalonia's `Color` is a sealed value type. The state machine therefore uses plain fields instead of `RestorableTimeOfDay`/`RestorableEnum`, the hour/minute fields limit length through `maxLength` with a suppressed counter instead of `LengthLimitingTextInputFormatter(2)`, and the dial still converts pointer positions through `RenderObject.GetPaintOffsetToRoot` instead of `globalToLocal`, and a user `inputDecorationTheme.fillColor` cannot itself be state-resolving. | Defaults, theme resolution, dial ring/label/geometry and tap/drag selection with 5-minute rounding, entry-mode switching, input parsing/validation and auto-advance, tap-target redirection, sizing, and the full semantics surface match. Process restoration is unavailable and the web-only select-all-on-focus behavior is absent. | Add restoration buckets/mixins, a `TextInputFormatter` chain, and a framework-owned state-color type, then port `_TimePickerDialogState`'s restorable fields, `_HourMinuteTextFieldState`'s formatters and web focus action, and `globalToLocal` directly. |
| `src/Plumix/Rendering/ImageProvider.cs`, `ImageStream.cs` | Avalonia's public bitmap decoder exposes a single decoded image rather than Flutter's codec/frame API, so providers currently complete through `OneFrameImageStreamCompleter`. | Animated image formats render as one static frame; frame timing and repetition-count behavior are unavailable. | Add a platform codec abstraction that exposes frame count/durations and port `MultiFrameImageStreamCompleter` scheduling. |
| `src/Plumix/Widgets/Image.cs`, `Rendering/Image.cs` | Avalonia's `IImage` has no Flutter-like cloneable native handle, and the browser host has no HTML platform-view image bridge. `RawImage` therefore borrows its image reference, `Image` resolves providers without `ScrollAwareImageProvider` (the scroll-velocity recommendation itself now exists as `Scrollable.RecommendDeferredLoadingForContext`), and `Image.Network` omits `webHtmlElementStrategy`. | `Image` owns decoded lifetime safely, but direct `RawImage` callers must keep the supplied image alive while mounted; rapid offscreen flings can decode images that Flutter would defer, and cross-origin browser images cannot opt into the HTML-element escape hatch. | Add a reference-counted image-handle abstraction and an HTML platform-view bridge; then clone handles in `RawImage`, port `ScrollAwareImageProvider` on top of the existing recommendation, and add the browser strategy branch. |
| `src/Plumix/Rendering/Decoration.cs` (`ShapeDecorationPainter`) | Avalonia's drawing backend paints `BoxShadows` only through its rounded-rectangle primitive, with no shadow operation for an arbitrary geometry. `ShapeDecoration` therefore paints its shadows through the rounded rectangle the shape reduces to (rounded rect, stadium, circle, `BoxBorder`), and skips them for shapes that have no such equivalent. | Fill, outline, image clip, hit testing and clip paths match for every shape. A `ShapeDecoration` whose shape is a star, bevel, continuous or compound border paints no shadow where Flutter would. | Add a backend path-shadow primitive, then paint every shadow from `shape.getOuterPath` regardless of shape. |
| `src/Plumix/Rendering/DecorationImage.cs`, `Rendering/Image.cs`, `Object.PaintingContext.cs` | Avalonia's public `DrawingContext` path does not expose Flutter-equivalent per-draw color matrices, smart inversion, or an isolated `saveLayer` for additive decoration-image blending. Sampling quality and edge antialiasing are now mapped through Avalonia `RenderOptions`. | `colorFilter` and `invertColors` do not change pixels; mid-transition image crossfades use source-over rather than Flutter's isolated additive blend and can have a small alpha difference. | Add a backend image-effect/save-layer bridge, then wire color matrices, inversion, and isolated additive blending. |
| `src/Plumix/Widgets/ImplicitAnimations.cs`, `Rendering/Decoration.cs`, `Rendering/Gradient.cs` | Core now has a framework-native `Gradient`/`LinearGradient` contract used by `BoxDecoration.Lerp`, while arbitrary Avalonia brushes and `BoxShadows` still lack Flutter-shaped value hierarchies. | Linear-gradient changes interpolate; arbitrary brush or shadow changes still snap at 50%. `CircleAvatar` is unaffected because it animates color, images, and constraints. | Add radial/sweep gradient and framework-native shadow lerp contracts, then replace the remaining arbitrary-brush/shadow midpoint selection. |
| `src/Plumix/RenderParagraph.cs`, `Widgets/DefaultTextStyle.cs` | Avalonia `TextLayout` does not expose Flutter's first-ascent/last-descent height trimming controls. `TextHeightBehavior` is propagated through `DefaultTextStyle` and retained by `RenderParagraph`, but both flags currently use Avalonia's native line-height behavior. | Style, alignment, wrapping, overflow, max-lines, width-basis inheritance, and animation match; disabling height on the first ascent or last descent can leave a small extra leading difference. | Add a paragraph backend that exposes first/last line metric trimming, then apply both `TextHeightBehavior` flags during measurement and paint. |
| `src/Plumix/Rendering/PhysicalModel.cs`, `Object.PaintingContext.cs` | Avalonia's public `DrawingContext` exposes neither Flutter's isolated save-layer clip nor `drawShadow` for arbitrary paths. `PhysicalModel`/`PhysicalShape` preserve source clip API and geometry; non-`none` geometry clips use backend sampling, while arbitrary-path elevation uses deterministic blurred-outline composition. | Layout, fill, rectangular/circular/custom-path clipping, animation, hit testing, and ordinary shadows match. `antiAliasWithSaveLayer` child blending and complex-path shadow falloff can differ at the edge. | Add backend clip-quality/save-layer and path-shadow primitives, then map every `Clip` value and Flutter physical shadow directly. |
| `src/Plumix/UI/Path.cs`, `Rendering/CustomClip.cs`, `Rendering/Layer.cs` | Avalonia exposes hard-edge and anti-aliased geometry sampling but no public isolated save-layer operation. The framework path surface currently covers rect/rounded-rect/oval plus line/quadratic/cubic contours; curve containment and paint are flattened deterministically. | `ClipOval` and ordinary `ClipPath` layout, paint, hit testing, semantics, and reclip behavior match. `antiAliasWithSaveLayer` uses ordinary anti-aliasing, and points extremely close to complex curved boundaries can differ slightly. | Add an isolated backend save-layer primitive plus an exact backend-neutral path command/containment engine, then map `antiAliasWithSaveLayer` and unflattened curves directly. |
| `src/Plumix/UI/Path.cs` (`Combine`) | The framework path model stores flattened contours and has no boolean path solver, so `Path.Combine` keeps its two operands and defers the operation to Avalonia's `CombinedGeometry` when painting, resolving `Contains`/`GetBounds` set-wise. | Painting, containment and bounds match for the difference/intersect/union/xor operations; a combined path cannot be extended with further contours. | Add a contour-level boolean path solver and flatten the result. |
| `src/Plumix.Material/InkFeatures.cs` (`InkSparkle`) | Avalonia's public drawing backend does not expose Flutter's bundled `FragmentProgram`/SkSL runtime shader path. | The 617ms radius, center, splash/sparkle alpha sequences, clipping, color, and deterministic turbulence seed match; the organic noise field is approximated with layered circles and deterministic white sparkles, so pixels differ from Android 12's shader. | Add a backend fragment-shader abstraction and port Flutter's `ink_sparkle.frag` uniforms directly. |
| `src/Plumix.Material/SnackBar.cs`, `Scaffold.cs` | Core now exposes `Animation<T>` and Flutter-shaped `Dismissible`, but snackbar still accepts `AnimationController` and has not adopted the shared dismissible composition; live `ScaffoldGeometry` is also unavailable. Swipe therefore removes after the threshold without drag-follow translation, and floating placement cannot continuously avoid a moving FAB/bottom surface. | Default fixed/floating bars, actions, queue lifecycle, overflow, and semantics match; custom animation implementations are unavailable and swipe/FAB relocation motion is simpler. | Migrate the snackbar animation contract, compose swipe behavior through core `Dismissible`, add `ScaffoldGeometry` listenables, and adopt the exact Flutter transition and avoidance choreography. |
| `src/Plumix/Widgets/Navigation.cs` | `ModalRoute` now installs the source `[barrier, scope]` `OverlayEntry` pair, but core has no `InheritedModel` aspect surface wired for `_ModalScopeStatus`, no `RestorationScope`, no `FocusScopeNode.traversalEdgeBehavior`/`setFirstFocus`, and no `TransitionDelegate`/`_RouteEntry` lifecycle. `RouteScope` is therefore a plain `InheritedWidget` carrying `route`/`isCurrent`/`canPop`/`impliesAppBarDismissal`/`opaque`, the scope has no `RestorationScope` wrapper or traversal-edge policy, and the navigator keeps its `_history`/`_exitingRoutes` pair instead of the staged lifecycle. | Entry pair and order, `opaque` on the bottom-most entry, `maintainState` build-skipping, `offstage`, `filter`, barrier composition, semantics sort keys, entry rearrangement and deferred disposal match. Aspect-scoped dependents rebuild whenever any of the five fields changes rather than only their own aspect, route state is not restored across process restarts, focus is not handed to a pushed route's scope, and declarative `pages`/`TransitionDelegate` routing is unavailable. | Add `InheritedModel` aspects for the scope status, restoration buckets, `FocusScopeNode.setFirstFocus`/traversal-edge behavior, and the `_RouteEntry`/`TransitionDelegate` lifecycle, then port `_ModalScopeState.build` and `_flushHistoryUpdates` verbatim. |
| `src/Plumix/Widgets/ModalBarrier.cs`, `src/Plumix/UI/SystemSound.cs`, `HapticFeedback.cs` | System-sound and haptic requests dispatch through framework host hooks, but platform hosts do not yet invoke native sound/vibration APIs; the shared semantics model also has no text-direction metadata field. | Non-dismissible barriers issue the Flutter-shaped alert request and Cupertino scrollbars request medium-impact feedback with source timing, but default host adapters remain silent. Barrier labels/actions and scrollbar interaction behavior match; bidi announcement direction is left to the host. | Implement native click/alert/haptic output in each platform host override and add text direction to the shared semantics configuration/node bridge. |
| `src/Plumix/Widgets/Scroll.cs`, `Rendering/Sliver.cs`, `Rendering/SliverFill.cs`, `src/Plumix.Material/SliverAppBar.cs` | The viewport now preserves out-of-range offsets and hands the leading overscroll to the first sliver as a negative overlap, but the scroll pipeline still does not expose Flutter's persistent-header snap configuration, show-on-screen protocol, or a render-layout builder callback, and trailing overscroll is not yet surfaced to slivers. App-bar `snap` and `stretch` retain their documented limits. | Fill viewport/remaining layout, shared user-scroll direction/end notifications, resizing/floating headers, collapsing, pinned/floating app-bar reveal, strict flexible-space layout/paint, regular/medium/large variants, overlapping pinned layout, and scroll-under styling match. Leading overscroll now reaches overscroll-aware slivers from a standard `CustomScrollView`; app-bar snap still differs after partial release and stretch cannot grow the header. | Surface trailing overscroll in sliver constraints; add persistent-header snap/show-on-screen and render-layout callback contracts; then wire fill stretch plus Flutter's app-bar stretch/snap controllers into the shared pipeline. |
| `src/Plumix/Widgets/TextInput.cs`, `src/Plumix.Material/TextField.cs`, `TextFormField.cs` | Core text editing owns retained editable geometry, pointer/handle selection, adaptive magnifiers, and explicit/default spell-check contracts, but not input formatter/autofill/restoration services or scroll-controller integration. | Keyboard/IME/pointer/handle editing, focus, context menus, misspelling results/actions, read-only/obscured/multiline input, grapheme max length, submission, form validation, and semantics match. Formatters, autofill hints and restoration IDs are ignored, and a long single-line value cannot be scrolled independently of the field. | Add formatter/autofill/restoration and scroll-controller services, then wire them through `EditableText`/`TextField`. |
| `src/Plumix.Material/InputBorder.cs` | `ShapeBorder.Paint` cannot be overridden with the extra gap parameters Flutter adds in `InputBorder.paint`, because C# forbids widening an override's parameter list. The inherited three-argument `Paint` is therefore sealed and forwards to a gap-aware overload whose `gapStart` has no default value. | Painting, gap geometry and every `ShapeBorder` contract match; only a positional `Paint(context, rect, null)` call is ambiguous and has to name its argument. | Nothing to close: the shape follows from the language. |
| `src/Plumix.Material/InputDecorator.cs`, `DataTable.cs` | Flutter's `Semantics` wrappers merge upward as fragments, and `InputDecorator.childConfigurationsDelegate` sees the tagging configuration itself. Plumix's compiler hands delegates compiled child nodes and loses an inner data-table header role while bucketing cells. Input affixes therefore form explicit containers, while `DataTable` places the equivalent column-header boundary outside its `InkWell`. | Input sort keys, tags, grouping and traversal match, but an affix stops merging at its node. Data-table roles, labels and actions match, while a sortable header carries its action in a nested node instead of Flutter's single merged header node. | Port Flutter's fragment-based merge-up groups so delegates receive descendant configurations and table bucketing retains an inner role; then restore both source wrapper positions. |
| `src/Plumix.Material/Dialog.cs` | Cupertino alert-dialog primitives, display-feature anchoring, and closed-loop focus traversal are not yet available. Material `Dialog`/`AlertDialog` and root/nearest navigator selection match, while inset/alignment APIs use physical `Thickness`/concrete `Alignment`. `DialogRoute` also keeps its own entry `AnimationController` (the route transition stays zero-length), so `BuildModalBarrier` drives the scrim from that controller rather than `ModalRoute.Animation`. | `AlertDialog.adaptive`/`showAdaptiveDialog`, foldable `anchorPoint`, and explicit traversal-edge policies are unavailable; ordinary Material dialogs are unaffected. | Port Cupertino dialog/action controls, display-feature sub-screens, directional geometry, and traversal-edge policies, then add the adaptive/advanced route APIs. |
| `src/Plumix.Material/About.cs` | Cupertino dialog actions, `DraggableScrollableSheet`, and the private responsive master-detail license shell are not available. The adaptive constructor uses Material actions, and `LicensePage` uses Flutter's narrow package-list/detail route flow at every width. | Explicit or nearest-`Title` metadata and ordinary dialog/license flows match; Apple adaptive actions and the >=840px side-by-side license browser differ. | Add Cupertino dialog actions, draggable sheets, and the responsive master-detail shell, then switch the adaptive/wide branches to the Dart composition. |
| `src/Plumix/Rendering/Semantics.cs`, `src/Plumix/Rendering/Object.RenderObjectSemantics.cs`, `src/Plumix/Rendering/Table.cs` | `SemanticsNode` has no per-node `transform`: the compiler resolves every node's `Rect` into root coordinates instead of Flutter's local-rect-plus-parent-transform pair, and there is no `showOnScreen` node callback (core has no `RenderObject.showOnScreen` protocol at all). `RenderTable.AssembleSemanticsNode` therefore positions synthesized row/cell nodes with absolute rects and computes child bucketing from the child rect relative to the table node's origin, rather than shifting child transforms; synthesized rows carry no scroll-into-view callback. | Row/cell node structure, roles, `indexInParent`, row and cell geometry, zero-height row and zero-width cell skipping, id-to-index bucketing and node reuse match, and node rects are equivalent to Flutter's composed transform for translation-only ancestors. A scaled or rotated ancestor makes the bucketing fall back the way Flutter's translation-only `getAsTranslation` does, and assistive technologies cannot ask a table row to scroll itself into view. | Add a per-node transform with local rects to the semantics compiler and the `RenderObject.showOnScreen` protocol (shared with the scroll pipeline's show-on-screen work), then pass `showOnScreen` to the synthesized rows and shift child transforms directly. |
| `src/Plumix/Rendering/TableBorder.cs` | Avalonia exposes no isolated `drawDRRect` or per-corner rounded-ring primitive, and `BorderSide` has no `strokeAlign`. Interior divisions are drawn as one stroked line per division instead of a single stroked path, and both radius branches build the ring from a combined geometry deflated by a single uniform inset. | Uniform and non-uniform border sides, interior division placement, colors, widths, `BorderStyle.None` suppression, paint order, `Scale`/`Lerp` and `Dimensions` match. A non-uniform border radius on a table whose visible outer sides differ in width can round its inner edge slightly differently, and hairline (`width == 0`) outer sides paint as a stroked edge rather than a filled quad. | Add a backend `drawDRRect`/per-corner ring primitive and a `strokeAlign`-carrying `BorderSide`, then paint the outer border from Flutter's inset/outset rounded rects. |
| `src/Plumix/Rendering/Table.cs` | Dart's `TableColumnWidth` hierarchy declares no `==`, so Flutter compares column widths by identity and relies on `const` canonicalization; the C# port models them as records with value equality. `IntrinsicColumnWidth.flex` is also exposed as `FlexFactor` because C# forbids a property named `Flex` alongside the inherited `Flex(cells)` method. | Every width computation, flex distribution and relayout trigger matches for `const`-equivalent values. A freshly constructed but structurally equal `TableColumnWidth` does not dirty layout where Flutter's would, and the constructor parameter stays `flex` while direct property access uses the language-shaped name. | No runtime close condition; generated Dart-to-C# interop metadata could expose the source name, and dropping value equality would only add redundant relayouts. |
| `src/Plumix/Widgets/PageView.cs`, `Rendering/PageView.cs`, `src/Plumix.Material/CalendarDatePicker.cs` | Core now has shared `PageStorage`, scroll-offset restoration and a `PageView` that reports Flutter-shaped `ScrollUpdateNotification`/`ScrollEndNotification`, but not Flutter's sliver-backed lazy page viewport pipeline, so the page view is gesture-driven rather than `ScrollPosition`-backed. The page viewport keeps all supplied children mounted, and the calendar rotates a bounded previous/current/next page window instead of using `PageView.builder`. | Tab drag/settle synchronization and ordinary one-month calendar swipes/navigation match. Very large supplied page sets use more memory, tabs keep offscreen state alive, a high-velocity calendar fling cannot skip several months in one settle, and page scroll notifications carry no drag details or physics-driven overscroll. | Add a lazy page-sliver adapter and route `PageView` and `CalendarDatePicker` through it with the available PageStorage restoration. |
