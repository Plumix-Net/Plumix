# Test Matrix

Purpose: map framework areas to existing test coverage and identify common gaps quickly.

## Coverage Map

| Area | Primary tests | What is covered | Typical gap to watch |
| --- | --- | --- | --- |
| Element lifecycle and reconciliation | `src/Flutter.Tests/ElementLifecycleTests.cs` | Keyed reorder, global key reparent/activation/disposal behavior, mixed keyed/unkeyed sibling reorder, and nested multi-parent mixed keyed/unkeyed reorder (keyed retention + moved unkeyed disposal). | Deep stress cases with rapid multi-frame updates that combine nested mixed keyed/unkeyed groups and `forgottenChildren` paths. |
| Inherited dependencies | `src/Flutter.Tests/InheritedWidgetTests.cs`, `src/Flutter.Tests/InheritedModelTests.cs`, `src/Flutter.Tests/InheritedNotifierTests.cs` | Dependent registration and selective rebuild semantics. | Edge cases around deep tree shadowing plus dynamic notifier swaps. |
| Frame and scheduler flow | `src/Flutter.Tests/FramePipelineTests.cs` | Begin/draw/post-frame order, persistent callbacks, build scheduling in frame. | Long-running callback interactions with host-level visibility transitions. |
| Render object parity | `src/Flutter.Tests/RenderingParityTests.cs` | Constraint normalization, relayout boundary behavior, root relayout triggers, failed-layout exception propagation with dirty-state preservation, `BoxConstraints.Tighten` clamp behavior for out-of-range requests, no-op invalidation guards in proxy/flex property setters, and overflow debug paint regression coverage for `RenderFlex` (overflow adds extra indicator paint commands versus non-overflow layout). | Rare constraint edge cases across deep mixed proxy/flex chains. |
| Compositing and layers | `src/Flutter.Tests/CompositingLayerTests.cs`, `src/Flutter.Tests/LayerV2Tests.cs` | Repaint boundary behavior, layer reuse/property updates, layer tree structure, no-op `RenderColoredBox` color updates, no-op `ReplaceRootLayer` handling, combined repaint + composited-layer update correctness in a single frame, and resize-driven clip-layer bounds refresh for implicit `RenderClipRRect` clipping. | Layer churn under repeated boundary toggles in large trees. |
| Basic proxy/layout/decoration widgets | `src/Flutter.Tests/BasicWidgetProxyTests.cs`, `src/Flutter.Tests/AlignTests.cs`, `src/Flutter.Tests/AspectRatioTests.cs`, `src/Flutter.Tests/FractionallySizedBoxTests.cs`, `src/Flutter.Tests/FittedBoxTests.cs`, `src/Flutter.Tests/UnconstrainedLimitedBoxTests.cs`, `src/Flutter.Tests/OverflowBoxTests.cs`, `src/Flutter.Tests/OffstageTests.cs`, `src/Flutter.Tests/StackTests.cs`, `src/Flutter.Tests/DecoratedBoxTests.cs`, `src/Flutter.Tests/ContainerTests.cs` | Widget-to-render wiring and rebuild update behavior for `Opacity`, `Transform`, `ClipRect`, `Align/Center`, `AspectRatio`, `Spacer`, `FractionallySizedBox`, `FittedBox`, `UnconstrainedBox/LimitedBox`, `OverflowBox/SizedOverflowBox`, `Offstage`, `Stack/Positioned`, `DecoratedBox`, and `Container` composition wrappers (`alignment`, `padding`, `margin`, `constraints`, `transform`), including alignment offsets, shrink-wrap factor behavior, ratio layout resolution on bounded axes, `Spacer` flex parent-data propagation, two-spacer proportional flex distribution in `RenderFlex`, fractional tight-constraint propagation with pass-through axes, `BoxFit`-driven fitted sizing/transform hit testing, axis-specific unconstraining with unbounded-axis `LimitedBox` max-clamp behavior, optional overflow constraint overrides with `OverflowBoxFit` sizing modes, fixed-size overflow alignment, offstage collapse semantics (layout without paint/hit-test and smallest parent size), positioned insets resolution, decoration data propagation (`border/radius/fill`), width/height tightening vs. explicit constraints, and wrapper ordering. | Richer nested layout stress scenarios with deep overlay stacks and mixed unconstrained parents. |
| MediaQuery and SafeArea | `src/Flutter.Tests/SafeAreaTests.cs` | `MediaQueryData` insets transformation parity (`removePadding` + view-padding adjustment), `SafeArea` edge selection/minimum semantics, `maintainBottomViewPadding` behavior when bottom padding is keyboard-consumed, and root-level ambient `MediaQuery` wiring from `WidgetHost`. | Full `MediaQueryData` parity surface (all Flutter fields/aspect-specific rebuild optimization) and non-bottom occlusion/view-inset edge cases remain to be expanded. |
| Text widget and paragraph layout parity | `src/Flutter.Tests/TextWidgetTests.cs` | `Text` widget -> `RenderParagraph` wiring for layout-affecting options (`textAlign`, `softWrap`, `maxLines`, `overflow`, `textDirection`, `fontWeight`, `fontStyle`, `height`, `letterSpacing`), inherited `DefaultTextStyle` resolution/override behavior, unbounded-layout regression coverage to ensure paragraph width is not artificially capped, and core `Icon` widget coverage (`IconTheme` defaults/overrides, null-icon square layout, RTL `matchTextDirection` mirroring, and Material `arrow_back` constant mapping with `matchTextDirection=true`). | Runtime visual parity across hosts/fonts (line metrics and glyph shaping) still needs screenshot/manual validation. |
| Material theming, shell, and button/checkbox/switch/radio/floating-action-button/bottom-navigation-bar baseline | `src/Flutter.Tests/TextWidgetTests.cs`, `src/Flutter.Tests/MaterialScaffoldTests.cs`, `src/Flutter.Tests/MaterialButtonsTests.cs`, `src/Flutter.Tests/MaterialFloatingActionButtonTests.cs`, `src/Flutter.Tests/MaterialCheckboxTests.cs`, `src/Flutter.Tests/MaterialSwitchTests.cs`, `src/Flutter.Tests/MaterialRadioTests.cs`, `src/Flutter.Tests/MaterialBottomNavigationBarTests.cs` | `Theme(data: ThemeData)` inherited lookup and `ThemeData.TextTheme.BodyMedium` propagation through `DefaultTextStyle` into framework `Text`/`RenderParagraph` defaults, expanded Material `BottomNavigationBar` coverage (item-count/index guards, default color resolution from theme canvas/primary/on-surface-variant tokens, active-icon rendering, tap callback index dispatch, theme-data precedence through `ThemeData.BottomNavigationBarTheme`, widget-over-theme override precedence, auto/type-driven fixed-vs-shifting defaults, shifting background-color resolution from selected item, label-style color precedence over item colors, icon-theme pair guard validation, tile semantics wrappers for button/enabled/selected/index-label behavior with index labels resolved through `MaterialLocalizations.TabLabel(...)` (default English fallback plus local override support), tooltip hover show/hide behavior, and shifting selection-width animation behavior), `Scaffold` background resolution, app-bar background/foreground color precedence (`widget -> appBarTheme -> mode-aware defaults`) with mode + brightness-aware coverage (`UseMaterial3=true` -> `CanvasColor`/`OnSurfaceColor`; `UseMaterial3=false` + `Brightness.Light` -> `PrimaryColor`/`OnPrimaryColor`; `UseMaterial3=false` + `Brightness.Dark` -> `CanvasColor`/`OnSurfaceColor`), app-bar icon-theme precedence for leading/actions slots (`iconTheme`: `widget -> appBarTheme -> mode-aware fallback`, where M3 default is foreground + `size: 24` and M2 default keeps foreground fallback without forcing size; `actionsIconTheme`: `widget -> appBarTheme -> iconTheme -> mode-aware fallback`, where M3 defaults to `OnSurfaceVariantColor` and M2 defaults to foreground path, including actions fallback to widget/app-bar `iconTheme` and null-color fallback behavior), app-bar leading-width precedence (`widget -> appBarTheme -> default`) with non-positive themed-width guard, plus leading-slot tight geometry by resolved leading width and toolbar height, app-bar toolbar-height precedence (`widget -> appBarTheme -> framework default 56`) with non-positive themed-height guard and mode-invariant default coverage (`UseMaterial3=true` -> `56`, `UseMaterial3=false` -> `56`), app-bar actions-padding precedence (`widget -> appBarTheme -> default zero`), app-bar toolbar geometry parity (`AppBar` outer toolbar padding defaults to `0`, actions row applies no extra hardcoded inter-item spacing, actions row uses `mainAxisSize: min`, and actions row cross-axis alignment follows `ThemeData.UseMaterial3` mode: `center` for M3 and `stretch` for M2), app-bar `primary` status-bar inset parity (`primary=true` applies top `MediaQuery.padding` inset when ambient `MediaQuery` is available via `SafeArea(bottom: false)`, `primary=false` opts out), app-bar system-bar overlay parity (`systemOverlayStyle` precedence `widget -> theme appBarTheme -> defaults`, and effective style push into framework `SystemChrome` for host propagation), app-bar title-layout parity coverage (`centerTitle` precedence across widget/theme/platform, macOS action-count platform fallback, `titleSpacing` horizontal insets, negative spacing guard, widget-vs-theme precedence for `titleSpacing`, default `titleText` single-line ellipsis behavior (`softWrap: false`, `maxLines: 1`, `overflow: ellipsis`), empty-string `titleText` rendering as text instead of being treated as missing title, implied-leading parity on non-root navigator routes via `automaticallyImplyLeading` default + explicit opt-out (including fullscreen-dialog close-icon fallback via `PageRoute.FullscreenDialog`), drawer-aware implied-leading menu fallback (`Scaffold.drawer` -> `IconButton(Icons.Menu)`), and end-drawer implied action fallback (`Scaffold.endDrawer` with no explicit actions -> trailing `IconButton(Icons.Menu)` with `automaticallyImplyActions` opt-out), scaffold drawer-state parity coverage (`ScaffoldState.openDrawer/closeDrawer` + `ScaffoldState.openEndDrawer/closeEndDrawer`, start/end mutual exclusion, default drawer width `304`, start/end edge-drag open flows, animated settle of open/close transitions, timestamp-derived px/s drag-release settle open/close behavior, drag-cancel settle by half-progress threshold, root-route drawer close on `Navigator.MaybePop`, and local-history-driven back handling), dedicated drawer-theme parity coverage (`ThemeData.DrawerTheme` + inherited `DrawerTheme` for background/elevation/shadow/width/scrim precedence plus invalid themed width/elevation guards), app-bar text-style parity coverage (`titleTextStyle`/`toolbarTextStyle` resolution precedence widget -> theme -> defaults, title fallback through `TextTheme.TitleLarge`, and inherited text-style application for title/actions, including mixed actions-subtree context where toolbar text style and actions icon theme are inherited together), Material button defaults/disabled visuals (`TextButton`, `ElevatedButton`, `OutlinedButton`, `FilledButton`, `FilledButton.tonal`, and `IconButton`) including filled/tonal token mapping and disabled-state tone checks, default elevated shadow coverage (enabled shadow presence and disabled shadow suppression), elevated style-from stateful elevation coverage (`disabled=0`, `pressed=base+6`, `hovered/focused=base+2`), non-elevated style-from shadow/elevation override coverage (`TextButton`/`OutlinedButton`/`FilledButton`), including theme-shadow fallback when elevation is set without explicit shadow color, pointer pressed-state visual transition coverage for `ElevatedButton`, hover-state enter/exit overlay coverage for `TextButton`, pointer-click focus-overlay reset coverage for `TextButton`, combined-state overlay priority coverage for Material buttons (`pressed+hovered`, `pressed+focused`, and `focused+hovered` conflict cases), keyboard-activation coverage (`Space` uses pressed overlay and invokes callback on key-down only, `NumPadEnter` activates, modified activation chords are ignored), button focus-control parity coverage (`focusNode`-driven focused overlay updates, `autofocus` mount behavior, and `autofocus` `false -> true` update behavior), ink-splash activation coverage for pointer down (`RenderInkSplash` presence/state), tight-width splash bounds coverage (`RenderInkSplash` width matches button width), padded tap-target hit-test redirection coverage for `TextButton` (`_InputPadding`-style center redirection), rounded-splash clipping coverage through `ClipRRect` integration, baseline Material min-size coverage (`TextButton` default `64x40` and `IconButton` mode-aware defaults `40x40`/`48x48`), style-driven constraint coverage for `minimumSize`/`maximumSize`/`fixedSize` (including finite-axis tightening, infinite-axis passthrough, and `minimumSize = 0` acceptance with negative-value rejection), style-driven label alignment coverage via `ButtonStyle.Alignment` (default/theme/widget precedence), state-aware text-style coverage via `ButtonStyle.TextStyle` resolver behavior (including widget resolver-null fallback to theme in disabled state), button icon-theme propagation coverage (`IconTheme` default color/size inheritance from composed button style and `styleFrom(...)` icon color/size + disabled icon-color overrides, plus `IconButtonTheme` precedence over ambient `IconTheme` with widget-style override precedence), tap-target-size parity coverage (`ThemeData.MaterialTapTargetSize` default `Padded`, theme-level `ShrinkWrap`, and `styleFrom(tapTargetSize: ...)` precedence over theme), surface-tint parity coverage (`ButtonStyle.SurfaceTintColor` in style composition, `styleFrom(surfaceTintColor: ...)`, elevation-based tinting on elevated backgrounds including theme-level style overrides, and `UseMaterial3` gating behavior where tint is disabled for M2 mode), floating-action-button baseline coverage (`regular`/`small`/`large`/`extended` constraints, M3 default foreground/background tokens, extended directional padding, theme-default and widget override precedence, hover/pressed/disabled elevation-state mapping, tooltip hover show/hide behavior, hover-cursor default/theme precedence via `MouseCursorManager`, and keyboard tap-feedback dispatch/suppression via `enableFeedback` widget/theme resolution), text/outlined/filled `styleFrom` disabled-color/background mapping parity coverage (text background/icon and outlined background use all-state mapping when disabled override is omitted, outlined background-only style also overrides themed disabled background, and filled background-only style preserves default disabled fallback when disabled override is omitted), mode-aware checkbox defaults/interaction coverage (`value` + `tristate` cycle, selected/unselected/disabled fill+border defaults, check/dash indicator rendering, keyboard toggle activation, and tap-target policy mapping to `ThemeData.MaterialTapTargetSize`), checkbox-theme precedence coverage (`ThemeData.CheckboxTheme` + local `CheckboxTheme` for fill/check/overlay/side/shape/tap-target/splashRadius with widget override precedence), checkbox semantics coverage (`semanticLabel` propagation plus checked/enabled/tap semantics), error-state token coverage (M3 error fill/border/check colors), adaptive-constructor guard coverage, splash-radius propagation into `RenderInkSplash`, transition animation coverage for value changes (check/dash crossfade), and adaptive Cupertino-path coverage for checkbox (`Checkbox.Adaptive` iOS defaults, ignored `fillColor`/`materialTapTargetSize`, macOS shrink-wrap hit-target behavior, and `14x14` adaptive visual geometry), Material switch baseline coverage (`value` on/off rendering, M3 selected/unselected/disabled track+thumb defaults, widget/theme precedence for thumb+track+outline, thumb-icon rendering, keyboard toggle activation, and tap-target policy mapping to `ThemeData.MaterialTapTargetSize`), switch semantics coverage (`semanticLabel` propagation plus enabled/unchecked/tap semantics), adaptive switch coverage on iOS/macOS (`Switch.Adaptive` `activeColor` track mapping, cupertino-like `51x31` track + `28` thumb geometry, disabled opacity `0.5`, and drag-threshold choreography (`commit=0.7`, `reverse=0.2`) with pointer-driven commit/reverse tests), Material radio baseline coverage (`groupValue` selection wiring, selected/unselected/disabled fill/border defaults, toggleable selected->null transition, keyboard activation, and tap-target policy mapping to `ThemeData.MaterialTapTargetSize`), radio-theme precedence coverage (`ThemeData.RadioTheme` + local `RadioTheme` for fill/overlay/background/side/tap-target/splash/inner-radius with widget override precedence), adaptive radio coverage on iOS/macOS (`Radio.Adaptive` Cupertino path defaults, ignored `fillColor` in adaptive mode, `useCupertinoCheckmarkStyle` indicator branch, and `18x18` Cupertino visual geometry), theme-token default resolution checks for `primary`/`primaryContainer`/`onPrimaryContainer`/`onSurface`/`surfaceContainerLow`/`surfaceContainerHighest`/`inverseSurface`/`onInverseSurface`/`outline`/`shadow` plus filled-button tokens (`secondaryContainer`/`onSecondaryContainer`) and scaffold/canvas M3 light surface defaults, default button typography coverage for `labelLarge` metrics (`14`, medium, `height: 1.43`, `letterSpacing: 0.1`), default M3 padding coverage for non-text buttons (`horizontal: 24`, `vertical: 0`), theme-level button-style override coverage from `ThemeData` (`textButtonStyle`/`elevatedButtonStyle`/`outlinedButtonStyle`/`filledButtonStyle`/`iconButtonStyle`) including precedence order `default -> theme -> widget -> legacy`, local inherited button-theme wrapper coverage (`TextButtonTheme`/`ElevatedButtonTheme`/`OutlinedButtonTheme`/`FilledButtonTheme`/`IconButtonTheme`) for subtree override precedence and local null-style clearing semantics, `ThemeData` button-theme data coverage (`TextButtonTheme`/`ElevatedButtonTheme`/`OutlinedButtonTheme`/`FilledButtonTheme`/`IconButtonTheme` properties) including precedence over legacy `*ButtonStyle` when both are set, `ButtonStyle.Merge(...)` semantics coverage (fills null fields without overriding existing values), initial `ButtonStyle` state-property override coverage (foreground/min-size/side), text-style color precedence coverage (`textStyle.color` does not override resolved foreground token), and expanded `StyleFrom(...)` coverage (disabled colors, text-style propagation, legacy parameter precedence, `foregroundColor`-derived overlay/splash, explicit `overlayColor` state-opacities+splash fallback, activation-captured ripple tint, interactive-state-only overlay application, transparent overlay highlight suppression, disabled-state fallback to default tokens when style resolver returns null, disabled-only override preservation of enabled-state defaults, resolver-null fallback to lower-priority/default style per state including `ElevatedButton` foreground/background and `OutlinedButton` foreground/side, direct `ButtonStyle` overlay-to-splash fallback across text/elevated/outlined buttons when `SplashColor` is omitted, and style-state parity checks mirrored for `ElevatedButton`/`OutlinedButton`, plus `IconButton` selected-icon and outlined selected-border behavior). | Advanced app-bar variants/scroll-under behavior (sliver app bars, dynamic elevation, adaptive title/action spacing beyond current `titleSpacing`/text-style scope), full Flutter `ButtonStyle` parity matrix, deeper Cupertino fidelity items (haptics/on-off labels/image thumbs), deeper Cupertino radio fidelity items (haptics/accessibility labels/advanced motion nuances), and deeper drawer-controller choreography parity beyond current `Scaffold` composition. |
| Material mode-aware button defaults (M2/M3 split) | `src/Flutter.Tests/MaterialButtonsTests.cs` | Mode-aware defaults now have explicit coverage for: `OutlinedButton` border-side split (M3 focused primary accent + outline default vs M2 focused/unfocused `onSurface(0.12)`), M2 geometry defaults for `TextButton`/`ElevatedButton`/`OutlinedButton` (`minimumSize` height `36`, M2 padding tokens, rounded clip-shape radius `4`), M2 color/elevation defaults for `ElevatedButton` (enabled `primary/onPrimary` pair plus elevation states `2/4/8`) with baseline M2 foreground-default checks for `TextButton`/`OutlinedButton`, M2 pressed/focused overlay-opacity defaults (`0.12`) for text/elevated/outlined buttons (including elevated `onPrimary` overlay blend path), filled-button default parity updates (`FilledButton`/`FilledButton.tonal` M2 horizontal padding `16` plus filled hovered elevation `1`), icon-factory padding defaults across M3/M2 for `TextButton.Icon`, `ElevatedButton.Icon`, `OutlinedButton.Icon`, `FilledButton.Icon`, and `FilledButton.TonalIcon`, icon-alignment behavior/preference (`IconAlignment.End`, style-level `iconAlignment`, explicit icon-factory override precedence, theme-level icon-alignment precedence, and RTL directionality-aware start/end icon order via `Directionality`), icon-factory text-scale spacing interpolation/parity coverage (`spacing: 8 -> 4` for effective scale `1 -> 2`, including interpolation at `1.5`, clamp at `>=2`, and style text-size impact via `buttonStyle.textStyle.fontSize` with ambient `MediaQuery.TextScaleFactor`), default/icon padding text-scale interpolation parity coverage across button variants (`scaledPadding` `1x -> 2x -> 3x` behavior, including directional start/end mapping under RTL where Flutter uses `EdgeInsetsDirectional`), `styleFrom(iconColor: ...)` disabled-state mapping for `ElevatedButton`/`OutlinedButton`/`FilledButton` when `disabledIconColor` is omitted, and mode-aware shadow fallback split for `TextButton`/`OutlinedButton` style-level elevation (`UseMaterial3=true` no shadow fallback due transparent defaults, `UseMaterial3=false` theme-shadow fallback). | Remaining gaps are deeper token-level parity and broader state/token combinations not yet covered by current default-focused tests; button parity for current framework scope is otherwise closed. |
| Semantics tree | `src/Flutter.Tests/SemanticsTreeTests.cs` | Actions, merge/split, transform/clip, dirty propagation and id reuse contracts. | End-to-end assistive flows with platform accessibility tooling. |
| Host semantics bridge | `src/Flutter.Tests/FlutterHostSemanticsTests.cs` | `FlutterHost` semantics runtime surface (`SemanticsRoot`, `SemanticsUpdated`, `PerformSemanticsAction`) and host-level action routing for interactive nodes. | Platform adapter wiring to native accessibility APIs on desktop/web/mobile runtimes. |
| Gestures and hit testing | `src/Flutter.Tests/GesturePipelineTests.cs` | Transform/clip hit testing, recognizer dispatch, arena conflict outcomes, pointer signals, drag primary-delta dispatch, drag-end primary velocity reported in px/s from pointer timestamps, and hover enter/exit transition dispatch (`PointerEnterEvent`/`PointerExitEvent`) from `GestureBinding` hover-hit tracking. | Multi-pointer gesture interactions and cancellation races beyond current cancel-callback plumbing. |
| Navigation | `src/Flutter.Tests/NavigationTests.cs`, `src/Flutter.Tests/HeroNavigatorTests.cs` | Push/pop/replace/remove APIs, named routes, observers, route data semantics, local-history-aware back handling (`MaybePop` consumed when `Route.WillPop` handles local entries on root routes), and shared-tag hero transition choreography on navigator push/pop (temporary dual-route composition, hero-flight overlay animation, destination-priority `Hero.createRectTween` bounds interpolation, destination-priority `Hero.flightShuttleBuilder` overlay composition with source fallback, `Hero.placeholderBuilder` size-aware placeholder composition for hidden heroes, Flutter-like default placeholder semantics (`push source -> offstage child`, `push destination/pop -> empty fixed-size box`), duplicate-tag guard for same-route hero registrations, nested-hero guard (`Hero` under `Hero` throws), push-flight interruption by pop diverts the active hero flight (reuses and reverses existing tween/overlay), popped-route disposal after flight completion, `HeroMode(enabled: false)` subtree opt-out from hero-flight participation, user-gesture transition gating via `Hero.transitionOnUserGestures`, and outer-route hero flights across nested navigators when matching heroes live on nested current routes). | Host back integration across nested navigators remains to be expanded. |
| Focus and keyboard flow | `src/Flutter.Tests/FocusTests.cs`, `src/Flutter.Tests/FlutterHostInputTests.cs` | Focus ownership transitions, primary focus bookkeeping, autofocus, key callback dispatch, host keyboard dispatch (`FlutterHost` key-down/key-up forwarding to `FocusManager`), tab traversal order/shift reversal, scope-bounded traversal boundaries, directional traversal fallback, geometry-aware directional candidate selection via traversal bounds, and transform-aware traversal-rect resolution through render-object transform chains. | Rotated/non-invertible transform edge cases and deeper scope hierarchy traversal stress cases. |
| Editable text input | `src/Flutter.Tests/TextInputTests.cs` | Focused text input delivery via host -> focus manager dispatch, controller text mutation, selection-aware insert/delete behavior, controller composing lifecycle (`SetComposing`/`CommitComposing`/`ClearComposing`), focus-manager composition dispatch (`update`/`commit`), host preedit bridge dispatch (`FlutterHost` text-input client `SetPreeditText`), host IME state sync (`SurroundingText`, selection getter/setter, cursor rectangle), multiline editing baseline (`Enter` newline insertion + `ArrowUp/ArrowDown` vertical caret movement), `TextLayout`-driven caret geometry with fallback, caret/selection navigation keys (`Left/Right/Home/End`, shift extend), word-level shortcuts (`Ctrl/Alt + ArrowLeft/ArrowRight` and `Ctrl/Alt + Backspace/Delete`), paragraph-level shortcuts in multiline mode (`Ctrl/Alt + ArrowUp/ArrowDown`), clipboard shortcuts (`Ctrl/Meta + C/X/V`), grapheme-aware caret/delete behavior for ZWJ/combining sequences, `Ctrl/Meta+A` selection, disabled-input behavior, and `onChanged` callback flow. | Advanced visual bidi navigation parity and platform text-action menu parity. |
| Scroll/slivers core | `src/Flutter.Tests/ScrollPipelineTests.cs` | Scroll position physics, viewport/sliver layout, cache extent, keep-alive reuse, sliver padding offset behavior, and `Scrollable + ListView.Separated` viewport continuity under jump-driven scroll updates. | Stress tests with very large child counts and rapid direction changes. |
| Scroll widget infrastructure | `src/Flutter.Tests/ScrollInfrastructureTests.cs` | Notifications, primary controller, keep-alive mixin, list/grid constructor semantics. | Combined nested scrollables and scrollbar interaction nuances. |
| Sample state behavior | `src/Flutter.Tests/SampleCounterStateTests.cs` | Counter model notifications and scope dependency behavior. | End-to-end sample page regressions across all demo routes. |

## How to Use for Iterative Work

1. Pick feature area row.
2. Read listed tests before opening implementation files.
3. Update or add tests in same row when behavior changes.
4. If no row fits, add one before shipping the feature.

## Porting Rule (Dart -> C#)

- For control/widget ports, tests must validate Dart-parity-critical behavior from `docs/ai/PORTING_MODE.md`:
  - defaults,
  - interaction states,
  - high-risk constraint/layout scenarios.
