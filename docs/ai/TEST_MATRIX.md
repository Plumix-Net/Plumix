# Test Matrix

Purpose: map framework areas to existing test coverage and identify common gaps quickly.

Document rules:

- Keep every cell short: 2-4 sentences max. This file answers "where are the tests and what do they roughly cover", nothing more.
- Detailed behavior lives in the tests themselves; historical detail lives in `CHANGELOG.md`. Do not append per-iteration coverage prose here.
- One row per area. If a row does not fit, add one before shipping the feature.

## Coverage Map

| Area | Primary tests | What is covered | Typical gap to watch |
| --- | --- | --- | --- |
| Element lifecycle and reconciliation | `src/Plumix.Tests/ElementLifecycleTests.cs` | Keyed/unkeyed reorder, global key reparenting, activation/disposal, nested mixed keyed groups, and exactly-once inactive-subtree finalization. | Multi-frame stress cases combining nested groups and `forgottenChildren` paths. |
| Inherited dependencies | `src/Plumix.Tests/InheritedWidgetTests.cs`, `InheritedModelTests.cs`, `InheritedNotifierTests.cs` | Dependent registration and selective rebuild semantics. | Deep tree shadowing and dynamic notifier swaps. |
| Core inherited themes and application metadata | `src/Plumix.Tests/TitleDefaultSelectionStyleTests.cs`, `MaterialAboutTests.cs`, `MaterialSelectionTests.cs` | Nearest-type captured themes, selection fallback/merge/consumer precedence, opaque title metadata updates, title-derived About names, and the themes `ShowLicensePage` captures for its pushed route. | Route/overlay owners must explicitly apply `CapturedThemes`; host-native task switchers beyond the desktop title need platform verification. |
| Core utility builders, view lookup, and lookup boundaries | `src/Plumix.Tests/StatefulBuilderLookupBoundaryTests.cs` | Local state-setter rebuild/update behavior; exact inherited reads and dependencies; platform-view lookup, identity-scoped notifications and boundary hiding; bounded widget/state/render-object lookups and ancestor/child visitors. | Deep global-key reparent stress across nested lookup boundaries. |
| Core state storage | `src/Plumix.Tests/StateStorageWidgetsTests.cs`, `ScrollInfrastructureTests.cs` | Composite/explicit page-storage identifiers, nearest-bucket lookup, route lifetime, scroll-offset restoration and opt-out, plus lazy shared values and aspect-selective rebuilds. | Lazy page-viewport integration remains a separate shared gap; scrollables still persist offsets through `PageStorage` rather than restoration buckets. |
| State restoration | `src/Plumix.Tests/RestorationBucketTests.cs`, `RestorationManagerTests.cs`, `RestorationWidgetsTests.cs`, `RestorationPropertiesTests.cs` | Bucket value/child API, claim/adopt/rename/drop and duplicate-id detection, use-after-dispose, manager root-bucket retrieval/replacement/`isReplacing`/`flushData`, scope and root-scope composition, the `RestorationState` ordering rules (claim, rename, toggle, re-restore, register/unregister/disable), and every `Restorable*` property default, encoding and notification. | The manager speaks the real `flutter/restoration` channel now, but no host answers it, so end-to-end persistence across a real process restart is unverified. |
| Platform channels | `src/Plumix.Tests/MessageCodecsTests.cs`, `PlatformChannelTests.cs` | `StandardMessageCodec` wire format (type discriminators, 1/3/5-byte sizes at their boundaries, double and typed-list alignment, integer boundary bytes, composite round-trips), the binary/string/JSON codecs, both method codecs' envelopes including native stacktraces, `MethodCall`/`PlatformException` `ToString`, messenger routing in both directions, `BasicMessageChannel`/`MethodChannel`/`OptionalMethodChannel` results, errors and handler-to-envelope mapping, `EventChannel` listen/cancel/error, the inbound navigation calls, `SystemNavigator`/`SystemSound`/`HapticFeedback` payloads, and every `SystemChannels` name and codec. | Byte expectations assume a little-endian host, as Flutter's own tests do; no host adapter answers the restoration or accessibility channels yet. |
| Core scroll controller/configuration | `src/Plumix.Tests/ScrollInfrastructureTests.cs`, `ScrollBehaviorParityTests.cs`, `ScrollPipelineTests.cs` | Primary controller lookup/none scopes, platform-and-axis inheritance, nested shielding, behavior copying, platform physics, drag-device policy, desktop scrollbar chrome, keyboard dismissal, platform/custom velocity trackers, logical-modifier mouse-wheel axis flipping, trackpad exclusion, and platform-default responses. | — |
| Nested scroll views | `src/Plumix.Tests/NestedScrollViewTests.cs` | `NestedScrollView` defaults, the overlap handle's writer accounting/orphan and multi-writer reporting, absorber geometry (obstruction removed from scroll/layout extent, clamped at zero, empty child, handle hand-off) and injector geometry (missing-absorber failure, scroll-offset and remaining-paint clamps, relayout on handle notification), the outer/inner drag split below/at/above the header extent and back, `FloatHeaderSlivers`, inner/outer `JumpTo` hand-off, controller seeding, pointer-signal movement plus its two-start/two-end notification pair, `innerBoxIsScrolled` flips, `UserScrollNotification` direction sequences, a live pinned absorber/injector pair, zero-area layout, and the empty-header can-drag rule. | Real fling trajectories through the gesture arena, and the ignore-pointer behavior tracked in `DIVERGENCES.md`. |
| Draggable scrollable sheets | `src/Plumix.Tests/DraggableScrollableSheetTests.cs` | Defaults and constructor validation, fractional layout and `expand`, drag-to-resize versus list hand-off, implied snap sizes and their error messages, extent conversion/clamping/`CopyWith` rules, notification payload and depth, controller attach/size/pixels/jump/animate/reset/listener contracts, widget-update propagation, actuator reset, constant-velocity snapping and the ballistic hand-off, and position absorption across a replacement. | Real pointer-driven flings still go through the harness's direct position calls rather than the gesture arena. |
| Core scroll physics and simulations | `src/Plumix.Tests/ScrollPhysicsTests.cs`, `ScrollPipelineTests.cs` | Tolerance/near-equal helpers, friction (including `Through`, `FinalX`, `TimeAtX`, constant deceleration) and bounded friction, critical/over/under-damped springs, snap-to-end, duration-and-bounce descriptions, clamped simulations, bouncing and clamping scroll simulations against Flutter's asserted trajectories, physics `ApplyTo` chains, tolerance-by-DPR, overscroll friction/easing/deceleration-rate rules, fling and spring defaults, carried momentum, boundary conditions, range-maintaining dimension adjustments, always/never scrollable acceptance and chaining, hold/drag entry points, the drag controller's motion-start threshold and carried-momentum rules, position-level overscroll plus spring-back, raw-view deferred-loading thresholds, and one-frame implied velocity after forced pixel changes. | Multi-axis overscroll and momentum carried across successive real flings. |
| Core overscroll indicators | `src/Plumix.Tests/OverscrollIndicatorTests.cs` | Glow/stretch defaults, axes, notification predicates, leading/trailing veto and paint offsets, source stretch strength/origin/return, conditional clips, painter composition, and Material M2/M3/platform selection. | Backend shader acceleration is unavailable, so `StretchEffect` follows Flutter's own affine fallback path. |
| Core drag, overlays, and tap regions | `src/Plumix.Tests/DragTargetTests.cs`, `TapRegionTests.cs` | Immediate/long-press drag contracts, overlay entry/theater lifecycle, portal nearest/root placement, inherited ownership, layout information, controller z-order, grouped inside/outside callbacks, route-aware registration, arena consumption, and drag/target callback policies. | Cross-host native haptic verification and automatic app-shell `TapRegionSurface` installation outside the sample host remain follow-ups. |
| Core pop handling | `src/Plumix.Tests/PopScopeTests.cs`, `NavigationTests.cs`, `MaterialPageTransitionsTests.cs` | Pop-entry registration, collective/dynamic veto, typed success/failure results, route disposition, nested-navigator notifications and handler callbacks, Form delegation, predictive gesture observer ownership/progress/cancel/commit, and Cupertino LTR/RTL edge-swipe tracking, settle decisions, paint composition, and balanced navigator callbacks. | Deprecated asynchronous `WillPopScope` remains a separate shared gap. |
| Frame and scheduler flow | `src/Plumix.Tests/FramePipelineTests.cs` | Begin/draw/post-frame order, persistent callbacks, build scheduling in frame. | Long-running callbacks vs host visibility transitions. |
| App and animation-status lifecycle | `src/Plumix.Tests/AppLifecycleListenerTests.cs` | Lifecycle transition synthesis/callback ordering, duplicate suppression, collective exit veto, Android activity/window-focus state combination, listener rebinding/disposal, status-only rebuilds, and disposable context ownership. | Native notification delivery still benefits from device-level browser/mobile smoke testing. |
| Hot reload / reassemble | `src/Plumix.Tests/HotReloadTests.cs` | Whole-tree reassemble with `State` preservation, `State.Reassemble`-before-rebuild ordering, host `ReassembleApplication`, `HotReloadManager` live-host fan-out, manual `Ctrl/Cmd+Shift+R` shortcut gating. | Rude-edit restart fallback and reassemble during active animations/routes. |
| Render object paint transforms | `src/Plumix.Tests/PaintTransformTests.cs`, `Matrix4Tests.cs`, `MatrixUtilsTests.cs`, `Matrix4TweenTests.cs`, `TransformWidgetTests.cs` | `RenderObject.ApplyPaintTransform` for boxes, transforms, fitted boxes and fractional translations, `GetTransformTo`/`LocalToGlobal`/`GlobalToLocal` composition (including subtrees hidden from semantics), and `Path.Combine`'s set operations; the column-major `Matrix4` value (storage, multiply, invert, rotate/scale/translate, Avalonia projection), the full `MatrixUtils` surface (translation/scale extraction, `MultiplyInPlace`, null-as-identity equality, point/rect transforms with the perspective and huge-rect paths, `ForceToPoint`, cylindrical projection), `Matrix4Tween` decomposition, and `Transform`'s named constructors with `origin`/`alignment` conjugation, singular short circuit, `transformHitTests` and perspective hit testing. | Golden verification of perspective paint across hosts. |
| Render object parity | `src/Plumix.Tests/RenderingParityTests.cs` | Constraint normalization, relayout boundaries, layout exception propagation, constraint clamping, invalidation no-op guards, `RenderFlex` overflow debug paint. | Constraint edge cases across deep mixed proxy/flex chains. |
| Compositing and layers | `src/Plumix.Tests/CompositingLayerTests.cs`, `LayerV2Tests.cs`, `AnnotatedRegionTests.cs` | Repaint boundary behavior, layer reuse/updates, layer tree structure, resize-driven clip-layer refresh, typed front-to-back annotation lookup, exact-type matching, bounds, opacity, local coordinates, aligned-transform anchor geometry, and transformed/clipped traversal. | Layer churn under repeated boundary toggles in large trees. |
| Core composited transforms | `src/Plumix.Tests/CompositedTransformTests.cs`, `CompositingLayerTests.cs` | Target/follower defaults and updates, shared-link ownership, anchor/offset geometry, linked/unlinked paint policy, transformed hit testing and semantics, and layer registration. | Root overlay/portal ownership remains a separate shared primitive. |
| Basic proxy/layout/decoration widgets | `src/Plumix.Tests/BasicWidgetProxyTests.cs`, `BaselineTests.cs`, `IntrinsicWidgetsTests.cs`, `IntrinsicQueryParityTests.cs`, `LayoutBuilderTests.cs`, `AlignTests.cs`, `ImplicitAnimationsTests.cs`, `TransitionsTests.cs`, `AnimatedSwitcherTests.cs`, `TickerProviderTickerModeTests.cs`, `VisibilityTests.cs`, `AspectRatioTests.cs`, `FractionallySizedBoxTests.cs`, `FittedBoxTests.cs`, `UnconstrainedLimitedBoxTests.cs`, `OverflowBoxTests.cs`, `OffstageTests.cs`, `StackTests.cs`, `DirectionalPositionedTableCellTests.cs`, `WrapTests.cs`, `DecoratedBoxTests.cs`, `GradientTests.cs`, `BoxShadowTests.cs`, `ContainerTests.cs`, `MaterialDialogTests.cs`, `MaterialDropdownTests.cs` | Widget-to-render wiring and rebuild behavior for proxy/layout/decoration widgets, including box/sliver constraints, directional Stack/IndexedStack/AnimatedSize alignment, TableCell alignment/semantics, constraint transforms, intrinsic sizing, cached intrinsic/dry-layout/nullable-baseline reuse and invalidation, dry-query side-effect guards, transition/listener lifecycle, nested ticker-mode policy without owner rebuilds, decoration updates, the full gradient family (linear/radial/sweep defaults, stop-union lerp, cross-kind cross-fade, `Scale`/`WithOpacity`/`FromColor`, rotation transforms, directional-alignment resolution, shader geometry and stop validation) and `Shadow`/`BoxShadow` lerp/list padding/blur-style propagation with structural decoration equality, visibility, physical paint, and pointer filters. | Nested layout stress with deep overlay stacks, mixed unconstrained parents, and pointer filters around gesture-heavy controls. |
| Core custom clipping and physical shapes | `src/Plumix.Tests/ClipWidgetsTests.cs`, `LayerV2Tests.cs` | `ClipOval`/`ClipPath`/`PhysicalShape` defaults and render updates, `CustomClipper<T>` cache/listener lifecycle, shape-aware hit regions, semantics clips, physical fill/shadow composition, clip-quality state, paint offsets, and `Clip.none`. | Isolated `antiAliasWithSaveLayer`, exact arbitrary-path shadows, and the wider `Path` command surface are tracked in `DIVERGENCES.md`. |
| Core flow and repaint isolation | `src/Plumix.Tests/FlowRepaintBoundaryTests.cs`, `IntrinsicQueryParityTests.cs`, `BasicWidgetProxyTests.cs`, `CompositingLayerTests.cs` | Default/unwrapped child composition and keys, delegate size/constraints plus direct intrinsic/dry sizing, listenable repaint, transform/opacity paint order, transparent-child hit testing, transformed semantics, clipping, replacement invalidation, and composited repaint boundaries. | Render-subtree image capture is tracked in `DIVERGENCES.md`. |
| Core custom multi-child layout and toolbar | `src/Plumix.Tests/CustomMultiChildLayoutTests.cs`, `IntrinsicQueryParityTests.cs` | `LayoutId` defaults/parent data, delegate size/constraints/positions, direct parent-only intrinsic/dry sizing, exactly-once and unique-ID contracts, listenable relayout, delegate replacement, hit testing, plus centered/start LTR/RTL `NavigationToolbar` geometry. | Further stress with deeply nested delegate-driven layouts. |
| Core dismiss and size-change notifications | `src/Plumix.Tests/DismissibleSizeChangedLayoutTests.cs`, `GesturePipelineTests.cs` | Defaults/guards, directional thresholds, updates, background clipping, confirmation/veto, move/collapse callbacks, drag-start/axis-projected fling-velocity contracts, and post-initial size notification bubbling. | Stress with rapid async confirmations while keyed list items are reordered. |
| Tickers and animation controllers | `src/Plumix.Tests/AnimationControllerTickerTests.cs`, `TickerProviderTickerModeTests.cs` | Ticker start/stop/mute/absorb lifecycle and elapsed-since-start reporting; `TickerFuture` completion versus cancellation, `OrCancel` faults and microtask `WhenCompleteOrCancel`; controller defaults/bounds, missing-duration errors, value-derived status, direction-specific durations, proportional and zero-duration `AnimateTo`, reset, `LastElapsedDuration`/`Velocity`, fling bounds and underdamped guard, repeat phase/count/min-max, `AnimateWith`/`AnimateBackWith`, `Toggle`, `Stop`, `AnimationBehavior` scaling, `Resync`, dispose guards and `ToString`. | Ticker behavior while frames are disabled by a host lifecycle. |
| Core animation builders | `src/Plumix.Tests/BuilderWidgetsTests.cs` | Constructor/default guards; listener rebinding/disposal and stable children; tween retargeting; dual enter/exit interruption continuity; repeating restart/reverse, pause/resume, curves, and duration. | Stress with rapid retargeting across custom reference-type animatables. |
| Core async builders | `src/Plumix.Tests/AsyncBuilderTests.cs` | Snapshot factories/data/error contracts; future waiting/completion/error and stale-callback suppression; stream connect/data/error/done/disconnect folding, replacement, and disposal. | High-rate concurrent observable producers across host UI synchronization contexts. |
| Core image pipeline, filters, and widgets | `src/Plumix.Tests/ImageProviderDecorationTests.cs`, `ImageWidgetTests.cs`, `FilterWidgetsTests.cs`, `IntrinsicQueryParityTests.cs` | Stream/cache/provider contracts, DPR assets, decoration paint, direct image intrinsic/dry sizing, image-widget lifecycle, fade/icon behavior, plus color/image/shader/backdrop filter defaults, config resolution, inherited group keys, scene-input reuse, render updates, retained layers, blend modes, and boundary toggling. | Animated codecs, scroll-aware deferral, raw-handle cloning, and grouped backdrop filter fusion are tracked in `DIVERGENCES.md`; cross-host filter goldens remain useful. |
| MediaQuery and SafeArea | `src/Plumix.Tests/SafeAreaTests.cs`, `MaterialBannerTests.cs`, `ApplicationWidgetsTests.cs` | Insets transformations, box/sliver safe-area edges and minimums, descendant edge removal, accessible navigation, exact linear/nonlinear `TextScaler` retention and compatibility guards, host brightness data, high-contrast policy, and root ambient MediaQuery/Overlay wiring. | Remaining `MediaQueryData` fields, native system-scaler/high-contrast reporting, and broader aspect-specific accessor coverage. |
| Text widget and paragraph layout | `src/Plumix.Tests/TextWidgetTests.cs`, `IconThemeDataTests.cs`, `InlineSpanTests.cs`, `MaterialBannerTests.cs` | `Text`/`Text.Rich` -> `RichText` -> `RenderParagraph` wiring, inherited styles, exact ambient nonlinear scaling, legacy scale-factor compatibility and cap behavior; strict `IconThemeData` fields/defaults/copy/merge/lerp/equality/diagnostics/validation; plus `Icon`/`IconTheme` sizing, color, opacity, ambient text scaling, direction, and glyph-excluding semantic labels. `InlineSpanTests` covers the span model: plain-text flattening, `CodeUnitAt`, affinity-based `GetSpanForPosition`, `CompareTo` invalidation levels, locale/spell-out inheritance, `CombineSemanticsInfo`, `WidgetSpan` asserts and scale extraction, and paragraph placeholder children. | Runtime visual parity across hosts/fonts (line metrics, glyph shaping); variable-font icon axes and icon shadows remain part of the qualified `Icon` widget port; span-level hit-test and semantics geometry (see `DIVERGENCES.md`). |
| Material animated icons | `src/Plumix.Tests/MaterialAnimatedIconTests.cs` | Complete catalog metadata, generated path frames, linear interpolation, theme size/color/opacity, semantics, RTL mirroring, argument guards, and animation-driven painter invalidation. | Cross-host antialiasing differences in filled cubic paths. |
| Form and form fields | `src/Plumix.Tests/FormTests.cs`, `PopScopeTests.cs` | Typed global-key state/context access, field registration, validate/granular validate, save/reset/clear-error, forced errors, interaction state, on-unfocus validation, form/invalid semantics, modern route-pop veto/result callbacks, and `restorationId`-driven persistence of `error_text`/`has_interacted_by_user`. | Deprecated async `onWillPop` is tracked in `DIVERGENCES.md`. |
| Core/Material application shell and theming | `src/Plumix.Tests/ApplicationWidgetsTests.cs`, `MaterialThemeAnimationTests.cs`, `MaterialScaffoldTests.cs`, `MaterialDrawerTests.cs` | `WidgetsApp` routing/localization/title/infrastructure composition; `MaterialApp` page routes, messenger/selection/localizations, theme-mode precedence and scroll behavior; interruptible global/component theme interpolation, exact policy midpoint switching, theme-extension union lerps, and principal Material shell behavior; classic `Drawer` direct M2/M3 roles, theme copy/lerp/capture, precedence, start/end LTR/RTL shape resolution, host-platform semantics, and zero-area layout; standard `AppBar` direct M2/M3 roles, visual/theme precedence, implied slots, stateful scroll-under resolution, toolbar composition, system overlays, and semantic ordering. | Router-delegate mode, asynchronous localization loading, process restoration, debug overlays, and the narrowed app-bar preferred-size/color type contracts are tracked in `DIVERGENCES.md`. |
| Material color and typography | `src/Plumix.Tests/MaterialColorSchemeTests.cs`, `MaterialColorsTests.cs`, `MaterialThemeAnimationTests.cs` | Complete color-role fallbacks/copy/lerp, light/dark HCT seed generation, all dynamic variants, contrast guards, `ThemeData` scheme projection, exact Material 2014/2018/2021 type scales, platform color/font/fallback themes, dense/tall script categories, localized theme merging/cache behavior, text-theme copy/merge/apply/lerp, and animated scheme/typography interpolation. Also the palette itself: `MaterialColor`/`MaterialAccentColor` shade lookup, swatch opacity/duplicate/primary-equality invariants over `Colors.Primaries`/`Accents`/`Grey`, `ColorSwatch` structural equality and clamped `Lerp`, `ColorScheme.FromSwatch` light/dark roles and overrides, the `primarySwatch` M2/M3 `ThemeData` derivation, the black/white opacity ramps, and `EstimateBrightnessForColor` over the palette. | Cross-host font rasterization and platform font availability remain host-specific. |
| Material sliver app bars | `src/Plumix.Tests/MaterialSliverAppBarTests.cs`, `SliverPersistentHeaderTests.cs` | Persistent-header extent contracts and geometry for all four modes (scrolling/pinned/floating/floating-pinned), paint origin under overlap, floating expansion rules, overscroll stretch and its edge-triggered callback, snap guards, reveal-driven expansion, delegate-to-render-object wiring and configuration updates, strict flexible-space state/settings API, none/pin/parallax offsets, logical title padding, scaled-width constraints, M2/M3 title styles, zoom/blur/fade stretch effects, repaint/semantics policy, regular/medium/large defaults, scroll-under surface precedence, custom-scroll integration, and alignment-aware transforms. | Trailing overscroll now matches Flutter, which surfaces no overlap there either. |
| Material text fields | `src/Plumix.Tests/MaterialTextFieldTests.cs`, `TextInputTests.cs` | Controller/focus/form lifecycle, persistent editable geometry, pointer/keyboard and handle selection, adaptive context menus/magnifiers, explicit/default spell-check services, input/submit/read-only/obscure paths, max length, and semantics. | Autofill/restoration and scroll-controller integration remain tracked in `DIVERGENCES.md`. |
| Material input decoration | `src/Plumix.Tests/MaterialInputDecoratorTests.cs` | `RenderDecoration` geometry (content padding, floating-label height, minimum interactive height, dense/collapsed/outline padding tables, icon and affix placement, RTL mirroring, subtext row, hit testing), border-gap tracking and float alignment, `textAlignVertical` with `expands`, visual density, intrinsics, M2/M3 default resolution per state, border slot precedence and state-border resolution, theme `applyDefaults`/constraints, `InputBorder` equality/dimensions/scale/lerp on the `ShapeBorder` hierarchy, `ShapedInputBorder`, the floating-label paint transform through `LocalToGlobal`/`GlobalToLocal`, the prefix/suffix/icon sibling semantics with their per-decorator ordinal sort keys, the exact `_InputDecoratorDefaultsM2`/`_InputDecoratorDefaultsM3` label/hint/helper/error/counter/icon/border tables per state (including focused-beats-hovered and the suffix-only error branch), `InputDecorationThemeData` copy/merge/equality with the six non-mergeable fields, the obsolete field-based `InputDecorationTheme` surface, `ApplyDefaults` from the theme widget, the ambient `IconButtonTheme` affix-colour fallback, state-resolving theme values, and the floating-label `baseStyle` merge. | Absolute pixel sizes track the host font metrics rather than Flutter's test font, so geometry is asserted through the layout formulas. |
| Core selection protocol | `src/Plumix.Tests/SelectionSubsystemTests.cs` | `SelectionUtils` result/drag-offset tables (LTR and RTL), `SelectionGeometry` invariants/equality/`CopyWith`, `SelectedContentRange` guards, `SelectionEvent` defaults, the `TextBoundary` family, `RenderParagraph` fragment registration and placeholder splitting, fragment select-all/clear/content/handle types, granular extension per `TextGranularity`, and `SelectionContainer` registration (enabled, disabled, empty, registrar-from-context) plus multi-selectable content/range aggregation. | Overlay handles, magnifier and platform gesture-arena outcomes for generic selection are tracked in `DIVERGENCES.md`. |
| Material read-only text selection | `src/Plumix.Tests/MaterialSelectionTests.cs` | `SelectableText` read-only `EditableText` composition, theme/cursor, pointer/handle drag, select-all/copy, automatic context-menu and magnifier actions, callbacks, `SelectionArea` multi-paragraph aggregation through the ported selection protocol, and the `TapAndPanGestureRecognizer`-driven tap tables (double-tap word, desktop triple-tap paragraph, series reset after the double-tap timeout, right-click menu over an active selection). | Rich spans, granular keyboard movement, and advanced bidi selection geometry remain tracked in `DIVERGENCES.md`. |
| Material text selection theme | `src/Plumix.Tests/MaterialTextSelectionThemeTests.cs`, `MaterialSelectionTests.cs`, `ApplicationWidgetsTests.cs` | `TextSelectionThemeData` defaults/`CopyWith`/equality/hash and `Lerp` (null pair, identity short circuit, colour interpolation); M2/M3 empty-theme cursor/selection/handle defaults from `ColorScheme.Primary`; `ThemeData.TextSelectionTheme` and the `TextSelectionTheme` widget overriding it; `TextField`/`SelectableText` colour parameters beating both; the widget overriding an ancestor `DefaultSelectionStyle` while leaving contexts above it untouched; the `_hasError` cursor branch (decoration error text, `cursorErrorColor`, over-`maxLength` counter); and the iOS/macOS `CupertinoTheme` fallback. | `debugFillProperties` and the `MaterialBasedCupertinoThemeData` bridge are tracked in `DIVERGENCES.md`. |
| Material and Cupertino selection toolbars | `src/Plumix.Tests/MaterialDesktopTextSelectionToolbarTests.cs`, `MaterialSelectionTests.cs`, `MaterialTextFieldTests.cs`, `ImplicitAnimationsTests.cs`, `FilterWidgetsTests.cs` | Android/iOS and Windows/macOS routing, editable/selectable context-menu ownership, localized actions, render-bound handles, adaptive magnifiers, native Android spell-check service registration, spell-check replacement styling, anchors/insets, overflow, clamping, disabled state, hover/press treatment, composed backdrop filters, and semantics. | Rounded-superellipse/path-shadow/retained-clip backend deltas remain tracked in `DIVERGENCES.md`. |
| Material ink responses | `src/Plumix.Tests/MaterialInkResponseTests.cs`, `GesturePipelineTests.cs` | `Ink` composition and ancestor-Material registration/paint order; response/well geometry, nested press ownership, and gestures; factory precedence and platform defaults; independent rapid splashes; pressed/hover/focus fades; splash/ripple/sparkle timing; circular Material clipping; deterministic sparkle seeds; non-painting `NoSplash`. | Shader-identical sparkle noise remains tracked in `DIVERGENCES.md`. |
| Material date/range pickers | `src/Plumix.Tests/MaterialDatePickerTests.cs` | Date value contracts, day/year/range selection, predicates, entry modes, exact direct-role M2/M3 defaults, theme copy/lerp/capture and precedence, state shapes/overlays, locale routing, geometry, connected range paint, typed route results, and semantics. | Restoration, IME hints, foldable anchoring, and advanced keyboard traversal are tracked in `DIVERGENCES.md`. |
| Material time picker | `src/Plumix.Tests/MaterialTimePickerDialogTests.cs`<br>`src/Plumix.Tests/MaterialTimePickerThemeTests.cs` | `TimePickerThemeData` defaults/copy/lerp and the `dayPeriodColor` auto-wrap, M2/M3 defaults, dialog shape/elevation/inset padding, portrait/landscape/input sizes, 12h and M2/M3 24h dial rings, dial tap/drag selection with 5-minute rounding, `_RenderInputPadding` tap-target redirection, entry-mode toggling and dial-only/input-only, input parsing/validation and auto-advance, separator glyphs and theme overrides, and the full semantics surface (mode announcements, increase/decrease, AM/PM checked-vs-selected). | Restoration, IME hints, foldable anchoring, haptics timing, and web select-all-on-focus are tracked in `DIVERGENCES.md`. |
| Material buttons | `src/Plumix.Tests/MaterialButtonsTests.cs`, `MaterialLegacyButtonTests.cs`, `MaterialFloatingActionButtonTests.cs` | Modern and legacy button defaults, `ButtonStyle`/`styleFrom` and `ButtonThemeData` precedence, state/elevation/overlay/splash/focus/highlight resolution, long-press-only enablement, icon factories, density/tap-target sizes; TextButton, ElevatedButton, OutlinedButton, and FilledButton/tonal direct M2/M3 scheme roles, executable overlay defaults, callbacks/state/semantics, inherited theme capture, source clip/style metadata, and FilledButton layer builders; exact IconButton M2/M3 composition, direct scheme roles, variants, tooltip/state-controller/theme copy/lerp; plus FAB direct M2/M3 scheme roles, variants, overflow layout, hero/semantics policy, and state-aware theme copy/lerp. | Full Flutter `ButtonStyle` parity matrix and deeper token combinations. |
| Raw/Material scrollbars | `src/Plumix.Tests/MaterialScrollbarTests.cs`, `ScrollInfrastructureTests.cs` | Source-shaped public painter/state extension API, constructor/controller guards, overlay-preserved child sizing, vertical/horizontal/RTL/padding/cross-margin/zero-area/infinite-extent geometry, transient and toggled visibility, 200ms hover color, 48px proximity, content-click isolation, thumb dragging, eased track paging, `WidgetStateProperty` theme copy/lerp/capture, direct `ColorScheme.onSurface` roles, Android/desktop defaults, and adaptive Cupertino brightness/resize/haptic requests. | Native haptic output remains a host-adapter concern tracked in `DIVERGENCES.md`. |
| Core/Material reorderable lists | `src/Plumix.Tests/MaterialReorderableListTests.cs` | Strict nullable/default and constructor guards; cache, anchor, drag-start, keyboard-dismiss, restoration-ID and clip forwarding; horizontal RTL/reverse direction; keyed mutation rebuilds; adjusted/deprecated callbacks; fixed/variable/prototype extents; desktop/mobile handles and dragged-state cursors; header/footer padding; local/global/free drag boundaries; animated overlay proxy/theme/constraint preservation; delayed drop, edge auto-scroll, localized semantics, and proxy clamping. | Process-level restoration buckets remain tracked in `DIVERGENCES.md`; focused coverage verifies the available page-storage persistence contract. |
| Core animated lists and grids | `src/Plumix.Tests/AnimatedListTests.cs` | Constructor/default guards, logical versus physical insert/remove indices, incoming/outgoing animation values and lifecycle, separated-list coordination, grid-delegate layout, ancestor lookup, MediaQuery padding, viewport clip/cache policy, bulk operations, and keyed state preservation. | — |
| Scroll metrics | `src/Plumix.Tests/ScrollMetricsTests.cs`, `ScrollInfrastructureTests.cs` | `FixedScrollMetrics` values and its three `has*` flags, the throw behind a missing value, axis resolution, `outOfRange`/`atEdge`/`extentBefore`/`extentInside`/`extentAfter`/`extentTotal` including both overscroll directions, invariant `toString`, `copyWith` preservation and per-field override (including keeping unavailable values unavailable), `ScrollPosition`'s implementation of the same contract, the `NestedScrollMetrics` subclass and its widened `CopyWith`, and `ScrollMetricsNotification.AsScrollUpdate`. | `PageMetrics` lives in `PageViewTests.cs`. |
| Core page views | `src/Plumix.Tests/PageViewTests.cs` | `PageView`/`PageController` defaults and the `scrollCacheExtent`/`allowImplicitScrolling` consistency assert, the four controller asserts with Flutter's messages, `PageMetrics.page` clamping, initial-page layout on the first frame, `viewportFraction` above/below/equal to one with `padEnds` geometry, the zero-viewport `_cachedPage` state machine and resize restore, viewport-fraction changes mid-flight, page stability across resizes, precision-error page rounding, fractional viewport widths with far jumps, vertical/reverse axis resolution, the lazy child window under three cache extents, `onPageChanged` firing exactly at the halfway point and once per page, drag snapping and disabled snapping, controller swap (null->value, value->value, value->null), `onAttach`/`onDetach`, `nextPage`/`previousPage` completion, `PageStorage` page round-trip with `keepPage: false`, `PageScrollPhysics` ballistic targets, `ForceImplicitScrollPhysics` chaining, the `showOnScreen` reveal of a cached page, and notification metrics type-testing as `PageMetrics` with its fraction-carrying `CopyWith`. | The `applyViewportDimension` correction loop is now Flutter's. |
| Core list wheel | `src/Plumix.Tests/ListWheelScrollViewTests.cs` | `ListWheelScrollView` defaults and its seven constructor asserts (with Flutter's messages), `RenderListWheelViewport` setter validation and index/offset helpers, the three child delegates (Dart modulo, `shouldRebuild`), `FixedExtentScrollController` (`selectedItem` asserts, `debugLabel`/`toString`, `onAttach`/`onDetach`, `initialItem` (known before the first layout), the ListWheel-only assert, `jumpToItem`, `animateToItem`, hot swap, reuse, `PageStorage` round-trip with `keepScrollOffset`), `FixedExtentMetrics.copyWith`, zero-child and zero-size layout, item-extent clamping, cross-axis centering, the lazy child window (start/middle/end, first-pixel enter, last-pixel leave, squeeze, looping and infinite builders, lower/upper limits, delegate updates, builder-called-once), the exact paint matrices (default, scrolled, `diameterRatio`, `perspective`, `offAxisFraction`, magnifier), the single opacity layer, `clipBehavior` on the clip layer, `onSelectedItemChanged` (first build, half-way switching, `onScrollEnd`, valid range, controller sync, looping true index, plain-controller opt-out), `FixedExtentScrollPhysics` (low/high fling, scenario table, wrong-position assert, limit-respecting flings), `getOffsetToReveal` (direct/descendant, custom rect, foreign axis), `showOnScreen` (jump and animated), taps on painted children and none on laid-out-but-unpainted ones, and the demo page. | Test gestures compensate Plumix's slop-on-accepted-drag divergence (see `DIVERGENCES.md`). |
| Material tabs and page views | `src/Plumix.Tests/MaterialTabsTests.cs` | `Tab` sizing/asserts/icon-margin defaults, `TabController` lifecycle (instant vs animated index changes, notification order, offset asserts, dispose, controller handoff), `TabBarThemeData` defaults/copy/lerp and `TabBarTheme` data-vs-property exclusivity and `Of` fallback, `UnderlineTabIndicator` geometry/lerp, M2/M3 primary and secondary default tables (label colors, indicator weight/radius, divider, overlay ladder, automatic indicator-color adjustment), widget-vs-theme-vs-default precedence, `TabAlignment` validation and scrollable `startOffset`/`start` defaults, uniform text+icon padding, exact indicator rects with weight/padding in LTR and RTL, linear vs elastic indicator math, `TabBarScrollController` attach/detach and initial centering, splash-border-radius/mouse-cursor/text-scaler plumbing, tab/tabBar/tabPanel semantics, zero-tab and zero-area handling, full gesture-route taps, fractional page layout, non-adjacent warps, swipe synchronization, and lazy tab-panel building. | Page-view behavior itself lives in `PageViewTests.cs`. |
| Material action buttons | `src/Plumix.Tests/MaterialActionButtonsTests.cs`, `MaterialScaffoldTests.cs` | Source `IconButton` inheritance/composition and standard-component keys; back/close/drawer/end-drawer glyphs; action-icon theme copy/precedence; direct M3 `onSurfaceVariant` and M2 ambient icon colors; localized tooltips and default-platform Android icon labels; custom/default navigation and Scaffold callbacks; style precedence; and AppBar implied actions. | Cross-host accessibility verification with native screen readers. |
| Material drawer headers | `src/Plumix.Tests/MaterialDrawerHeaderTests.cs` | Generic decoration/directional inset defaults, status-bar/divider geometry, direct `ColorScheme.primary`, picture limits/sizes/tap targets, exact LTR/RTL account-details layout, reversible arrow motion, callback stability, typography, zero-area handling, and localized semantics. | Cross-host golden verification of animated arrow and decoration transitions. |
| Core/Material selection controls | `src/Plumix.Tests/FocusTraversalTests.cs`, `RadioGroupRawRadioTests.cs`, `MaterialCheckboxTests.cs`, `MaterialSwitchTests.cs`, `MaterialRadioTests.cs`, `MaterialRadioExpansionTileTests.cs` | Stable geometry/bidi reading-order bands and zero-area handling; registry/client lifecycle; nested focus-policy flattening; selected-only Tab entry; reading-order arrows; disabled skipping, wrapping, Space, and shortcut fall-through; raw toggleable timing; strict Checkbox, Switch, and Radio M2/M3 direct-token defaults, state/transition paint, density/tap targets, cursor/theme copy/lerp, background/side/inner-radius precedence, semantics, value cycles, adaptive Cupertino paths, and adaptive group registration. | Cross-host radio/ink golden verification remains. |
| Core/Material/Cupertino magnifiers | `src/Plumix.Tests/MagnifierTests.cs`, `SelectionOverlayTests.cs` | Raw lens defaults/guards, render/layer wiring, bounds shifting, root-overlay entry lifecycle and `below` ordering, animation visibility, handle-capture ordering, Material Android geometry, text-line/focal clamping, adaptive platform routing, and Cupertino animation geometry. | Transformed lens capture and multiple overlapping magnifiers need broader cross-host verification. |
| Material progress indicators | `src/Plumix.Tests/MaterialLinearProgressIndicatorTests.cs`, `MaterialCircularProgressIndicatorTests.cs`, `MaterialRefreshIndicatorTests.cs` | Determinate/indeterminate behavior; direct M2/M3 scheme roles; 2023/2024 stop, gap, cap, alignment, constraint, and padding geometry; source theme copy/lerp/capture and controller precedence; progress/loading roles with numeric ranges; adaptive Cupertino paths; plus refresh surfaces, drag/status/programmatic lifecycle, and leading glow/stretch suppression. | Cross-host golden verification of the complete indeterminate animation sequence. |
| Material bar controls | `src/Plumix.Tests/MaterialBarControlsTests.cs`, `MaterialLegacyButtonTests.cs`, `MaterialScaffoldTests.cs`, `MaterialScaffoldGeometryTests.cs`, `MaterialFloatingActionButtonLocationTests.cs` | `BottomAppBar` direct M2/M3 scheme defaults, theme copy/lerp/precedence, SafeArea, configured FAB notch geometry and cutout hit testing; standard FAB locations/animator contracts; standard FAB locations with the exact `kMiniButtonOffsetAdjustment`, the scaling/no-animation animator scale and turn animations, `_ScaffoldLayout` slot measurement and paint order, live `ScaffoldGeometry` notification and paint-phase guard, measured snack-bar/bottom-sheet/keyboard FAB avoidance, persistent-footer geometry/alignment/media padding/`SafeArea`/decoration, `_BodyBuilder`'s `extendBody`/`extendBodyBehindAppBar` padding restoration and body-state preservation, tight drawer slots with the `_endDrawerOpened` paint swap, drawer dismiss-intent and change callbacks, the iOS/macOS status-bar slot and its 1000ms `easeOutCirc` foreground-only scroll, and the 400ms FAB relocation motion with interrupt restart and rebuild-free relayout; and strict `ButtonBar` defaults/theme API, directional legacy-button padding, `RenderFlex` row/vertical overflow and dry layout, LTR/RTL alignment, spacing, direction, and padded/constrained sizing. | Full custom `ShapeBorder` and cross-host notch golden verification. |
| Core shape borders | `src/Plumix.Tests/ShapeBorderTests.cs`, `DecoratedBoxTests.cs` | `BorderSide` defaults/stroke geometry/merge table/scale/lerp, `ShapeBorder.Lerp` identity and half-way fallback, `CompoundBorder` merge/dimensions/scale/lerp/`PreferPaintInterior`/`ToString`, `Border` and `BorderDirectional` constructors, dimensions, uniformity, add/scale/lerp, non-uniform paint guards and text-direction paths, `BoxBorder.Lerp` dispatch, every concrete `OutlinedBorder` (defaults, `CopyWith`, dimensions, scale, lerp chains toward circle/stadium/rounded rect, outer/inner paths, eccentricity), `StarBorder`/`LinearBorder` value semantics and degenerate-rect safety, and `ShapeDecoration` `FromBoxDecoration`/padding/hit test/clip path. | Pixel goldens for star, bevel and continuous outlines; shape shadows are tracked in `DIVERGENCES.md`. |
| Core tables | `src/Plumix.Tests/TableTests.cs`, `DirectionalPositionedTableCellTests.cs` | `TableColumnWidth` algebra defaults/formulas, the flex-grow and deficit-shrink column algorithm, every `TableCellVerticalAlignment` (including baseline sharing, zero-height fill rows and intrinsic-height second passes), RTL column order, intrinsics/dry layout/dry baseline, flat-grid mutation (`SetFlatChildren`/`AddRow`/`SetChild`/row and column shrink), row-decoration painting and painter disposal, reverse-order hit testing, `TableBorder` defaults/`Scale`/`Lerp`, the table semantics role, synthesized row/cell semantics nodes (roles, `IndexInParent`, row/cell geometry, wrapper insertion for multi-node and non-cell-role children, empty-row and zero-width-cell skipping, node reuse across passes and release on `ClearSemantics`), and widget-level row keying, construction guards and dimension changes. | Border path/pixel goldens. |
| Material data tables | `src/Plumix.Tests/MaterialDataTableTests.cs` | Strict API/guards and arbitrary decorations; local/global theme precedence; direct M2/M3 selected-row and divider roles; state-resolving row colors; source transparent-`Material` clipping, ambient text-style merge and column-header semantics; 150ms sort-arrow transitions; column negotiation, row/cell geometry, `TableRowInkWell` row bounds and cell override policy, sorting/selection, source caching, and pagination. | `PageStorage`-restored page index after subtree removal and cross-host golden verification. |
| Material sliders | `src/Plumix.Tests/MaterialSliderTests.cs`, `MaterialRangeSliderTests.cs` | Constructor guards, direct M2/M3 `ColorScheme` roles, source theme copy/lerp, widget/theme precedence, drag lifecycle, discrete snapping/ticks, labels/value-indicator policy, cursor/directional-padding resolution, interaction modes, 2023/2024 geometry, keyboard/semantics, adaptive Cupertino routing, pluggable track/thumb/tick/value-indicator shapes, range thumb selection, and minimum separation. | Cross-host golden verification of custom shapes and overflow value indicators. |
| Material navigation surfaces | `src/Plumix.Tests/MaterialBottomNavigationBarTests.cs`, `MaterialNavigationSurfacesTests.cs` | Bottom navigation plus `NavigationBar`/`NavigationRail` and `NavigationDrawer`/`NavigationDrawerDestination` guards, direct M2/M3 `ColorScheme` defaults, state-aware component-theme copy/lerp, fixed/shifting and light/dark bottom-bar roles, icon-opacity fallback, M3 drawer surfaces, theme precedence, label/indicator geometry, mixed-child indexing, tap/disabled states, and localized semantics. | Cross-host golden rendering at extreme text scales. |
| Material segmented buttons | `src/Plumix.Tests/MaterialSegmentedButtonsTests.cs` | `ToggleButtons` direct scheme/state-fill defaults, theme/widget precedence, adjacent selected-border ownership, elliptical radii, axis-aware tap targets, checked semantics, equalized layout, and zero-area safety; `SegmentedButton<T>` direct M3 defaults, theme copy/lerp/capture, state-controller retention, `TextButton` composition, selection content, selected/enabled semantics, equalized intrinsic/dry layout, 48px tap targets, RTL/vertical expansion, and mixed-state border paint. | Cross-host golden verification for custom asymmetric border shapes. |
| Banner and MaterialBanner | `src/Plumix.Tests/MaterialBannerTests.cs`, `TransitionsTests.cs` | Ribbon defaults/paint transforms/RTL; Material direct M2/M3 `ColorScheme` defaults, theme copy/lerp/capture and widget precedence, directional insets, action overflow, text scaling, threshold/vector entrance animation, accessible navigation, live-region dismissal, FIFO messenger presentation/closed reasons, SnackBar coexistence, root-Scaffold selection, and body push/overlay geometry. | Cross-host golden verification. |
| SnackBar and ScaffoldMessenger | `src/Plumix.Tests/MaterialSnackBarTests.cs` | Dart's constructor/theme asserts, `SnackBarThemeData` defaults/copyWith/equality and its `lerp` (including Dart's `showCloseIcon` omission), `persist` derivation, `WithAnimation` field-by-field copy and `CreateAnimationController` durations, direct M2/M3 defaults (alpha-blended M2 background, inverse-surface M3, `inversePrimary`/`secondary`/`primary` action colors), fixed-vs-floating shape and `Material` elevation/clipBehavior, widget-over-local-`SnackBarTheme`-over-`ThemeData` precedence, the M2 colour-scheme inversion (and M3 keeping the ambient theme), `TextPainter`-measured action overflow onto a second `Wrap` run at the 0.25/0/1 thresholds, one-shot action state with plain and `WidgetStateColor` colours, the composed core `Dismissible` (direction chain, null resize duration, hit-test behavior chain), floating custom-width centring, the three fixed-behavior margin/width messages, single-controller FIFO presentation with `action`/`swipe`/`remove`/`hide`/`timeout` reasons, `ClearSnackBars`, the no-descendant-Scaffold throw, `onVisible` firing once and never while queued, close-icon tooltip/size, live-region dismiss semantics, and the accessible-navigation transition bypass; floating avoidance geometry around the FAB is covered by `MaterialScaffoldGeometryTests.cs`. | Timer expiry hands back through the host dispatcher, which no unit-test harness pumps, so end-to-end timeout and drag-follow motion stay runtime/golden verification. |
| Material bottom sheets | `src/Plumix.Tests/MaterialBottomSheetTests.cs` | Direct M2/M3 `ColorScheme` roles, theme copy/lerp/diagnostics/capture and widget/theme precedence; state-resolving drag-handle geometry/hover/semantics order; drag close and reopen thresholds; 9/16 versus scroll-controlled layout; modal scrim label/hint and semantics clipping; opaque page hit-test semantics; safe-area composition; display-feature anchoring; supplied transition controllers; persistent controller/LocalHistory/height-factor lifecycle; and the draggable-scrollable protocol, body scrim, FAB visibility, and `Scaffold.bottomSheet` actuator/local-history reset. | Cross-host golden verification. |
| Display feature sub-screens | `src/Plumix.Tests/DisplayFeatureSubScreenTests.cs` | Sub-screen splitting around a hinge, anchor-point and directional fallback selection, and `MediaQueryData.RemoveDisplayFeatures` inset shrinking/validation. | Horizontal folds and multi-feature screens. |
| Split curve | `src/Plumix.Tests/CurvesSplitTests.cs` | Segment mapping, default `easeOutCubic` tail, custom begin curve, and split-point validation. | — |
| Core modal barriers | `src/Plumix.Tests/ModalBarrierTests.cs`, `ModalRouteBarrierTests.cs`, `MaterialDialogTests.cs` | Static/animated defaults, platform semantics policy, opaque any-button dismissal, system-alert dispatch, animated color updates, semantic focus clipping, sibling-semantics blocking, and dialog-route integration, plus `ModalRoute` barrier ownership: defaults, transparent fallback, `BarrierCurve` color drive, sibling paint order outside the route transition, page-before-barrier semantics order, dismissible/non-dismissible taps, and pointer ignoring while animating out. | Native host sound output remains adapter-specific. |
| Core developer visualization | `src/Plumix.Tests/DebugPaintingWidgetsTests.cs` | `Placeholder` defaults, unbounded fallback and child sizing, background cross painter, plus `GridPaper` defaults/guards, foreground composition, primary/major/minor strokes, hit testing, and repaint fields. | Cross-host golden verification of subpixel stroke rasterization. |
| Material dialog family | `src/Plumix.Tests/MaterialDialogTests.cs` | `Dialog`/`AlertDialog`/`SimpleDialog`/`SimpleDialogOption` defaults on the `Material` surface, theme precedence, `AlertDialog.Adaptive`/`ShowAdaptiveDialog` platform switching, host-platform route labels, icon-driven title centering, intrinsic/text-scale/scroll/action/choice layout, `DialogRoute` on `RawDialogRoute` (captured themes, safe area, opaque surface semantics, 150ms fade, `AnimationStyle` duration), closed-loop/leave-view traversal edges, `requestFocus`, semantics, and typed results completed on pop. | Root-navigator selection, restoration, and cross-host golden verification. |
| Cupertino theme foundation | `src/Plumix.Tests/CupertinoColorsTests.cs`, `CupertinoIconThemeDataTests.cs`, `CupertinoThemeTests.cs`, `CupertinoTextThemeTests.cs` | The full `CupertinoColors` table (plain colours, the eight-variant system colours, aliases); `CupertinoDynamicColor` equality/`ToString`/`WithBrightness`/`WithBrightnessAndContrast` and resolution against brightness, high contrast and interface elevation (including all eight variants and the no-ancestor path); strict `CupertinoIconThemeData` copy/equality/diagnostics and consumer-context dynamic-colour resolution; `CupertinoThemeData` defaults, cascade, `CopyWith`, `NoDefault`, equality and `Of`-time resolution in both brightnesses; theme/icon-theme dependency rebuild counts; and the `CupertinoTextThemeData` Apple design-resource metrics, primary-colour action styles and `ResolveFrom`. | High-contrast and elevated variants are only reachable through `MediaQuery`/`CupertinoUserInterfaceLevel` in tests — no host surfaces the accessibility contrast flag yet. |
| Cupertino localizations | `src/Plumix.Tests/CupertinoLocalizationsTests.cs` | English picker date/time formatting, both ordering enums, timer plurals and label sets, action/semantics strings, delegate support/load/reload policy, subclass overrides, and required inherited lookup. | Generated `GlobalCupertinoLocalizations` and non-English resources remain in the Cupertino work list. |
| Cupertino application shell | `src/Plumix.Tests/CupertinoAppTests.cs` | Classic and router API/defaults; Cupertino route, theme, dynamic title color, selection, localization, interface-level, scroll and system-overlay composition; desktop/mobile scrollbar, physics, multitouch and keyboard-dismiss policy. | Specialized debug overlays and asynchronous localization/restoration remain shared app-layer gaps in `DIVERGENCES.md`. |
| Cupertino page scaffold | `src/Plumix.Tests/CupertinoPageScaffoldTests.cs` | Constructor defaults; dynamic and inherited background color resolution; opaque navigation-bar content offset versus translucent overlap padding; keyboard-inset consumption with and without a navigation bar; resize opt-out; navigation-bar text-scale isolation; scroll-notification observer composition; foreground status-bar scroll-to-top; and zero-area layout/paint. | The probe navigation bar uses the public obstruction contract until `nav_bar.dart` lands. |
| Cupertino focus halo | `src/Plumix.Tests/CupertinoFocusHaloTests.cs`, `FocusTests.cs` | All three shape factories; descendant-focus propagation and stable focus while moving inside one group; exact 3.5px HSL-derived outline; foreground decoration; rounded-superellipse shape selection; and zero-area layout/paint. | Avalonia's Skia backend uses the same rounded-rectangle fallback as Flutter's Skia adapter when rasterizing an `RSuperellipse`. |
| Cupertino dialog family | `src/Plumix.Tests/CupertinoDialogTests.cs` | `CupertinoAlertDialog` fixed 270/310 widths, title/content/action text styles, translucent surface and separator hairlines, default/destructive/disabled/merged action styles, side-by-side versus stacked two-button layout, `_PriorityColumn` actions-section minimum height, sliding-tap press/slide/confirm and disabled skipping, alert-role route semantics with button nodes, `ShowCupertinoDialog` non-dismissible barrier default, and the spring-driven scale/fade entrance with fade-only exit. | Arena-arbitrated slide-versus-scroll, overscroll background goldens, and high-contrast dynamic colors are tracked in `DIVERGENCES.md`. |
| Cupertino routes | `src/Plumix.Tests/CupertinoRouteTests.cs`, `CupertinoDialogTests.cs`, `MaterialPageTransitionsTests.cs` | Page-route defaults and live previous-title/page updates; page-backed route reuse; standard versus fullscreen transition composition; directional parallax, edge shadow and leading-edge back gestures; modal-popup defaults, spring motion, dynamic barriers, display-feature anchoring and typed completion; helper navigator selection and option forwarding; Material compatibility delegation. | Cross-host pixel goldens and process-level route restoration. |
| Cupertino icons | `src/Plumix.Tests/CupertinoIconsTests.cs` | Generated 1,322-member catalog and normalized manifest, font/package metadata, every code-point alias group, four directional flags, public C# member projection, exact bundled-font asset hash/resolution, representative glyph builds, and RTL mirroring. | Cross-host pixel goldens for the entire glyph catalog. |
| Material popup menus | `src/Plumix.Tests/MaterialPopupMenuTests.cs` | Strict button/item/checked/divider API and defaults; direct M2/M3 surface, label, disabled, icon, and padding roles; theme copy/lerp/capture/live updates; source callback order and navigator-owned route lifecycle; directional/display-feature positioning; selected-item scrolling; semantics; no-animation/custom constraints; zero-area layout; and mixed-entry keyboard traversal. | Cross-host golden verification of staged menu/item animation pixels. |
| Material dropdowns and menu anchors | `src/Plumix.Tests/MaterialDropdownTests.cs`, `MaterialMenuAnchorTests.cs`, `MaterialMenuAcceleratorTests.cs`, `RawMenuAnchorTests.cs` | Legacy `DropdownButton` contracts and the two Dart asserts, directional alignment, selected/hint/disabledHint fallbacks including the disabled-with-value and nothing-to-show cases, `selectedItemBuilder` length mismatch, dense/aligned button padding, the hairline underline and its custom replacement, positioned route geometry (`menuWidth`, `menuMaxHeight`, border-radius clip and painter slots), item-tap-before-`onChanged` ordering, barrier policy, arrow/Enter traversal skipping disabled rows, autofocus and disabled-cannot-focus, variable row measurement through `_MenuItem`, the `_resize` interval checkpoints, `getMenuLimits`/`getItemOffset` against Dart's exact scroll offsets (2180/4312/0) and menu-height clamps, `_DropdownMenuRouteLayout` constraints/LTR-RTL position/`shouldRelayout`, `_DropdownMenuPainter`'s animated rect, the `kElevationToShadow` table, the orientation-change close, and the form field's defaults, deprecated `value`, validation/change/reset ordering, decoration hint fallbacks, custom error widget and unaligned padding; the core anchor tree (detached-controller asymmetry, sibling exclusivity, group no-op open, `CloseChildren`, intercepted `onOpenRequested`/`onCloseRequested`, `DismissMenuAction`, ancestor-scroll and view-size closure, `MaybeOf`/`MaybeIsOpenOf`, dispose detach, overlay info); and the Material panel (six/eight-entry shortcut maps, open/close curve checkpoints, per-item stagger intervals, directional padding geometry, `_MenuLayout` reserved-padding deflation, LTR/RTL directional alignment resolution, directional `alignmentOffset` mirroring, anchor-relative explicit positions, cascade flip vs screen clamp, keyboard/display-feature avoidance, height-factor growth, `ShouldRelayout` inputs, the `_MenuButtonDefaultsM3` table, `_scaledPadding` density/text-scale lerp, `_MenuItemLabel` spacing and shortcut slot ordering, submenu-arrow visibility, directional content alignment under LTR/RTL, `MouseRegion.onHover` edge-detected hover focus, theme precedence, semantics); the `_MenuBarDefaultsM3`/`_MenuDefaultsM3` token tables (`ColorScheme.surfaceContainer`/`shadow` roles, elevation 3, 4px corners, directional padding and alignment, unset size/side/cursor slots), `MenuStyle` copy/merge/lerp/equality under Dart's `runtimeType` guard, the three theme datas' defaults, lerp special cases, `MenuBarThemeData extends MenuThemeData` inequality and `InheritedTheme.Wrap`, the side-into-shape fold and the min/max-clamped fixed size; accelerator marker/grapheme rules, Alt visibility, nearest-registry lifecycle, focus-local precedence, identical child replacement, missing-registry behavior, Apple policy, direct rich-span composition and zero-area layout; plus `ShortcutSerialization` channel shape and the per-platform `_LocalizedShortcutLabeler` modifier order, separator, graphic/localized/Unicode trigger fallback chain and localizations cache. | Restoration and complete input configuration remain tracked in `DIVERGENCES.md`. |
| Material `DropdownMenu` family | `src/Plumix.Tests/MaterialDropdownMenuTests.cs` | Strict `DropdownMenu`/`DropdownMenuEntry`/`DropdownMenuFormField` defaults and the four constructor asserts; `DropdownMenuThemeData` equality/copy/lerp and the `_DropdownMenuDefaultsM3` table (including the menu-surface slots it deliberately leaves to `MenuAnchor`); the double-built entries, highlight/overlay styling and entry-vs-`MenuButtonTheme` merge order; disabled colors and blocked interaction; width/`expandedInsets`/`menuHeight` sizing, the 112px floor, dry layout and zero-area rendering; trailing-icon toggling and collapsed padding; arrow/Enter/Escape keyboard flow with disabled-entry skipping; default and custom search/filter; selection callbacks and controller text; `closeBehavior`; platform-driven `canRequestFocus`, `requestFocusOnTap`, `selectOnly` and cursors; expand/collapse semantics and the measurement-only entries staying out of the semantics tree; decoration/error/`decorationBuilder` flow; text-field pass-throughs and `FilteringTextInputFormatter`; and the form field's validation, save, reset and error-builder lifecycle. | Restoration across a real `restartAndRestore` cycle is not exercised. |
| Material search controls | `src/Plumix.Tests/MaterialSearchTests.cs` | `SearchBar` contracts, exact M3 `Material`-surface defaults with the double padding and collapsed-field decoration shield, theme/widget state-property precedence, hint-style fallback chain, capitalization/smart-punctuation/scroll-padding forwarding to `EditableText`, controller open/close route lifecycle with the anchored grow choreography (docked geometry, LTR/RTL navigator clamping, fullscreen fill, docked close on resize), `CapturedThemes` local-theme transfer, async suggestion dedupe/refresh, default back/clear header buttons, shrink-wrap divider gating, theme-record lerp quirks, zero-area safety, plus `SearchDelegate`/`showSearch` query, keyed results/suggestions, generic proxy route animation, typed close/reuse, search semantics, theme, and keyboard/action/correction configuration. | Text-service gaps (`SystemContextMenu`, formatter/autofill/restoration, host capitalization) remain tracked in `DIVERGENCES.md`. |
| Core/Material autocomplete | `src/Plumix.Tests/MaterialAutocompleteTests.cs` | Strict public defaults and field/options composition; sync/async/custom filtering, display and reselection; external controller/focus lifetime; exact M2/M3 canvas/focus/shadow roles; capped/shrink-wrapped sizing; down/up/most-space placement; keyboard/lazy scrolling; overlay-first hit testing; focusable button semantics; zero-area layout; portal/theme/announcement/dismissal behavior. | Cross-host announcement adapter and transformed-anchor golden verification. |
| Material surfaces, Card and MergeableMaterial | `src/Plumix.Tests/MaterialElevationOverlayTests.cs`, `MaterialCardTests.cs`, `MaterialMergeableMaterialTests.cs` | M3 tint levels/interpolation, M2 dark overlays, theme predicates and Material integration; Card direct scheme roles, variants, exact Material/semantics composition, precedence, clipping, border paint order, theme copy/lerp and semantics; strict MergeableMaterial defaults, keyed slice/gap/chunk reconciliation, extent/corner/divider animation, slice/theme colors, directional list-body geometry, transparent ink surfaces and render-owned group shadows. | Full Flutter `ShapeBorder` hierarchy parity. |
| Material CarouselView | `src/Plumix.Tests/MaterialCarouselTests.cs` | M3 item-surface defaults and overrides, theme precedence (widget/`CarouselViewTheme`/`ThemeData`), theme `CopyWith`/`Lerp`/equality, uncontained layout with trailing shrink, `initialItem`, item-extent changes, `shrinkExtent`, zero/infinite item extent, zero-sized viewport, weighted extent distribution and interpolation while scrolling, `consumeMaxWeight` slot reservation, vertical axis, snapping targets and foreign-position rejection, position resize/`CopyWith`/leading-item math, controller leading item, clamping, animate/jump-to-item, lazy builder, infinite wrap, weight validation, tap/splash wiring. | Gesture-driven fling and drag sequences, spring animation timing, reverse/RTL paint order, cross-host golden verification. |
| Material list tiles and expansion | `src/Plumix.Tests/MaterialListTileTests.cs`, `MaterialRadioExpansionTileTests.cs`, `MaterialExpansionPanelTests.cs` | Strict `ListTile` API/theme precedence, M2/M3 defaults, state colors, exact LTR/RTL slot geometry, heights, intrinsic/dry layout, dividers, ink, input, and semantics; strict `RadioListTile` registry/callback ownership, affinity, scale, adaptive control, expanded forwarding surface, and checked merged semantics; checkbox/switch row states and density; strict `ExpansionTile` API/theme copy/lerp/capture, direct M2/M3 roles, directional body geometry, per-side/custom shapes, `Expansible` animation/controller lookup, `PageStorage`, disabled/programmatic flow, state forwarding, and platform semantics; strict `ExpansionPanelList` defaults, composition, pointer gating, colors, radio callbacks, gaps, and transitions. | Cross-host ListTile/ExpansionTile golden verification. |
| Material disclosure and steppers | `src/Plumix.Tests/MaterialExpandIconTests.cs`, `MaterialStepperTests.cs` | Strict `ExpandIcon` API/defaults, M2/M3 enabled/disabled and custom colors, callback ownership, half-turn timing, directional padding, action-specific hints, zero-area layout; plus strict `Stepper` directional/state/style API, direct light/dark M2/M3 scheme roles, control styles, gradient/circular-border paint, exact connector geometry, custom old/new icon transitions, disabled taps, animated `ensureVisible` scrolling, shared body/text/size transitions, and state-maintaining horizontal panels. | Cross-host golden verification of transition pixels. |
| Material about and licenses | `src/Plumix.Tests/MaterialAboutTests.cs` | License parsing/registry laziness, application metadata/actions/zero-area rendering, list-tile entry and dense heights, modal lifecycle and root/nested navigator routing, adaptive Cupertino-vs-Material actions per platform, package grouping/sorting/plurals, `_AboutProgram` vertical rhythm, safe-area vs gutter padding, paragraph indent/centering, single-scrollbar primary list, stream-error surfacing, nested-vs-lateral master-detail layout at the 840 px breakpoint, lateral detail swapping, and per-Material-version title theming. Also covers the `Scheduler` task queue and `Priority`. | Flutter's debug `Timeline`/`FlutterError.reportError` instrumentation (see `docs/ai/DIVERGENCES.md`). |
| Material Divider | `src/Plumix.Tests/MaterialDividerTests.cs` | `Divider`/`VerticalDivider` non-negative guards, direct M2/M3 color roles, source container composition, hairlines, null-context border sides, widget/local/global theme precedence, theme copy/lerp/capture, physical and directional per-corner radii, RTL indents, zero-area layout, and horizontal/vertical geometry. | — |
| Core/Material selection handles | `src/Plumix.Tests/SelectionOverlayTests.cs`, `MaterialTextSelectionControlsTests.cs`, `MaterialTextFieldTests.cs` | `SelectionOverlay` and render-bound `TextSelectionOverlay` show/hide handles, toolbar and magnifier; retained endpoint/line/viewport geometry; collapsed visibility, live rebuilds, touch-gated drags, Android haptics/non-crossing policy, interactive geometry, Material handle paint, select-all rules, and clipboard gating. | Exact bidi box styles and cross-host transformed-line golden verification remain. |
| Core/Material tooltips and Material Badge | `src/Plumix.Tests/RawTooltipTests.cs`, `MaterialTooltipTests.cs`, `MaterialBadgeTests.cs` | Badge direct M3 error/on-error roles, widget/theme/default precedence, count validation, stadium sizing, narrow-child geometry, physical/logical/mixed alignment lerp, LTR/RTL placement, and clipping; plus raw/material tooltip portal placement, viewport flipping, custom delegates, triggers, timing, feedback, semantic events, global dismissal, visibility scopes, exact theme copy/lerp/capture, source diagnostics, arbitrary decorations, directional padding/margin, rich-message span rendering, pointer policy, and deferred cursor behavior. | Cross-host golden verification of rich `WidgetSpan` baselines and custom shape antialiasing. |
| Core diagnostics | `src/Plumix.Tests/DiagnosticsTests.cs` | Flutter's own `toStringDeep` goldens for every `DiagnosticsTreeStyle` (dense/sparse/offstage/transition/error/whitespace/flat/singleLine/errorProperty), with and without properties, plus mixed-style composition and the stretched error header/footer; the `TextTreeRenderer` word-wrap engine at width 40 (no word splitting, `allowWrap: false`, unwrappable URLs, multi-line values, prefix accounting); supplied-prefix invariants; `Diagnosticable` `ToString`/`ToStringShort`/`ToStringShallow` level filtering; every property subclass's description/level/filter rules including the `kNoDefaultValue`-vs-null-default distinction; `ToJsonMap` key presence and the serialization delegate's depth/properties/truncation behavior; and `Widget`'s dense style and keyed short name. | Release-mode elision has no counterpart (see `docs/ai/DIVERGENCES.md`). |
| Material CircleAvatar | `src/Plumix.Tests/MaterialCircleAvatarTests.cs` | Constructor contracts, radius constraints, direct M3 `ColorScheme` precedence, M2 brightness colors and typography, circular foreground/background image composition, error fallback, and implicit color/diameter transitions. | Animated image codecs and isolated additive crossfade blending remain backend gaps. |
| Material chips | `src/Plumix.Tests/MaterialChipTests.cs` | `Chip`/`ActionChip`/`ChoiceChip`/`FilterChip`/`InputChip` guards, exact M2 derived colors, direct M3 flat/elevated roles, theme copy/lerp/capture plus widget/theme/state precedence, body/delete callbacks, selection/disabled semantics, localization, painted checkmarks/scrims, three-slot intrinsic/dry layout, exact LTR/RTL offsets and hit regions, custom constraints, density, minimum delete semantics, and independent forward/reverse enable/select/avatar/delete choreography. | Cross-host golden verification of animated checkmark and disabled-layer pixels. |
| Material grid tiles | `src/Plumix.Tests/MaterialGridTileTests.cs`, `MaterialGridTileBarTests.cs` | Strict `GridTile` constructor/defaults, direct-child branch, ordered overlay composition, and positioned header/footer geometry; strict `GridTileBar` API, heights, typography, merged icons, backgrounds, inherited direction, logical padding, RTL slot order, and zero-area layout. | Cross-host visual verification with complex custom title widgets. |
| Semantics tree | `src/Plumix.Tests/SemanticsTreeTests.cs`, `SemanticsTraversalTests.cs`, `MetaDataIndexedSemanticsTests.cs`, `TableTests.cs` | The fragment compiler: which render objects merge up versus form nodes, sibling conflicts forcing node formation, `ChildConfigurationsDelegate` receiving descendant merge-up configurations, sibling merge groups and incomplete fragments (identity, geometry, clips), blocking siblings and blocked-branch dirty parent data, dirty propagation and id reuse. Geometry: local `Rect` plus `Transform`, `GlobalRect`, distinct semantics/paint clips, hidden versus clipped-out nodes, synthesized table row/cell transforms. Traversal: paint-order fallback without a text direction, LTR/RTL grouping, sort keys over geometry. Plus actions, `AreUserActionsBlocked`, hit-test metadata, index annotation, `SemanticsHitTestBehavior` defaults/propagation/compatibility, the `AssembleSemanticsNode` boundary hook, and `SemanticsTag` identity/tagging. | End-to-end assistive flows with platform accessibility tooling. |
| Host semantics bridge | `src/Plumix.Tests/FlutterHostSemanticsTests.cs` | `FlutterHost` semantics runtime surface and host-level action routing. | Platform adapter wiring to native accessibility APIs. |
| Mouse regions | `src/Plumix.Tests/MouseRegionTests.cs` | `MouseRegion` defaults, and `onHover` dispatch from ordinary pointer routing: it fires on the entering hover event and on every later move inside, never on exit, and reads the callback from the current widget after an update. | `hitTestBehavior`, `validForMouseTracker`, and frame-driven enter/exit when the region moves under a stationary pointer. |
| Gestures and hit testing | `src/Plumix.Tests/GesturePipelineTests.cs`, `TapAndDragGestureTests.cs` | Transform/clip hit testing, tap/double-tap/down/cancel dispatch, arena conflicts (losers rejected before the winner is accepted), drag deltas/velocity, fling distance/velocity floors and ceilings, down-then-cancel completion, hover enter/exit; plus the tap-and-drag family: consecutive-tap counting with the `kDoubleTapTimeout`/`kDoubleTapSlop`/`maxConsecutiveTap` resets, pan and precise-pointer slop thresholds, the past-hit-slop-but-not-drag-distance drag, the horizontal recognizer's already-won off-axis acceptance, eager vs deferred arena victory, pointer-cancel reset, the `kPressTimeout` deadline's consecutive-tap-only resolution, secondary-button and second-pointer rejection, and the drag-end position/zero velocity contract. | Multi-pointer interactions, cancellation races, and transformed-ancestor drag accumulation. |
| Navigation | `src/Plumix.Tests/NavigationTests.cs`, `NavigatorOverlayTests.cs`, `NavigatorPagesTests.cs`, `ModalRouteAspectTests.cs`, `HeroNavigatorTests.cs`, `MaterialDialogTests.cs`, `MaterialThemeAnimationTests.cs`, `MaterialPageTransitionsTests.cs` | Stack mutations, named routes, observers (including `DidChangeTop` and the push-before-remove ordering), nested back dispatch, transition-route motion, Material platform selection, delegated transitions, predictive back, hero flights, the overlay-backed `[barrier, scope]` installation and its flags, declarative `Pages` routing (initial list, keyed reuse, pageless attachment, `OnDidRemovePage`, `Page.CanPop`) with the `DefaultTransitionDelegate` and custom-delegate validation, and the aspect-scoped `ModalRoute.*Of` rebuild rules plus route focus-scope/traversal-edge wiring. | Interactive Cupertino edge swipes and process-level route restoration (no host restoration channel yet). |
| Router and route information | `src/Plumix.Tests/RouterTests.cs` | Synchronous and asynchronous parse/delegate flows, stale-parse and stale-pop transaction arbitration, `Router.Of`/`MaybeOf` lookup and errors, `Router.WithConfig`, the delegate-over-provider precedence on re-parse, the back-button dispatcher priority chain (`TakePriority`/`DeferTo`/`Forget`, nested children, recursive priority) and `BackButtonListener` innermost-wins/fall-through, one-report-per-frame reporting with `Neglect`/`Navigate` types and parser redirects, parser dependency vs non-dependency reads, `PlatformRouteInformationProvider` `replace` decisions and uri equivalence, restoration through `SetRestoredRoutePath`, and `WidgetsApp.Router` mode gating. | No host ships a navigation channel, so real browser history and platform deep links are unverified; asynchronous parsers are pumped rather than driven by a single-threaded scheduler. |
| Focus, keyboard, actions, and shortcuts | `src/Plumix.Tests/FocusTests.cs`, `HardwareKeyboardTests.cs`, `KeyboardListenerTests.cs`, `ActionsShortcutsTests.cs`, `DefaultTextEditingShortcutsTests.cs`, `FocusableActionDetectorTests.cs`, `FlutterHostInputTests.cs` | Focus ownership, descendant focus/traversal policy, input-mode highlights, detector callbacks/gating, key dispatch/listeners, typed actions, activators, nested/modal shortcuts, callbacks, and deferred registry entries; plus the seven `DefaultTextEditingShortcuts` platform maps (home/end line vs document vs scroll, `continuesAtWrap` per platform, Xerox and IBM CUA clipboard tables, Apple meta/alt word and line movement, emacs control bindings, macOS page-scroll intents, the numLock-dependent Linux numpad maps, deletion intents, the iOS/macOS/web disabling maps and their selection rule, the IME space/enter passthrough, and `intentForMacOSSelector`); plus the keyboard identity stack: generated logical/physical key tables (labels, debug names, planes, synonyms, control characters), the `KeyDownEvent`/`KeyUpEvent`/`KeyRepeatEvent` field contract, `HardwareKeyboard` pressed-key/lock-mode state and irregular-event tolerance, handler ordering/duplicates/copy-on-write/exception reporting, `RawKeyboard` modifier synchronization and listeners, `KeyEventManager` synthesized sync events and skip filtering, and the Avalonia host key map. | Rotated/non-invertible transforms, complete traversal-policy groups, deep scope hierarchies, and per-platform native raw key payloads. |
| Text input service | `src/Plumix.Tests/TextInputServiceTests.cs` | `TextInputType` variants/indices/equality/`ToJson`/`ToString`, `TextInputActionType` wire round-trip and the throwing parse, `TextInputConfiguration` defaults/`CopyWith`/equality/`ToJson`/hint-locale tags, `TextInputStyle` equality and `fontWeightIndex`/`textAlignIndex`/`textDirectionIndex` serialization, `SelectionRect`/`KeyboardInsertedContent`, `TextSelection` affinity rules and `TextEditingValue.Replaced`/`IsComposingRangeValid`, the whole inbound `flutter/textinput` dispatch (reattach, focus/connection-closed, commitContent, selectors, private commands, floating cursor, placeholders, scribble), the `TextEditingDelta` family incl. diagnostics and out-of-bounds `Apply`, `TextInputControl` attach/detach/state fan-out with the platform `TextInputType.none` substitution and microtask-deferred hide, connection dedup caches and rect sanitization, and every channel error context. | Keystroke delivery through a connection is untested because no host installs a control (`DIVERGENCES.md`). |
| Editable text input | `src/Plumix.Tests/TextInputTests.cs` | Focused input delivery, selection/caret editing, IME composition lifecycle and host preedit bridge, multiline editing, word/paragraph/clipboard shortcuts, grapheme-aware behavior. | Visual bidi navigation and platform text-action menu parity. |
| Autofill | `src/Plumix.Tests/AutofillTests.cs` | `AutofillConfiguration` defaults/equality/`ToJson` and the `autofill` key omission, `AutofillScope.Attach`'s `fields` payload and enabled-client assert, inbound `updateEditingStateWithTag` routing (mismatched client id, unknown/disabled tag), `TextInput.FinishAutofillContext` control fan-out, `AutofillGroup` client sets across nesting/toggling/reparenting, topmost-only dispose actions, and `EditableText` registration, autofill write-through and the keyboard-type/autocorrect hint inference on Apple and non-Apple platforms. | No host answers `flutter/textinput` yet, so an end-to-end platform autofill is unverified. |
| Scroll/slivers core | `src/Plumix.Tests/ScrollPipelineTests.cs`, `SliverHeaderTests.cs`, `SliverOpacityTests.cs`, `VisibilityTests.cs`, `DecoratedPinnedSliverTests.cs`, `SliverGroupTests.cs`, `SliverFillTests.cs`, `IntrinsicQueryParityTests.cs` | Scroll physics/direction/activity, overlapping viewport/sliver layout, cache/keep-alive lifecycle, proxy/decorated/fill/group/header geometry, side-effect-free fill/header child queries, prototype/varied extents, snap modes, opacity/visibility, hit testing, semantics policies, physics-driven drag registration and wheel gating, raw-view deferred-loading thresholds, axis targeting, forced-pixel implied velocity, and the `IScrollContext` contract (`ShouldIgnorePointer` per activity incl. trackpad drags and out-of-range settles, the `IgnorePointer` the scrollable drives across hold/drag/fling, `SetCanDrag`/`SetSemanticsActions`/`SaveOffset`, constructor-time `oldPosition` absorption, `CreateScrollPosition(physics, context, oldPosition)`, and the notification context sitting inside the `ScrollBehavior` wrappers so `Scrollable.Of(notification.Context)` resolves). | Very large child counts, rapid direction changes, negative overscroll. |
| Scrollable semantics | `src/Plumix.Tests/ScrollSemanticsTests.cs` | The two-pane split (`RenderViewport.UseTwoPaneSemantics`/`ExcludeFromScrolling` tagging, scrolling node versus non-scrolling siblings), the directional scroll actions (registered on the `RenderSemanticsGestureHandler` the scrollable's `RawGestureDetector` creates, merged up into the scroll node) and their leading/middle/trailing availability, `scrollToOffset` on both axes and its `allowImplicitScrolling` gate, `hasImplicitScrolling`, scroll extents/position reporting, `scrollChildCount`/`scrollIndex` including `semanticIndexOffset` and separator suppression, semantic scrolling running through the real drag physics, pinned persistent headers and partially-scrolled `PinnedHeaderSliver` tagging, cache-extent children reported hidden rather than dropped, and `excludeFromSemantics`. | Platform accessibility tooling end-to-end; viewport child ordering (sliver order rather than paint order, see `DIVERGENCES.md`). |
| Viewport reveal protocol | `src/Plumix.Tests/ViewportRevealTests.cs` | `RevealedOffset.ClampOffset` edge selection, `RenderAbstractViewport.MaybeOf`/`Of`/`DefaultCacheExtent`, `GetOffsetToReveal` numbers for the box and sliver viewports (down/right, explicit `rect`, half alignment, sliver padding, foreign targets, axis mismatch), `Scrollable.EnsureVisible` leading/trailing/clamped/no-op cases and both keep-visible policies, nested `ShowOnScreen` through two scrollables, nearest-edge reveal, `allowImplicitScrolling` suppression, the semantics `ShowOnScreen` fallback and `SemanticsConfiguration.OnShowOnScreen`, pinned-header infinite offsets and pinned-extent discounting, sliver child-position accessors, and `PersistentHeaderShowOnScreenConfiguration` validation. | Cross-host golden verification of reveal rects. |
| Viewport offset and layout | `src/Plumix.Tests/ViewportPortTests.cs`, `ScrollPipelineTests.cs` | `ViewportOffset.Fixed`/`Zero` semantics, `CorrectBy` not notifying while `JumpTo` does, `debugFillDescription`, `flipScrollDirection`, the anchor-folded `applyContentDimensions` values, `center`-anchored two-directional layout with reverse-growth constraints and negative minimum extents, the negated reverse-sequence scroll-offset correction, `ScrollOffsetOf`/`IndexOfFirstChild`/`LabelForChild`, default and `lastIsTop` paint and hit-test order, pixel and viewport cache extents through `describeSemanticsClip`, `describeApproximatePaintClip` under `clipBehavior`, the two intrinsics error messages, cross-axis-direction validation and propagation, offset swapping, `getOffsetToReveal` for boxes, pinned slivers and reverse growth, and `RenderShrinkWrappingViewport` sizing/clamping/logical offsets/unbounded-cross-axis rejection. | Two-dimensional viewports. |
| Scroll widget infrastructure | `src/Plumix.Tests/ScrollInfrastructureTests.cs`, `ScrollNotificationObserverTests.cs`, `MaterialScrollbarTests.cs`, `MaterialDataTableTests.cs` | Typed/cancellable notification bubbling, mutation-safe observer listeners, initial/dimension metrics, primary controllers, keep-alive, list/grid construction, nested viewport shrink-wrap, and scrollbar metrics. | Nested scrollables and multi-position primary-controller nuances. |
| Sample state behavior | `src/Plumix.Tests/SampleCounterStateTests.cs` | Counter model notifications and scope dependency behavior. | End-to-end sample page regressions across demo routes. |

## How to Use for Iterative Work

1. Pick feature area row.
2. Read listed tests before opening implementation files.
3. Update or add tests in same row when behavior changes; keep the row summary within the cell-size rule.
4. If no row fits, add one before shipping the feature.

## Porting Rule (Dart -> C#)

- For control/widget ports, tests must validate Dart-parity-critical behavior from `docs/ai/PORTING_MODE.md`:
  - defaults,
  - interaction states,
  - high-risk constraint/layout scenarios.
