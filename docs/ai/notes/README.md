# Archived Feature Notes

Historical per-iteration task notes. They are a journal, not rules: read them only when investigating why a specific behavior exists.

- Notes dated before 2026-07-01 reference the pre-rename layout (`src/Flutter*`, `src/Sample/Flutter.Net`). Current paths are `src/Plumix*` and `src/Sample/Plumix.Sample`.
- Divergence entries inside these notes may already be closed by later notes. The living registry is `docs/ai/DIVERGENCES.md`; migrate still-active divergences there when touching the related control.
- New notes are required only for iterations that end blocked (unclosed parity) or introduce a divergence — see `AGENTS.md`.
